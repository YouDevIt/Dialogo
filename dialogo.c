/* A packrat parser generated by PackCC 1.3.1 */

#ifdef _MSC_VER
#undef _CRT_SECURE_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#endif /* _MSC_VER */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef _MSC_VER
#if defined __GNUC__ && defined _WIN32 /* MinGW */
#define strnlen(str, maxlen) pcc_strnlen(str, maxlen)
static size_t pcc_strnlen(const char *str, size_t maxlen) {
    size_t i;
    for (i = 0; i < maxlen && str[i]; i++);
    return i;
}
#endif /* defined __GNUC__ && defined _WIN32 */ 
#endif /* !_MSC_VER */

#include "dialogo.h"


#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Array rooms, objects;

char* last_name = NULL;
int last_genre = 0;

void array_init(struct Array* array, int size) {
        array->size = size;
        array->len = 0;
        array->data = malloc(sizeof(char*)*size);
        for(int ct = 0; ct < size; ct++) (array->data)[ct] = NULL;
}
void array_free(struct Array* array) {
        for(int ct = 0; ct < array->len; ct++)
                if(array->data[ct] != NULL) {
                        free(array->data[ct]);
                        array->data[ct] = NULL;
                };
        array->len = 0;
}
char* array_add(struct Array* array, char* str) {
        if(array->size == array->len) {
                array->size *= 2;
                array->data = realloc(array->data,sizeof(char*)*array->size);
        }
        return (array->data[array->len++] = strdup(str));
}
char* array_get(struct Array* array, int idx) {return idx < 0 || (idx >= array->len)? NULL: array->data[idx];}
int array_contains(struct Array* array, char* str) {
        for(int ct = 0; ct < array->len; ct++)
                if(strcmp(str,array->data[ct]) == 0)
                        return 1;
        return 0;
}

void init() {
        array_init(&rooms,100);
        array_init(&objects,100);
        print_start();
}

void final() {
        array_free(&rooms);
        array_free(&objects);
}

void set_last_name(char* name) {
        if(last_name != NULL)
                free(last_name);
        last_name = strdup(name);
}

void print_start() {
}
void print_room(char* name) {
        array_add(&rooms,name);
        printf("(name #%s) %s\n",name,name);
        printf("(room #%s)\n",name);
        if(last_genre == 1)
                printf("(female #%s)\n\n",name);
}
void print_supporter(char* name) {
        array_add(&objects,name);
        printf("(name #%s) %s\n",name,name);
        printf("(supporter #%s)\n",name);
        printf("(on-seat #%s)\n",name);
        if(last_genre == 1)
                print_female(name);
}
void print_container(char* name) {
        array_add(&objects,name);
        printf("(name #%s) %s\n",name,name);
        printf("(container #%s)\n",name);
        printf("(item #%s)\n",name);
        printf("(#%s is handled)\n",name);
        printf("(openable #%s)\n",name);
        printf("(#%s is open)\n",name);
        if(last_genre == 1)
                print_female(name);
}
void print_item(char* name) {
        array_add(&objects,name);
        printf("(name #%s) %s\n",name,name);
        printf("(item #%s)\n\n",name);
        printf("(#%s is handled)\n\n",name);
        if(last_genre == 1)
                print_female(name);
}
void print_player(char* name) {
        array_add(&objects,name);
        printf("(current player #%s)\n\n",name);
}
void print_female(char* name) {
        printf("(female #%s)\n\n",name);
}
void print_edible(char* name) {
        printf("(edible #%s)\n",name);
}
void print_open(char* name) {
        printf("(#%s is open)\n",name);
}
void print_close(char* name) {
        printf("(#%s is closed)\n",name);
}
void print_is_in(char* name, char* loc) {
        printf("(#%s is #in #%s)\n\n",name,loc);
}
void print_is_on(char* name, char* loc) {
        printf("(#%s is #on #%s)\n\n",name,loc);
}
void print_descr(char* name, char* str) {
        printf("(descr #%s) %s\n\n",name,str);
}
void print_look(char* name, char* str) {
        printf("(look #%s) %s\n\n",name,str);
}
void print_appearance(char* name, char* str) {
        printf("(appearance #%s) %s\n\n",name,str);
}
char* dirs[] = {"up","in","north","northeast","east","southeast","down","out","south","southwest","west","northwest"};
void print_fromto(int dir, char* locfrom, char* locto) {
        printf("(from #%s go #%s to #%s)\n", locfrom, dirs[dir], locto);
}
void print_fromtoandback(int dir, char* locfrom, char* locto) {
        printf("(from #%s go #%s to #%s)\n", locfrom, dirs[dir], locto);
        printf("(from #%s go #%s to #%s)\n", locto, dirs[(dir+6)%12], locfrom);
}


#ifndef PCC_BUFFERSIZE
#define PCC_BUFFERSIZE 256
#endif /* !PCC_BUFFERSIZE */

#ifndef PCC_ARRAYSIZE
#define PCC_ARRAYSIZE 2
#endif /* !PCC_ARRAYSIZE */

typedef enum pcc_bool_tag {
    PCC_FALSE = 0,
    PCC_TRUE
} pcc_bool_t;

typedef struct pcc_char_array_tag {
    char *buf;
    int max;
    int len;
} pcc_char_array_t;

typedef struct pcc_range_tag {
    int start;
    int end;
} pcc_range_t;

typedef int pcc_value_t;

typedef void *pcc_auxil_t;

typedef struct pcc_value_table_tag {
    pcc_value_t *buf;
    int max;
    int len;
} pcc_value_table_t;

typedef struct pcc_value_refer_table_tag {
    pcc_value_t **buf;
    int max;
    int len;
} pcc_value_refer_table_t;

typedef struct pcc_capture_tag {
    pcc_range_t range;
    char *string; /* mutable */
} pcc_capture_t;

typedef struct pcc_capture_table_tag {
    pcc_capture_t *buf;
    int max;
    int len;
} pcc_capture_table_t;

typedef struct pcc_capture_const_table_tag {
    const pcc_capture_t **buf;
    int max;
    int len;
} pcc_capture_const_table_t;

typedef struct pcc_thunk_tag pcc_thunk_t;
typedef struct pcc_thunk_array_tag pcc_thunk_array_t;

typedef void (*pcc_action_t)(pcc_context_t *, pcc_thunk_t *, pcc_value_t *);

typedef enum pcc_thunk_type_tag {
    PCC_THUNK_LEAF,
    PCC_THUNK_NODE
} pcc_thunk_type_t;

typedef struct pcc_thunk_leaf_tag {
    pcc_value_refer_table_t values;
    pcc_capture_const_table_t capts;
    pcc_capture_t capt0;
    pcc_action_t action;
} pcc_thunk_leaf_t;

typedef struct pcc_thunk_node_tag {
    const pcc_thunk_array_t *thunks; /* just a reference */
    pcc_value_t *value; /* just a reference */
} pcc_thunk_node_t;

typedef union pcc_thunk_data_tag {
    pcc_thunk_leaf_t leaf;
    pcc_thunk_node_t node;
} pcc_thunk_data_t;

struct pcc_thunk_tag {
    pcc_thunk_type_t type;
    pcc_thunk_data_t data;
};

struct pcc_thunk_array_tag {
    pcc_thunk_t **buf;
    int max;
    int len;
};

typedef struct pcc_thunk_chunk_tag {
    pcc_value_table_t values;
    pcc_capture_table_t capts;
    pcc_thunk_array_t thunks;
    int pos;
} pcc_thunk_chunk_t;

typedef struct pcc_lr_entry_tag pcc_lr_entry_t;

typedef enum pcc_lr_answer_type_tag {
    PCC_LR_ANSWER_LR,
    PCC_LR_ANSWER_CHUNK
} pcc_lr_answer_type_t;

typedef union pcc_lr_answer_data_tag {
    pcc_lr_entry_t *lr;
    pcc_thunk_chunk_t *chunk;
} pcc_lr_answer_data_t;

typedef struct pcc_lr_answer_tag pcc_lr_answer_t;

struct pcc_lr_answer_tag {
    pcc_lr_answer_type_t type;
    pcc_lr_answer_data_t data;
    int pos;
    pcc_lr_answer_t *hold;
};

typedef pcc_thunk_chunk_t *(*pcc_rule_t)(pcc_context_t *);

typedef struct pcc_rule_set_tag {
    pcc_rule_t *buf;
    int max;
    int len;
} pcc_rule_set_t;

typedef struct pcc_lr_head_tag pcc_lr_head_t;

struct pcc_lr_head_tag {
    pcc_rule_t rule;
    pcc_rule_set_t invol;
    pcc_rule_set_t eval;
    pcc_lr_head_t *hold;
};

typedef struct pcc_lr_memo_tag {
    pcc_rule_t rule;
    pcc_lr_answer_t *answer;
} pcc_lr_memo_t;

typedef struct pcc_lr_memo_map_tag {
    pcc_lr_memo_t *buf;
    int max;
    int len;
} pcc_lr_memo_map_t;

typedef struct pcc_lr_table_entry_tag {
    pcc_lr_head_t *head; /* just a reference */
    pcc_lr_memo_map_t memos;
    pcc_lr_answer_t *hold_a;
    pcc_lr_head_t *hold_h;
} pcc_lr_table_entry_t;

typedef struct pcc_lr_table_tag {
    pcc_lr_table_entry_t **buf;
    int max;
    int len;
} pcc_lr_table_t;

struct pcc_lr_entry_tag {
    pcc_rule_t rule;
    pcc_thunk_chunk_t *seed; /* just a reference */
    pcc_lr_head_t *head; /* just a reference */
};

typedef struct pcc_lr_stack_tag {
    pcc_lr_entry_t **buf;
    int max;
    int len;
} pcc_lr_stack_t;

struct pcc_context_tag {
    int pos;
    pcc_char_array_t buffer;
    pcc_lr_table_t lrtable;
    pcc_lr_stack_t lrstack;
    pcc_auxil_t auxil;
};

#ifndef PCC_ERROR
#define PCC_ERROR(auxil) pcc_error()
static void pcc_error(void) {
    fprintf(stderr, "Syntax error\n");
    exit(1);
}
#endif /* !PCC_ERROR */

#ifndef PCC_GETCHAR
#define PCC_GETCHAR(auxil) getchar()
#endif /* !PCC_GETCHAR */

#ifndef PCC_MALLOC
#define PCC_MALLOC(auxil, size) pcc_malloc_e(size)
static void *pcc_malloc_e(size_t size) {
    void *const p = malloc(size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* !PCC_MALLOC */

#ifndef PCC_REALLOC
#define PCC_REALLOC(auxil, ptr, size) pcc_realloc_e(ptr, size)
static void *pcc_realloc_e(void *ptr, size_t size) {
    void *const p = realloc(ptr, size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* !PCC_REALLOC */

#ifndef PCC_FREE
#define PCC_FREE(auxil, ptr) free(ptr)
#endif /* !PCC_FREE */

static char *pcc_strndup_e(pcc_auxil_t auxil, const char *str, size_t len) {
    const size_t m = strnlen(str, len);
    char *const s = (char *)PCC_MALLOC(auxil, m + 1);
    memcpy(s, str, m);
    s[m] = '\0';
    return s;
}

static void pcc_char_array__init(pcc_auxil_t auxil, pcc_char_array_t *array, int max) {
    array->len = 0;
    array->max = max;
    array->buf = (char *)PCC_MALLOC(auxil, array->max);
}

static void pcc_char_array__add(pcc_auxil_t auxil, pcc_char_array_t *array, char ch) {
    if (array->max <= 0) array->max = 1;
    while (array->max <= array->len) array->max <<= 1;
    array->buf = (char *)PCC_REALLOC(auxil, array->buf, array->max);
    array->buf[array->len++] = ch;
}

static void pcc_char_array__term(pcc_auxil_t auxil, pcc_char_array_t *array) {
    PCC_FREE(auxil, array->buf);
}

static void pcc_value_table__init(pcc_auxil_t auxil, pcc_value_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t *)PCC_MALLOC(auxil, table->max * sizeof(pcc_value_t));
}

static void pcc_value_table__resize(pcc_auxil_t auxil, pcc_value_table_t *table, int len) {
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_value_t *)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_value_t));
    }
    table->len = len;
}

static void pcc_value_table__term(pcc_auxil_t auxil, pcc_value_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_value_refer_table__init(pcc_auxil_t auxil, pcc_value_refer_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t **)PCC_MALLOC(auxil, table->max * sizeof(pcc_value_t *));
}

static void pcc_value_refer_table__resize(pcc_auxil_t auxil, pcc_value_refer_table_t *table, int len) {
    int i;
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_value_t **)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_value_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_value_refer_table__term(pcc_auxil_t auxil, pcc_value_refer_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_table__init(pcc_auxil_t auxil, pcc_capture_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_capture_t *)PCC_MALLOC(auxil, table->max * sizeof(pcc_capture_t));
}

static void pcc_capture_table__resize(pcc_auxil_t auxil, pcc_capture_table_t *table, int len) {
    int i;
    for (i = table->len - 1; i >= len; i--) PCC_FREE(auxil, table->buf[i].string);
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_capture_t *)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_capture_t));
    }
    for (i = table->len; i < len; i++) {
        table->buf[i].range.start = 0;
        table->buf[i].range.end = 0;
        table->buf[i].string = NULL;
    }
    table->len = len;
}

static void pcc_capture_table__term(pcc_auxil_t auxil, pcc_capture_table_t *table) {
    int i;
    for (i = table->len - 1; i >= 0; i--) PCC_FREE(auxil, table->buf[i].string);
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_const_table__init(pcc_auxil_t auxil, pcc_capture_const_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (const pcc_capture_t **)PCC_MALLOC(auxil, table->max * sizeof(const pcc_capture_t *));
}

static void pcc_capture_const_table__resize(pcc_auxil_t auxil, pcc_capture_const_table_t *table, int len) {
    int i;
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (const pcc_capture_t **)PCC_REALLOC(auxil, (pcc_capture_t **)table->buf, table->max * sizeof(const pcc_capture_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_capture_const_table__term(pcc_auxil_t auxil, pcc_capture_const_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static pcc_thunk_t *pcc_thunk__create_leaf(pcc_auxil_t auxil, pcc_action_t action, int valuec, int captc) {
    pcc_thunk_t *const thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_LEAF;
    pcc_value_refer_table__init(auxil, &thunk->data.leaf.values, valuec);
    pcc_value_refer_table__resize(auxil, &thunk->data.leaf.values, valuec);
    pcc_capture_const_table__init(auxil, &thunk->data.leaf.capts, captc);
    pcc_capture_const_table__resize(auxil, &thunk->data.leaf.capts, captc);
    thunk->data.leaf.capt0.range.start = 0;
    thunk->data.leaf.capt0.range.end = 0;
    thunk->data.leaf.capt0.string = NULL;
    thunk->data.leaf.action = action;
    return thunk;
}

static pcc_thunk_t *pcc_thunk__create_node(pcc_auxil_t auxil, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    pcc_thunk_t *const thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_NODE;
    thunk->data.node.thunks = thunks;
    thunk->data.node.value = value;
    return thunk;
}

static void pcc_thunk__destroy(pcc_auxil_t auxil, pcc_thunk_t *thunk) {
    if (thunk == NULL) return;
    switch (thunk->type) {
    case PCC_THUNK_LEAF:
        PCC_FREE(auxil, thunk->data.leaf.capt0.string);
        pcc_capture_const_table__term(auxil, &thunk->data.leaf.capts);
        pcc_value_refer_table__term(auxil, &thunk->data.leaf.values);
        break;
    case PCC_THUNK_NODE:
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, thunk);
}

static void pcc_thunk_array__init(pcc_auxil_t auxil, pcc_thunk_array_t *array, int max) {
    array->len = 0;
    array->max = max;
    array->buf = (pcc_thunk_t **)PCC_MALLOC(auxil, array->max * sizeof(pcc_thunk_t *));
}

static void pcc_thunk_array__add(pcc_auxil_t auxil, pcc_thunk_array_t *array, pcc_thunk_t *thunk) {
    if (array->max <= 0) array->max = 1;
    while (array->max <= array->len) array->max <<= 1;
    array->buf = (pcc_thunk_t **)PCC_REALLOC(auxil, array->buf, array->max * sizeof(pcc_thunk_t *));
    array->buf[array->len++] = thunk;
}

static void pcc_thunk_array__revert(pcc_auxil_t auxil, pcc_thunk_array_t *array, int len) {
    while (array->len > len) {
        array->len--;
        pcc_thunk__destroy(auxil, array->buf[array->len]);
    }
}

static void pcc_thunk_array__term(pcc_auxil_t auxil, pcc_thunk_array_t *array) {
    int i;
    for (i = array->len - 1; i >= 0; i--) pcc_thunk__destroy(auxil, array->buf[i]);
    PCC_FREE(auxil, array->buf);
}

static pcc_thunk_chunk_t *pcc_thunk_chunk__create(pcc_auxil_t auxil) {
    pcc_thunk_chunk_t *const chunk = (pcc_thunk_chunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_chunk_t));
    pcc_value_table__init(auxil, &chunk->values, PCC_ARRAYSIZE);
    pcc_capture_table__init(auxil, &chunk->capts, PCC_ARRAYSIZE);
    pcc_thunk_array__init(auxil, &chunk->thunks, PCC_ARRAYSIZE);
    chunk->pos = 0;
    return chunk;
}

static void pcc_thunk_chunk__destroy(pcc_auxil_t auxil, pcc_thunk_chunk_t *chunk) {
    if (chunk == NULL) return;
    pcc_thunk_array__term(auxil, &chunk->thunks);
    pcc_capture_table__term(auxil, &chunk->capts);
    pcc_value_table__term(auxil, &chunk->values);
    PCC_FREE(auxil, chunk);
}

static void pcc_rule_set__init(pcc_auxil_t auxil, pcc_rule_set_t *set, int max) {
    set->len = 0;
    set->max = max;
    set->buf = (pcc_rule_t *)PCC_MALLOC(auxil, set->max * sizeof(pcc_rule_t));
}

static int pcc_rule_set__index(pcc_auxil_t auxil, const pcc_rule_set_t *set, pcc_rule_t rule) {
    int i;
    for (i = 0; i < set->len; i++) {
        if (set->buf[i] == rule) return i;
    }
    return -1;
}

static pcc_bool_t pcc_rule_set__add(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    const int i = pcc_rule_set__index(auxil, set, rule);
    if (i >= 0) return PCC_FALSE;
    if (set->max <= 0) set->max = 1;
    while (set->max <= set->len) set->max <<= 1;
    set->buf = (pcc_rule_t *)PCC_REALLOC(auxil, set->buf, set->max * sizeof(pcc_rule_t));
    set->buf[set->len++] = rule;
    return PCC_TRUE;
}

static pcc_bool_t pcc_rule_set__remove(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    const int i = pcc_rule_set__index(auxil, set, rule);
    if (i < 0) return PCC_FALSE;
    memmove(set->buf + i, set->buf + (i + 1), (set->len - (i + 1)) * sizeof(pcc_rule_t));
    return PCC_TRUE;
}

static void pcc_rule_set__clear(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    set->len = 0;
}

static void pcc_rule_set__copy(pcc_auxil_t auxil, pcc_rule_set_t *set, const pcc_rule_set_t *src) {
    int i;
    pcc_rule_set__clear(auxil, set);
    for (i = 0; i < src->len; i++) {
        pcc_rule_set__add(auxil, set, src->buf[i]);
    }
}

static void pcc_rule_set__term(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    PCC_FREE(auxil, set->buf);
}

static pcc_lr_head_t *pcc_lr_head__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_head_t *const head = (pcc_lr_head_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_head_t));
    head->rule = rule;
    pcc_rule_set__init(auxil, &head->invol, PCC_ARRAYSIZE);
    pcc_rule_set__init(auxil, &head->eval, PCC_ARRAYSIZE);
    head->hold = NULL;
    return head;
}

static void pcc_lr_head__destroy(pcc_auxil_t auxil, pcc_lr_head_t *head) {
    if (head == NULL) return;
    pcc_lr_head__destroy(auxil, head->hold);
    pcc_rule_set__term(auxil, &head->eval);
    pcc_rule_set__term(auxil, &head->invol);
    PCC_FREE(auxil, head);
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr);

static pcc_lr_answer_t *pcc_lr_answer__create(pcc_auxil_t auxil, pcc_lr_answer_type_t type, int pos) {
    pcc_lr_answer_t *answer = (pcc_lr_answer_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_answer_t));
    answer->type = type;
    answer->pos = pos;
    answer->hold = NULL;
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        answer->data.lr = NULL;
        break;
    case PCC_LR_ANSWER_CHUNK:
        answer->data.chunk = NULL;
        break;
    default: /* unknown */
        PCC_FREE(auxil, answer);
        answer = NULL;
    }
    return answer;
}

static void pcc_lr_answer__set_chunk(pcc_auxil_t auxil, pcc_lr_answer_t *answer, pcc_thunk_chunk_t *chunk) {
    pcc_lr_answer_t *const a = pcc_lr_answer__create(auxil, answer->type, answer->pos);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        a->data.lr = answer->data.lr;
        break;
    case PCC_LR_ANSWER_CHUNK:
        a->data.chunk = answer->data.chunk;
        break;
    default: /* unknown */
        break;
    }
    a->hold = answer->hold;
    answer->hold = a;
    answer->type = PCC_LR_ANSWER_CHUNK;
    answer->data.chunk = chunk;
}

static void pcc_lr_answer__destroy(pcc_auxil_t auxil, pcc_lr_answer_t *answer) {
    if (answer == NULL) return;
    pcc_lr_answer__destroy(auxil, answer->hold);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        pcc_lr_entry__destroy(auxil, answer->data.lr);
        break;
    case PCC_LR_ANSWER_CHUNK:
        pcc_thunk_chunk__destroy(auxil, answer->data.chunk);
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, answer);
}

static void pcc_lr_memo_map__init(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, int max) {
    map->len = 0;
    map->max = max;
    map->buf = (pcc_lr_memo_t *)PCC_MALLOC(auxil, map->max * sizeof(pcc_lr_memo_t));
}

static int pcc_lr_memo_map__index(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    int i;
    for (i = 0; i < map->len; i++) {
        if (map->buf[i].rule == rule) return i;
    }
    return -1;
}

static void pcc_lr_memo_map__put(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    const int i = pcc_lr_memo_map__index(auxil, map, rule);
    if (i >= 0) {
        pcc_lr_answer__destroy(auxil, map->buf[i].answer);
        map->buf[i].answer = answer;
    }
    else {
        if (map->max <= 0) map->max = 1;
        while (map->max <= map->len) map->max <<= 1;
        map->buf = (pcc_lr_memo_t *)PCC_REALLOC(auxil, map->buf, map->max * sizeof(pcc_lr_memo_t));
        map->buf[map->len].rule = rule;
        map->buf[map->len].answer = answer;
        map->len++;
    }
}

static pcc_lr_answer_t *pcc_lr_memo_map__get(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    const int i = pcc_lr_memo_map__index(auxil, map, rule);
    return (i >= 0) ? map->buf[i].answer : NULL;
}

static void pcc_lr_memo_map__term(pcc_auxil_t auxil, pcc_lr_memo_map_t *map) {
    int i;
    for (i = map->len - 1; i >= 0; i--) pcc_lr_answer__destroy(auxil, map->buf[i].answer);
    PCC_FREE(auxil, map->buf);
}

static pcc_lr_table_entry_t *pcc_lr_table_entry__create(pcc_auxil_t auxil) {
    pcc_lr_table_entry_t *const entry = (pcc_lr_table_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_table_entry_t));
    entry->head = NULL;
    pcc_lr_memo_map__init(auxil, &entry->memos, PCC_ARRAYSIZE);
    entry->hold_a = NULL;
    entry->hold_h = NULL;
    return entry;
}

static void pcc_lr_table_entry__destroy(pcc_auxil_t auxil, pcc_lr_table_entry_t *entry) {
    if (entry == NULL) return;
    pcc_lr_head__destroy(auxil, entry->hold_h);
    pcc_lr_answer__destroy(auxil, entry->hold_a);
    pcc_lr_memo_map__term(auxil, &entry->memos);
    PCC_FREE(auxil, entry);
}

static void pcc_lr_table__init(pcc_auxil_t auxil, pcc_lr_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_lr_table_entry_t **)PCC_MALLOC(auxil, table->max * sizeof(pcc_lr_table_entry_t *));
}

static void pcc_lr_table__resize(pcc_auxil_t auxil, pcc_lr_table_t *table, int len) {
    int i;
    for (i = table->len - 1; i >= len; i--) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_lr_table_entry_t **)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_lr_table_entry_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_lr_table__set_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    table->buf[index]->head = head;
}

static void pcc_lr_table__hold_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    head->hold = table->buf[index]->hold_h;
    table->buf[index]->hold_h = head;
}

static void pcc_lr_table__set_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    pcc_lr_memo_map__put(auxil, &table->buf[index]->memos, rule, answer);
}

static void pcc_lr_table__hold_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    answer->hold = table->buf[index]->hold_a;
    table->buf[index]->hold_a = answer;
}

static pcc_lr_head_t *pcc_lr_table__get_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return table->buf[index]->head;
}

static pcc_lr_answer_t *pcc_lr_table__get_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_rule_t rule) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return pcc_lr_memo_map__get(auxil, &table->buf[index]->memos, rule);
}

static void pcc_lr_table__shift(pcc_auxil_t auxil, pcc_lr_table_t *table, int count) {
    int i;
    if (count > table->len) count = table->len;
    for (i = 0; i < count; i++) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    memmove(table->buf, table->buf + count, (table->len - count) * sizeof(pcc_lr_table_entry_t *));
    table->len -= count;
}

static void pcc_lr_table__term(pcc_auxil_t auxil, pcc_lr_table_t *table) {
    int i;
    for (i = table->len - 1; i >= 0; i--) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    PCC_FREE(auxil, table->buf);
}

static pcc_lr_entry_t *pcc_lr_entry__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_entry_t *const lr = (pcc_lr_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_entry_t));
    lr->rule = rule;
    lr->seed = NULL;
    lr->head = NULL;
    return lr;
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr) {
    PCC_FREE(auxil, lr);
}

static void pcc_lr_stack__init(pcc_auxil_t auxil, pcc_lr_stack_t *stack, int max) {
    stack->len = 0;
    stack->max = max;
    stack->buf = (pcc_lr_entry_t **)PCC_MALLOC(auxil, stack->max * sizeof(pcc_lr_entry_t *));
}

static void pcc_lr_stack__push(pcc_auxil_t auxil, pcc_lr_stack_t *stack, pcc_lr_entry_t *lr) {
    if (stack->max <= 0) stack->max = 1;
    while (stack->max <= stack->len) stack->max <<= 1;
    stack->buf = (pcc_lr_entry_t **)PCC_REALLOC(auxil, stack->buf, stack->max * sizeof(pcc_lr_entry_t *));
    stack->buf[stack->len++] = lr;
}

static pcc_lr_entry_t *pcc_lr_stack__pop(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    return stack->buf[--stack->len];
}

static void pcc_lr_stack__term(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    PCC_FREE(auxil, stack->buf);
}

static pcc_context_t *pcc_context__create(pcc_auxil_t auxil) {
    pcc_context_t *const ctx = (pcc_context_t *)PCC_MALLOC(auxil, sizeof(pcc_context_t));
    ctx->pos = 0;
    pcc_char_array__init(auxil, &ctx->buffer, PCC_BUFFERSIZE);
    pcc_lr_table__init(auxil, &ctx->lrtable, PCC_BUFFERSIZE);
    pcc_lr_stack__init(auxil, &ctx->lrstack, PCC_ARRAYSIZE);
    ctx->auxil = auxil;
    return ctx;
}

static void pcc_context__destroy(pcc_context_t *ctx) {
    if (ctx == NULL) return;
    pcc_lr_stack__term(ctx->auxil, &ctx->lrstack);
    pcc_lr_table__term(ctx->auxil, &ctx->lrtable);
    pcc_char_array__term(ctx->auxil, &ctx->buffer);
    PCC_FREE(ctx->auxil, ctx);
}

static int pcc_refill_buffer(pcc_context_t *ctx, int num) {
    int n, c;
    n = ctx->buffer.len - ctx->pos;
    if (n >= num) return n;
    while (ctx->buffer.len < ctx->pos + num) {
        c = PCC_GETCHAR(ctx->auxil);
        if (c == EOF) break;
        pcc_char_array__add(ctx->auxil, &ctx->buffer, (char)c);
    }
    return ctx->buffer.len - ctx->pos;
}

static void pcc_commit_buffer(pcc_context_t *ctx) {
    memmove(ctx->buffer.buf, ctx->buffer.buf + ctx->pos, ctx->buffer.len - ctx->pos);
    ctx->buffer.len -= ctx->pos;
    pcc_lr_table__shift(ctx->auxil, &ctx->lrtable, ctx->pos);
    ctx->pos = 0;
}

static const char *pcc_get_capture_string(pcc_context_t *ctx, const pcc_capture_t *capt) {
    if (capt->string == NULL)
        ((pcc_capture_t *)capt)->string =
            pcc_strndup_e(ctx->auxil, ctx->buffer.buf + capt->range.start, capt->range.end - capt->range.start);
    return capt->string;
}

static pcc_bool_t pcc_apply_rule(pcc_context_t *ctx, pcc_rule_t rule, pcc_thunk_array_t *thunks, pcc_value_t *value) {
    static pcc_value_t null;
    pcc_thunk_chunk_t *c = NULL;
    const int p = ctx->pos;
    pcc_bool_t b = PCC_TRUE;
    pcc_lr_answer_t *a = pcc_lr_table__get_answer(ctx->auxil, &ctx->lrtable, p, rule);
    pcc_lr_head_t *h = pcc_lr_table__get_head(ctx->auxil, &ctx->lrtable, p);
    if (h != NULL) {
        if (a == NULL && rule != h->rule && pcc_rule_set__index(ctx->auxil, &h->invol, rule) < 0) {
            b = PCC_FALSE;
            c = NULL;
        }
        else if (pcc_rule_set__remove(ctx->auxil, &h->eval, rule)) {
            b = PCC_FALSE;
            c = rule(ctx);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos);
            a->data.chunk = c;
            pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
        }
    }
    if (b) {
        if (a != NULL) {
            ctx->pos = a->pos;
            switch (a->type) {
            case PCC_LR_ANSWER_LR:
                if (a->data.lr->head == NULL) {
                    a->data.lr->head = pcc_lr_head__create(ctx->auxil, rule);
                    pcc_lr_table__hold_head(ctx->auxil, &ctx->lrtable, p, a->data.lr->head);
                }
                {
                    int i;
                    for (i = ctx->lrstack.len - 1; i >= 0; i--) {
                        if (ctx->lrstack.buf[i]->head == a->data.lr->head) break;
                        ctx->lrstack.buf[i]->head = a->data.lr->head;
                        pcc_rule_set__add(ctx->auxil, &a->data.lr->head->invol, ctx->lrstack.buf[i]->rule);
                    }
                }
                c = a->data.lr->seed;
                break;
            case PCC_LR_ANSWER_CHUNK:
                c = a->data.chunk;
                break;
            default: /* unknown */
                break;
            }
        }
        else {
            pcc_lr_entry_t *const e = pcc_lr_entry__create(ctx->auxil, rule);
            pcc_lr_stack__push(ctx->auxil, &ctx->lrstack, e);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_LR, p);
            a->data.lr = e;
            pcc_lr_table__set_answer(ctx->auxil, &ctx->lrtable, p, rule, a);
            c = rule(ctx);
            pcc_lr_stack__pop(ctx->auxil, &ctx->lrstack);
            a->pos = ctx->pos;
            if (e->head == NULL) {
                pcc_lr_answer__set_chunk(ctx->auxil, a, c);
            }
            else {
                e->seed = c;
                h = a->data.lr->head;
                if (h->rule != rule) {
                    c = a->data.lr->seed;
                    a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos);
                    a->data.chunk = c;
                    pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
                }
                else {
                    pcc_lr_answer__set_chunk(ctx->auxil, a, a->data.lr->seed);
                    if (a->data.chunk == NULL) {
                        c = NULL;
                    }
                    else {
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, h);
                        for (;;) {
                            ctx->pos = p;
                            pcc_rule_set__copy(ctx->auxil, &h->eval, &h->invol);
                            c = rule(ctx);
                            if (c == NULL || ctx->pos <= a->pos) break;
                            pcc_lr_answer__set_chunk(ctx->auxil, a, c);
                            a->pos = ctx->pos;
                        }
                        pcc_thunk_chunk__destroy(ctx->auxil, c);
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, NULL);
                        ctx->pos = a->pos;
                        c = a->data.chunk;
                    }
                }
            }
        }
    }
    if (c == NULL) return PCC_FALSE;
    if (value == NULL) value = &null;
    memset(value, 0, sizeof(pcc_value_t)); /* in case */
    pcc_thunk_array__add(ctx->auxil, thunks, pcc_thunk__create_node(ctx->auxil, &c->thunks, value));
    return PCC_TRUE;
}

static void pcc_do_action(pcc_context_t *ctx, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    int i;
    for (i = 0; i < thunks->len; i++) {
        pcc_thunk_t *const thunk = thunks->buf[i];
        switch (thunk->type) {
        case PCC_THUNK_LEAF:
            thunk->data.leaf.action(ctx, thunk, value);
            break;
        case PCC_THUNK_NODE:
            pcc_do_action(ctx, thunk->data.node.thunks, thunk->data.node.value);
            break;
        default: /* unknown */
            break;
        }
    }
}

static void pcc_action_statement_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    set_last_name((char*)name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define str (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    if(array_contains(&rooms,last_name))
            print_look(last_name,(char*)str);
    else
            print_descr(last_name,(char*)str);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    set_last_name((char*)name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define str (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_appearance(last_name,(char*)str); 
#undef _0e
#undef _0s
#undef _0
#undef str
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_statement_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_statement_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    set_last_name((char*)name); print_player((char*)name);
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define dir (*__pcc_in->data.leaf.values.buf[2])
#define locfrom (*__pcc_in->data.leaf.values.buf[3])
#define locto (*__pcc_in->data.leaf.values.buf[4])
#define vice (*__pcc_in->data.leaf.values.buf[5])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    if(vice) print_fromtoandback(dir, (char*)locfrom, (char*)locto);
    else print_fromto(dir, (char*)locfrom, (char*)locto);
#undef _0e
#undef _0s
#undef _0
#undef vice
#undef locto
#undef locfrom
#undef dir
#undef __
#undef auxil
}

static void pcc_action_statement_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    set_last_name((char*)name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_room(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define dir (*__pcc_in->data.leaf.values.buf[2])
#define locto (*__pcc_in->data.leaf.values.buf[4])
#define vice (*__pcc_in->data.leaf.values.buf[5])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    if(vice) print_fromtoandback(dir, last_name, (char*)locto);
    else print_fromto(dir, last_name, (char*)locto);
#undef _0e
#undef _0s
#undef _0
#undef vice
#undef locto
#undef dir
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_11(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_room(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_12(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_supporter(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_13(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_container(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_14(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_item(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_15(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define loc (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_is_in(last_name, (char*)loc); 
#undef _0e
#undef _0s
#undef _0
#undef loc
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_16(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define loc (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_is_on(last_name, (char*)loc); 
#undef _0e
#undef _0s
#undef _0
#undef loc
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_17(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define loc (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_is_in(last_name, (char*)loc); 
#undef _0e
#undef _0s
#undef _0
#undef loc
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_18(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define loc (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_is_on(last_name, (char*)loc); 
#undef _0e
#undef _0s
#undef _0
#undef loc
#undef name
#undef __
#undef auxil
}

static void pcc_action_statement_19(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s __pcc_in->data.leaf.capts.buf[0]->range.start
#define _1e __pcc_in->data.leaf.capts.buf[0]->range.end
    fprintf(stderr,"\nSYNTAX ERROR=<%s>\n",_1); 
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LOHI_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_SU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LOHI_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_GIU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_INOUT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_DENTRO;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_INOUT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_FUORI;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_UPDOWN_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_GIU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_UPDOWN_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_SU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NOTVICE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = 0; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_VICE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = 1; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_NORDEST; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_NORDOVEST; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_NORD; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_GIU; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_DENTRO; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_FUORI; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_SUDEST; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_SUDOVEST; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_SUD; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_SU; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_EST; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_11(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    __ = D_OVEST; 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_edible(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINER_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_open(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINER_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_close(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJFEMALE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    print_female(last_name); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_WORD_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s __pcc_in->data.leaf.capts.buf[0]->range.start
#define _1e __pcc_in->data.leaf.capts.buf[0]->range.end
    __ = (int)_1; 
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_QUOTED_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s __pcc_in->data.leaf.capts.buf[0]->range.start
#define _1e __pcc_in->data.leaf.capts.buf[0]->range.end
    __ = (int)_1; 
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETMS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETFS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETNN_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const int)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const int)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 2;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_statement(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LOHI(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_INOUT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_UPDOWN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NOTVICE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_VICE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DIR(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPOBJECT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJOBJECT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPCONTAINER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJCONTAINER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJFEMALE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_WORD(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTED(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETFS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETNN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDET(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDFS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTIND(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPFS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPNN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDI(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPA(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDA(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPIN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPSU(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_THEREIS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_IS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AND(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SPACE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_COMMA(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STOP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule__(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_EOL(pcc_context_t *ctx);

static pcc_thunk_chunk_t *pcc_evaluate_rule_statement(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 7);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != '/' ||
            (ctx->buffer.buf + ctx->pos)[1] != '/'
        ) goto L0004;
        ctx->pos += 2;
        {
            int i;
            for (i = 0;; i++) {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0006;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0005;
                L0006:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                }
                if (pcc_refill_buffer(ctx, 1) < 1) goto L0005;
                ctx->pos++;
                if (ctx->pos == p) break;
                continue;
            L0005:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0007;
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0009;
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 11) < 11 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->pos)[2] != 's' ||
                    (ctx->buffer.buf + ctx->pos)[3] != 'c' ||
                    (ctx->buffer.buf + ctx->pos)[4] != 'r' ||
                    (ctx->buffer.buf + ctx->pos)[5] != 'i' ||
                    (ctx->buffer.buf + ctx->pos)[6] != 'z' ||
                    (ctx->buffer.buf + ctx->pos)[7] != 'i' ||
                    (ctx->buffer.buf + ctx->pos)[8] != 'o' ||
                    (ctx->buffer.buf + ctx->pos)[9] != 'n' ||
                    (ctx->buffer.buf + ctx->pos)[10] != 'e'
                ) goto L0011;
                ctx->pos += 11;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0011;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0011;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0011;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_0, 7, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0010;
            L0011:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 3) < 3 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'a'
                    ) goto L0013;
                    ctx->pos += 3;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0013;
                    goto L0014;
                L0013:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0014:;
                }
                if (
                    pcc_refill_buffer(ctx, 11) < 11 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->pos)[2] != 's' ||
                    (ctx->buffer.buf + ctx->pos)[3] != 'c' ||
                    (ctx->buffer.buf + ctx->pos)[4] != 'r' ||
                    (ctx->buffer.buf + ctx->pos)[5] != 'i' ||
                    (ctx->buffer.buf + ctx->pos)[6] != 'z' ||
                    (ctx->buffer.buf + ctx->pos)[7] != 'i' ||
                    (ctx->buffer.buf + ctx->pos)[8] != 'o' ||
                    (ctx->buffer.buf + ctx->pos)[9] != 'n' ||
                    (ctx->buffer.buf + ctx->pos)[10] != 'e'
                ) goto L0012;
                ctx->pos += 11;
                goto L0010;
            L0012:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0009;
            L0010:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0009;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0009;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTE, &chunk->thunks, NULL)) goto L0009;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[1]))) goto L0009;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTE, &chunk->thunks, NULL)) goto L0009;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_1, 7, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->pos;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0008;
        L0009:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0015;
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 's' ||
                    (ctx->buffer.buf + ctx->pos)[2] != 'p' ||
                    (ctx->buffer.buf + ctx->pos)[3] != 'e' ||
                    (ctx->buffer.buf + ctx->pos)[4] != 't' ||
                    (ctx->buffer.buf + ctx->pos)[5] != 't' ||
                    (ctx->buffer.buf + ctx->pos)[6] != 'o'
                ) goto L0017;
                ctx->pos += 7;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0017;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0017;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_2, 7, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0016;
            L0017:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 3) < 3 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'o'
                    ) goto L0019;
                    ctx->pos += 3;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0019;
                    goto L0020;
                L0019:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0020:;
                }
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 's' ||
                    (ctx->buffer.buf + ctx->pos)[2] != 'p' ||
                    (ctx->buffer.buf + ctx->pos)[3] != 'e' ||
                    (ctx->buffer.buf + ctx->pos)[4] != 't' ||
                    (ctx->buffer.buf + ctx->pos)[5] != 't' ||
                    (ctx->buffer.buf + ctx->pos)[6] != 'o'
                ) goto L0018;
                ctx->pos += 7;
                goto L0016;
            L0018:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0015;
            L0016:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0015;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0015;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTE, &chunk->thunks, NULL)) goto L0015;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[1]))) goto L0015;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTE, &chunk->thunks, NULL)) goto L0015;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_3, 7, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->pos;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0008;
        L0015:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0021;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'g' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'i' ||
                (ctx->buffer.buf + ctx->pos)[2] != 'o' ||
                (ctx->buffer.buf + ctx->pos)[3] != 'c' ||
                (ctx->buffer.buf + ctx->pos)[4] != 'a' ||
                (ctx->buffer.buf + ctx->pos)[5] != 't'
            ) goto L0021;
            ctx->pos += 6;
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 'o' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 'r' ||
                    (ctx->buffer.buf + ctx->pos)[2] != 'e'
                ) goto L0023;
                ctx->pos += 3;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_4, 7, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0022;
            L0023:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 't' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 'r' ||
                    (ctx->buffer.buf + ctx->pos)[2] != 'i' ||
                    (ctx->buffer.buf + ctx->pos)[3] != 'c' ||
                    (ctx->buffer.buf + ctx->pos)[4] != 'e'
                ) goto L0024;
                ctx->pos += 5;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_5, 7, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0022;
            L0024:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0021;
            L0022:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0021;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0021;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0021;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_6, 7, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->pos;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0008;
        L0021:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[2]))) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[3]))) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THEREIS, &chunk->thunks, NULL)) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0027;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[4]))) goto L0027;
                goto L0026;
            L0027:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0028;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[3]))) goto L0028;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0028;
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                    (ctx->buffer.buf + ctx->pos)[1] != 'i'
                ) goto L0028;
                ctx->pos += 2;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0028;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 2) < 2 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 'v' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'a'
                    ) goto L0030;
                    ctx->pos += 2;
                    goto L0029;
                L0030:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 4) < 4 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'a' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'l' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'e'
                    ) goto L0031;
                    ctx->pos += 4;
                    goto L0029;
                L0031:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'c' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'e' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'n' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 'd' ||
                        (ctx->buffer.buf + ctx->pos)[5] != 'e'
                    ) goto L0032;
                    ctx->pos += 6;
                    goto L0029;
                L0032:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 5) < 5 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 'e' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'n' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'r' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 'a'
                    ) goto L0033;
                    ctx->pos += 5;
                    goto L0029;
                L0033:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 4) < 4 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 'e' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'c' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'e'
                    ) goto L0034;
                    ctx->pos += 4;
                    goto L0029;
                L0034:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0028;
                L0029:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0028;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    {
                        const int p = ctx->pos;
                        const int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0037;
                        goto L0038;
                    L0037:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0038:;
                    }
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[2]))) goto L0036;
                    goto L0035;
                L0036:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 2) < 2 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 'i' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'n'
                    ) goto L0039;
                    ctx->pos += 2;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0039;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LOHI, &chunk->thunks, &(chunk->values.buf[2]))) goto L0039;
                    goto L0035;
                L0039:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[2]))) goto L0040;
                    goto L0035;
                L0040:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0028;
                L0035:;
                }
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    {
                        const int p = ctx->pos;
                        const int n = chunk->thunks.len;
                        if (
                            pcc_refill_buffer(ctx, 5) < 5 ||
                            (ctx->buffer.buf + ctx->pos)[0] != 'v' ||
                            (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
                            (ctx->buffer.buf + ctx->pos)[2] != 'r' ||
                            (ctx->buffer.buf + ctx->pos)[3] != 's' ||
                            (ctx->buffer.buf + ctx->pos)[4] != 'o'
                        ) goto L0043;
                        ctx->pos += 5;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0043;
                        goto L0044;
                    L0043:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0044:;
                    }
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0042;
                    goto L0041;
                L0042:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0045;
                    goto L0041;
                L0045:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0046;
                    goto L0041;
                L0046:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0028;
                L0041:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[4]))) goto L0028;
                goto L0026;
            L0028:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0048;
                    goto L0049;
                L0048:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0049:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[4]))) goto L0047;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0047;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0047;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0047;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[2]))) goto L0047;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0047;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[3]))) goto L0047;
                goto L0026;
            L0047:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0025;
            L0026:;
            }
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOTVICE, &chunk->thunks, &(chunk->values.buf[5]))) goto L0051;
                goto L0050;
            L0051:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VICE, &chunk->thunks, &(chunk->values.buf[5]))) goto L0052;
                goto L0050;
            L0052:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0025;
            L0050:;
            }
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_7, 7, 1);
                thunk->data.leaf.values.buf[2] = &(chunk->values.buf[2]);
                thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->pos;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0008;
        L0025:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0055;
                    goto L0056;
                L0055:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0056:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0054;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_8, 7, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0054;
                goto L0057;
            L0054:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0057:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0053;
            {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0060;
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'n' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 'z' ||
                        (ctx->buffer.buf + ctx->pos)[5] != 'a'
                    ) goto L0060;
                    ctx->pos += 6;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0060;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_9, 7, 1);
                        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->pos;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0061;
                L0060:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0061:;
                }
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0063;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[2]))) goto L0063;
                    goto L0062;
                L0063:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[2]))) goto L0064;
                    goto L0062;
                L0064:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[2]))) goto L0065;
                    goto L0062;
                L0065:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0059;
                L0062:;
                }
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0067;
                    goto L0066;
                L0067:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0068;
                    goto L0066;
                L0068:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0069;
                    goto L0066;
                L0069:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0059;
                L0066:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[4]))) goto L0059;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOTVICE, &chunk->thunks, &(chunk->values.buf[5]))) goto L0071;
                    goto L0070;
                L0071:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VICE, &chunk->thunks, &(chunk->values.buf[5]))) goto L0072;
                    goto L0070;
                L0072:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0059;
                L0070:;
                }
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_10, 7, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.values.buf[2] = &(chunk->values.buf[2]);
                    thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                    thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0058;
            L0059:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0073;
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'n' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 'z' ||
                        (ctx->buffer.buf + ctx->pos)[5] != 'a'
                    ) goto L0075;
                    ctx->pos += 6;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_11, 7, 1);
                        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->pos;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0074;
                L0075:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 's' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'p' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'p' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 'o' ||
                        (ctx->buffer.buf + ctx->pos)[5] != 'r' ||
                        (ctx->buffer.buf + ctx->pos)[6] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[7] != 'o'
                    ) goto L0076;
                    ctx->pos += 8;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_12, 7, 1);
                        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->pos;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0074;
                L0076:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 11) < 11 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 'c' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'n' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 'e' ||
                        (ctx->buffer.buf + ctx->pos)[5] != 'n' ||
                        (ctx->buffer.buf + ctx->pos)[6] != 'i' ||
                        (ctx->buffer.buf + ctx->pos)[7] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[8] != 'o' ||
                        (ctx->buffer.buf + ctx->pos)[9] != 'r' ||
                        (ctx->buffer.buf + ctx->pos)[10] != 'e'
                    ) goto L0077;
                    ctx->pos += 11;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_13, 7, 1);
                        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->pos;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    {
                        const int p = ctx->pos;
                        const int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0078;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPCONTAINER, &chunk->thunks, NULL)) goto L0078;
                        goto L0079;
                    L0078:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0079:;
                    }
                    goto L0074;
                L0077:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 7) < 7 ||
                        (ctx->buffer.buf + ctx->pos)[0] != 'o' ||
                        (ctx->buffer.buf + ctx->pos)[1] != 'g' ||
                        (ctx->buffer.buf + ctx->pos)[2] != 'g' ||
                        (ctx->buffer.buf + ctx->pos)[3] != 'e' ||
                        (ctx->buffer.buf + ctx->pos)[4] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[5] != 't' ||
                        (ctx->buffer.buf + ctx->pos)[6] != 'o'
                    ) goto L0080;
                    ctx->pos += 7;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_14, 7, 1);
                        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->pos;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    {
                        const int p = ctx->pos;
                        const int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0081;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPOBJECT, &chunk->thunks, NULL)) goto L0081;
                        goto L0082;
                    L0081:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0082:;
                    }
                    goto L0074;
                L0080:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0073;
                L0074:;
                }
                {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0083;
                    {
                        const int p = ctx->pos;
                        const int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0085;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0085;
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_15, 7, 1);
                            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                            thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->pos;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0084;
                    L0085:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0086;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0086;
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_16, 7, 1);
                            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                            thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->pos;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0084;
                    L0086:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0083;
                    L0084:;
                    }
                    goto L0087;
                L0083:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0087:;
                }
                goto L0058;
            L0073:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0088;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0088;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_17, 7, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0058;
            L0088:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0089;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0089;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_18, 7, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->pos;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0058;
            L0089:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0053;
            L0058:;
            }
            goto L0008;
        L0053:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0007;
        L0008:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOP, &chunk->thunks, NULL)) goto L0007;
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const int p = ctx->pos;
            int q;
            {
                int i;
                for (i = 0;; i++) {
                    const int p = ctx->pos;
                    const int n = chunk->thunks.len;
                    {
                        const int p = ctx->pos;
                        const int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0092;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0091;
                    L0092:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    }
                    if (pcc_refill_buffer(ctx, 1) < 1) goto L0091;
                    ctx->pos++;
                    if (ctx->pos == p) break;
                    continue;
                L0091:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    break;
                }
            }
            q = ctx->pos;
            chunk->capts.buf[0].range.start = p;
            chunk->capts.buf[0].range.end = q;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0090;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_statement_19, 7, 1);
            thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0090:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LOHI(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'a' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[2] != 't' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'o'
        ) goto L0002;
        ctx->pos += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LOHI_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'b' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'a' ||
            (ctx->buffer.buf + ctx->pos)[2] != 's' ||
            (ctx->buffer.buf + ctx->pos)[3] != 's' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'o'
        ) goto L0003;
        ctx->pos += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LOHI_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_INOUT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[3] != 't' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'o'
        ) goto L0002;
        ctx->pos += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_INOUT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'f' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'i'
        ) goto L0003;
        ctx->pos += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_INOUT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_UPDOWN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'p' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'a'
        ) goto L0002;
        ctx->pos += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_UPDOWN_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 't' ||
            (ctx->buffer.buf + ctx->pos)[3] != 't' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'o'
        ) goto L0003;
        ctx->pos += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_UPDOWN_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NOTVICE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'm' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'a'
        ) goto L0002;
        ctx->pos += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'n'
        ) goto L0002;
        ctx->pos += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'v' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'c' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'v' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[6] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[7] != 's' ||
            (ctx->buffer.buf + ctx->pos)[8] != 'a'
        ) goto L0002;
        ctx->pos += 9;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'z' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'a'
        ) goto L0003;
        ctx->pos += 5;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[2] != 't' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[6] != 'o'
        ) goto L0003;
        ctx->pos += 7;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NOTVICE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_VICE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0001;
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != 'e'
            ) goto L0003;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'v' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'i' ||
                (ctx->buffer.buf + ctx->pos)[2] != 'c' ||
                (ctx->buffer.buf + ctx->pos)[3] != 'e' ||
                (ctx->buffer.buf + ctx->pos)[4] != 'v' ||
                (ctx->buffer.buf + ctx->pos)[5] != 'e' ||
                (ctx->buffer.buf + ctx->pos)[6] != 'r' ||
                (ctx->buffer.buf + ctx->pos)[7] != 's' ||
                (ctx->buffer.buf + ctx->pos)[8] != 'a'
            ) goto L0003;
            ctx->pos += 9;
            goto L0002;
        L0003:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'c' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
                (ctx->buffer.buf + ctx->pos)[2] != 'n'
            ) goto L0004;
            ctx->pos += 3;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'r' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'i' ||
                (ctx->buffer.buf + ctx->pos)[2] != 't' ||
                (ctx->buffer.buf + ctx->pos)[3] != 'o' ||
                (ctx->buffer.buf + ctx->pos)[4] != 'r' ||
                (ctx->buffer.buf + ctx->pos)[5] != 'n' ||
                (ctx->buffer.buf + ctx->pos)[6] != 'o'
            ) goto L0004;
            ctx->pos += 7;
            goto L0002;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0001;
        L0002:;
        }
        goto L0005;
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0005:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_VICE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DIR(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[5] != 's' ||
            (ctx->buffer.buf + ctx->pos)[6] != 't'
        ) goto L0002;
        ctx->pos += 7;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'v' ||
            (ctx->buffer.buf + ctx->pos)[6] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[7] != 's' ||
            (ctx->buffer.buf + ctx->pos)[8] != 't'
        ) goto L0003;
        ctx->pos += 9;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'd'
        ) goto L0004;
        ctx->pos += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'g' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->pos)[3] != '\xb9'
        ) goto L0005;
        ctx->pos += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_3, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[3] != 't' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'o'
        ) goto L0006;
        ctx->pos += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_4, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'f' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'i'
        ) goto L0007;
        ctx->pos += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_5, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[4] != 's' ||
            (ctx->buffer.buf + ctx->pos)[5] != 't'
        ) goto L0008;
        ctx->pos += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_6, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'v' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[6] != 's' ||
            (ctx->buffer.buf + ctx->pos)[7] != 't'
        ) goto L0009;
        ctx->pos += 8;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_7, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0009:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'd'
        ) goto L0010;
        ctx->pos += 3;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_8, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0010:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u'
        ) goto L0011;
        ctx->pos += 2;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_9, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0011:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[1] != 's' ||
            (ctx->buffer.buf + ctx->pos)[2] != 't'
        ) goto L0012;
        ctx->pos += 3;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_10, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0012:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'v' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[3] != 's' ||
            (ctx->buffer.buf + ctx->pos)[4] != 't'
        ) goto L0013;
        ctx->pos += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_11, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0013:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPOBJECT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJOBJECT, &chunk->thunks, NULL)) goto L0000;
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COMMA, &chunk->thunks, NULL)) goto L0003;
            goto L0002;
        L0003:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0004;
            goto L0002;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0001;
        L0002:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPOBJECT, &chunk->thunks, NULL)) goto L0001;
        goto L0005;
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0005:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJOBJECT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 12) < 12 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'c' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'm' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'm' ||
            (ctx->buffer.buf + ctx->pos)[4] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[5] != 's' ||
            (ctx->buffer.buf + ctx->pos)[6] != 't' ||
            (ctx->buffer.buf + ctx->pos)[7] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[8] != 'b' ||
            (ctx->buffer.buf + ctx->pos)[9] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[10] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[11] != 'e'
        ) goto L0002;
        ctx->pos += 12;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJFEMALE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPCONTAINER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJCONTAINER, &chunk->thunks, NULL)) goto L0000;
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COMMA, &chunk->thunks, NULL)) goto L0003;
            goto L0002;
        L0003:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0004;
            goto L0002;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0001;
        L0002:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPCONTAINER, &chunk->thunks, NULL)) goto L0001;
        goto L0005;
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0005:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJCONTAINER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'a' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'p' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'e' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'r' ||
            (ctx->buffer.buf + ctx->pos)[4] != 't' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'o'
        ) goto L0002;
        ctx->pos += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINER_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'c' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'h' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[3] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[4] != 's' ||
            (ctx->buffer.buf + ctx->pos)[5] != 'o'
        ) goto L0003;
        ctx->pos += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINER_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->pos;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJFEMALE, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJFEMALE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 9) < 9 ||
        (ctx->buffer.buf + ctx->pos)[0] != 'f' ||
        (ctx->buffer.buf + ctx->pos)[1] != 'e' ||
        (ctx->buffer.buf + ctx->pos)[2] != 'm' ||
        (ctx->buffer.buf + ctx->pos)[3] != 'm' ||
        (ctx->buffer.buf + ctx->pos)[4] != 'i' ||
        (ctx->buffer.buf + ctx->pos)[5] != 'n' ||
        (ctx->buffer.buf + ctx->pos)[6] != 'i' ||
        (ctx->buffer.buf + ctx->pos)[7] != 'l' ||
        (ctx->buffer.buf + ctx->pos)[8] != 'e'
    ) goto L0000;
    ctx->pos += 9;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJFEMALE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_WORD(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const int p = ctx->pos;
        int q;
        {
            int i;
            for (i = 0;; i++) {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                {
                    char c;
                    if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
                    c = ctx->buffer.buf[ctx->pos];
                    if (!(
                        (c >= 'a' && c <= 'z') ||
                        (c >= 'A' && c <= 'Z') ||
                        c == '\xc3' ||
                        c == '\xa0' ||
                        c == '\xc3' ||
                        c == '\xa8' ||
                        c == '\xc3' ||
                        c == '\xa9' ||
                        c == '\xc3' ||
                        c == '\xac' ||
                        c == '\xc3' ||
                        c == '\xb2' ||
                        c == '\xc3' ||
                        c == '\xb9'
                    )) goto L0001;
                    ctx->pos++;
                }
                if (ctx->pos == p) break;
                continue;
            L0001:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        q = ctx->pos;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_WORD_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTED(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const int p = ctx->pos;
        int q;
        {
            int i;
            for (i = 0;; i++) {
                const int p = ctx->pos;
                const int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] == '\"'
                ) goto L0001;
                ctx->pos++;
                if (ctx->pos == p) break;
                continue;
            L0001:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        q = ctx->pos;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_QUOTED_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'i' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'l'
        ) goto L0002;
        ctx->pos += 2;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'I' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'l'
        ) goto L0004;
        ctx->pos += 2;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'L' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'o'
        ) goto L0005;
        ctx->pos += 2;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETMS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETFS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'a'
        ) goto L0002;
        ctx->pos += 2;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'L' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'a'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETFS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETNN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\''
        ) goto L0002;
        ctx->pos += 2;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'L' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\''
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETNN_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDET(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETMS, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETFS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETNN, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'o'
        ) goto L0002;
        ctx->pos += 3;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'n'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDFS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'a'
        ) goto L0002;
        ctx->pos += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'u' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'n' ||
            (ctx->buffer.buf + ctx->pos)[2] != '\''
        ) goto L0003;
        ctx->pos += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTIND(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTINDMS, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTINDFS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'l' ||
            (ctx->buffer.buf + ctx->pos)[2] != 'o'
        ) goto L0002;
        ctx->pos += 3;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'l'
        ) goto L0003;
        ctx->pos++;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPFS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->pos)[0] != 'l' ||
        (ctx->buffer.buf + ctx->pos)[1] != 'l' ||
        (ctx->buffer.buf + ctx->pos)[2] != 'a'
    ) goto L0000;
    ctx->pos += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPNN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->pos)[0] != 'l' ||
        (ctx->buffer.buf + ctx->pos)[1] != 'l' ||
        (ctx->buffer.buf + ctx->pos)[2] != '\''
    ) goto L0000;
    ctx->pos += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPMS, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPFS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPNN, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDI(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'e'
            ) goto L0004;
            ctx->pos += 2;
            goto L0003;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'D' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'e'
            ) goto L0005;
            ctx->pos += 2;
            goto L0003;
        L0005:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'i'
            ) goto L0008;
            ctx->pos += 2;
            goto L0007;
        L0008:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'D' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'i'
            ) goto L0009;
            ctx->pos += 2;
            goto L0007;
        L0009:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0006;
        L0007:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPA(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'a'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'A'
        ) goto L0003;
        ctx->pos++;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0004;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0004:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDA(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'd' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'a'
        ) goto L0002;
        ctx->pos += 2;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'D' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'a'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0004;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0004:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPIN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'n' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'e'
            ) goto L0004;
            ctx->pos += 2;
            goto L0003;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'N' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'e'
            ) goto L0005;
            ctx->pos += 2;
            goto L0003;
        L0005:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'i' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'n'
            ) goto L0008;
            ctx->pos += 2;
            goto L0007;
        L0008:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->pos)[0] != 'I' ||
                (ctx->buffer.buf + ctx->pos)[1] != 'n'
            ) goto L0009;
            ctx->pos += 2;
            goto L0007;
        L0009:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0006;
        L0007:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPSU(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 's' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u'
        ) goto L0002;
        ctx->pos += 2;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'S' ||
            (ctx->buffer.buf + ctx->pos)[1] != 'u'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0004;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0004:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_THEREIS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'c' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\'' ||
            (ctx->buffer.buf + ctx->pos)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->pos)[3] != '\xa8'
        ) goto L0002;
        ctx->pos += 4;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->pos)[0] != 'C' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\'' ||
            (ctx->buffer.buf + ctx->pos)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->pos)[3] != '\xa8'
        ) goto L0003;
        ctx->pos += 4;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_IS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != '\xc3' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\xa8'
        ) goto L0002;
        ctx->pos += 2;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != '\xc3' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\x88'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'a'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'A'
        ) goto L0003;
        ctx->pos++;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AND(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != 'e'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SPACE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p0 = ctx->pos;
        const int n0 = chunk->thunks.len;
        int i;
        for (i = 0;; i++) {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            {
                char c;
                if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
                c = ctx->buffer.buf[ctx->pos];
                if (!(
                    c == ' ' ||
                    c == '\t'
                )) goto L0001;
                ctx->pos++;
            }
            if (ctx->pos == p) break;
            continue;
        L0001:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            break;
        }
        if (i < 1) {
            ctx->pos = p0;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n0);
            goto L0000;
        }
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '\"'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_COMMA(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STOP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '.'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule__(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int i;
        for (i = 0;; i++) {
            const int p = ctx->pos;
            const int n = chunk->thunks.len;
            {
                char c;
                if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
                c = ctx->buffer.buf[ctx->pos];
                if (!(
                    c == ' ' ||
                    c == '\t'
                )) goto L0001;
                ctx->pos++;
            }
            if (ctx->pos == p) break;
            continue;
        L0001:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            break;
        }
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_EOL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const int p = ctx->pos;
        const int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\n'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->pos)[0] != '\r' ||
            (ctx->buffer.buf + ctx->pos)[1] != '\n'
        ) goto L0003;
        ctx->pos += 2;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\r'
        ) goto L0004;
        ctx->pos++;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

pcc_context_t *pcc_create(void *auxil) {
    return pcc_context__create(auxil);
}

int pcc_parse(pcc_context_t *ctx, int *ret) {
    pcc_thunk_array_t thunks;
    pcc_thunk_array__init(ctx->auxil, &thunks, PCC_ARRAYSIZE);
    if (pcc_apply_rule(ctx, pcc_evaluate_rule_statement, &thunks, ret))
        pcc_do_action(ctx, &thunks, ret);
    else
        PCC_ERROR(ctx->auxil);
    pcc_commit_buffer(ctx);
    pcc_thunk_array__term(ctx->auxil, &thunks);
    return pcc_refill_buffer(ctx, 1) >= 1;
}

void pcc_destroy(pcc_context_t *ctx) {
    pcc_context__destroy(ctx);
}

int main(int argc, char *argv[]) {
  init();
  pcc_context_t *ctx = pcc_create(NULL);
  while (pcc_parse(ctx, NULL));
  pcc_destroy(ctx);
  final();
  return 0;
}