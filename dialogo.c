/* A packrat parser generated by PackCC 1.5.0 */

#ifdef _MSC_VER
#undef _CRT_SECURE_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#endif /* _MSC_VER */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef _MSC_VER
#if defined __GNUC__ && defined _WIN32 /* MinGW */
#ifndef PCC_USE_SYSTEM_STRNLEN
#define strnlen(str, maxlen) pcc_strnlen(str, maxlen)
static size_t pcc_strnlen(const char *str, size_t maxlen) {
    size_t i;
    for (i = 0; i < maxlen && str[i]; i++);
    return i;
}
#endif /* !PCC_USE_SYSTEM_STRNLEN */
#endif /* defined __GNUC__ && defined _WIN32 */
#endif /* !_MSC_VER */

#define PCC_DBG_EVALUATE 0
#define PCC_DBG_MATCH    1
#define PCC_DBG_NOMATCH  2

#include "dialogo.h"


#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
static const char *dbg_str[] = { "Evaluating rule", "*Matched rule", "Abandoning rule" };
#define PCC_DEBUG(event, rule, level, pos, buffer, length) \
    fprintf(stderr, "%*s%s %s @%d [%.*s]\n", level * 2, "", dbg_str[event], rule, pos, length, buffer)
*/

struct Array rooms, objects, persons, containers, supporters, vehicles;

struct Array newActCmd, newActInf, newActPP, newActPrep;

struct Array last_list1, last_list2, last_list3;

char *last_name = NULL;
char *last_name2 = NULL;
char *last_name3 = NULL;
char *last_locfrom = NULL;
char *last_locto = NULL;
char *last_loc = NULL;
char *last_indent = NULL;

void set_string(char **storage, char *str) {
        if(*storage != NULL)
                free(*storage);
        if(str == NULL)
                *storage = NULL;
        else
                *storage = strdup(str);
}
void set_extstring(char **storage, char prefix, char *str) {
        if(*storage != NULL)
                free(*storage);
        if(str == NULL)
                *storage = NULL;
        else {
                *storage = malloc(sizeof(char)*strlen(str)+2);
                **storage = prefix;
                strcpy(*(storage)+1,str);
        }
}
void set_objorvar(char** storage, char *word) {
        int isvar = 1;
        for(int ct = 0; ct < strlen(word); ct++) {
                char ch = word[ct];
                if(ch != '_' && (ch < 'A' || ch > 'Z' )) {
                        isvar = 0;
                        break;
                }
        }
        if(isvar)
                set_extstring(storage,'$',word);
        else
                set_extstring(storage,'#',word);
}
void set_varnum(char** storage, char *word) {
        int isnum = 1;
        for(int ct = 0; ct < strlen(word); ct++) {
                char ch = word[ct];
                if(ch < '0' || ch > '9' ) {
                        isnum = 0;
                        break;
                }
        }
        if(isnum)
                set_string(storage,word);
        else
                set_extstring(storage,'$',word);
}
void set_name(char *word) {set_objorvar(&last_name,word);}
void set_name2(char *word) {set_objorvar(&last_name2,word);}
void set_name3(char *word) {set_objorvar(&last_name3,word);}
void set_varornum2(char *word) {set_varnum(&last_name2,word);}
void set_varornum3(char *word) {set_varnum(&last_name3,word);}
void set_locfrom(char *word) {set_objorvar(&last_locfrom,word);}
void set_locto(char *word) {set_objorvar(&last_locto,word);}
void set_loc(char *word) {set_objorvar(&last_loc,word);}

int last_genre = 0;
int last_plural = 0;
int last_proper = 0;

int ret_mode = 1;

void array_init(struct Array *array, int size) {
        array->size = size;
        array->len = 0;
        array->strings = malloc(sizeof(char*)*size);
        array->arg1 = malloc(sizeof(int)*size);
        array->arg2 = malloc(sizeof(int)*size);
        array->arg3 = malloc(sizeof(int)*size);
        for(int ct = 0; ct < size; ct++) {
                (array->strings)[ct] = NULL;
                (array->arg1)[ct] = 0;
                (array->arg2)[ct] = 0;
                (array->arg3)[ct] = 0;
        }
}
void array_free(struct Array *array) {
        for(int ct = 0; ct < array->len; ct++)
                if(array->strings[ct] != NULL) {
                        free(array->strings[ct]);
                        array->strings[ct] = NULL;
                        array->arg1[ct] = 0;
                        array->arg2[ct] = 0;
                        array->arg3[ct] = 0;
                };
        array->len = 0;
}
int array_add(struct Array *array, char *str, int arg1, int arg2, int arg3) {
        if(array->size == array->len) {
                array->size *= 2;
                array->strings = realloc(array->strings,sizeof(char*)*array->size);
        }
        array->strings[array->len] = strdup(str);
        array->arg1[array->len] = arg1;
        array->arg2[array->len] = arg2;
        array->arg3[array->len] = arg3;
        return array->len++;
}
char *array_getstring(struct Array *array, int idx) {return idx < 0 || (idx >= array->len)? NULL: array->strings[idx];}
int array_getarg1(struct Array *array, int idx) {return idx < 0 || (idx >= array->len)? 0: array->arg1[idx];}
int array_getarg2(struct Array *array, int idx) {return idx < 0 || (idx >= array->len)? 0: array->arg2[idx];}
int array_getarg3(struct Array *array, int idx) {return idx < 0 || (idx >= array->len)? 0: array->arg3[idx];}
int array_contains(struct Array* array, char *str) {
        for(int ct = 0; ct < array->len; ct++)
                if(strcmp(str,array->strings[ct]) == 0)
                        return 1;
        return 0;
}

void init() {
        array_init(&newActCmd,5);
        array_init(&newActInf,5);
        array_init(&newActPP,5);
        array_init(&newActPrep,5);
        array_init(&last_list1,5);
        array_init(&last_list2,5);
        array_init(&last_list3,5);
        array_init(&rooms,100);
        array_init(&objects,100);
        array_init(&persons,10);
        array_init(&containers,20);
        array_init(&supporters,20);
        array_init(&vehicles,10);
        p_start();
}

void final() {
        p_final();
        array_free(&rooms);
        array_free(&objects);
        array_free(&objects);
        array_free(&persons);
        array_free(&supporters);
        array_free(&vehicles);
        array_free(&last_list1);
        array_free(&last_list2);
        array_free(&last_list3);
        array_free(&newActCmd);
        array_free(&newActInf);
        array_free(&newActPP);
        array_free(&newActPrep);
}

void print_replacing_space(char *str){
        for(int ct = 0; ct < strlen(str); ct++) {
                char ch = *(str+ct);
                if(ch == '_') printf(" ");
                else printf("%c",ch);
        }
}
void print_name(char *str) { _pindent("(name %s) ",str); print_replacing_space(str+1); printf("\n"); }

void p_error(char *type, char *msg) {
        fprintf(stderr,"\n%s=<%s>\n",type,msg);
        printf("\n%%%%%s=<%s>\n",type,msg);
}
void p_start() {}
void p_final() {}
void _pindent(const char *format, ...) {
   va_list arg;
   va_start (arg, format);
   if(last_indent)
        printf("%s",last_indent);
   vfprintf(stdout, format, arg);
   va_end (arg);
}
void p_cmd_val(char *cmd, char* val, int ret) { printf("(%s) %s ",cmd,val); if(ret) printf("\n"); }
void p_cmd(char *cmd, int ret) { printf("(%s)",cmd); if(ret) printf("\n");}
void p_prop_val(char *prop, char* obj, char *val, int ret) { printf("(%s %s) %s ",prop,obj,val); if(ret) printf("\n"); }
void p_prop(char *prop, char* obj, int ret) { printf("(%s %s)",prop,obj); if(ret) printf("\n"); }
void p_is_prop(char *prop, char *obj, int ret) { printf("(%s is %s)",obj,prop); if(ret == 1) printf("\n"); }
void p_nameprops(char* name, int genre, int plural, int proper) {
        if(genre == 1) {p_prop("female",name,1);}
        if(plural == 1) {p_prop("plural",name,1);}
        if(proper == 1) {p_prop("proper",name,1);}
}
void p_room(char *name, int genre, int plural, int proper) {
        array_add(&rooms,name,genre,plural,proper);
        print_name(name);
        _pindent("(room %s)\n",name);
        p_nameprops(name,genre,plural,proper);
}
void p_supporter(char *name, int genre, int plural, int proper, int mobile) {
        array_add(&objects,name,genre,plural,proper);
        array_add(&supporters,name,genre,plural,proper);
        print_name(name);
        _pindent("(supporter %s)\n",name);
        if(mobile) {
                array_add(&vehicles,name,genre,plural,proper);
                _pindent("(vehicle %s)\n",name);
                _pindent("(actor supporter %s)\n",name);
        }
        p_nameprops(name,genre,plural,proper);
}
void p_container(char *name, int genre, int plural, int proper, int mobile) {
        array_add(&objects,name,genre,plural,proper);
        array_add(&containers,name,genre,plural,proper);
        print_name(name);
        _pindent("(container %s)\n",name);
        if(mobile) {
                array_add(&vehicles,name,genre,plural,proper);
                _pindent("(vehicle %s)\n",name);
                _pindent("(actor container %s)\n",name);
        }
        p_nameprops(name,genre,plural,proper);
}
void p_person(char *name, int genre, int plural, int proper) {
        array_add(&objects,name,genre,plural,proper);
        array_add(&persons,name,genre,plural,proper);
        print_name(name);
        _pindent("(animate %s)\n",name);
        p_nameprops(name,genre,plural,proper);
}
void p_object(char *name, int genre, int plural, int proper) {
        array_add(&objects,name,genre,plural,proper);
        print_name(name);
        p_nameprops(name,genre,plural,proper);
}
void p_is_rel(char *name, char *rel, char *loc, int ret) {_pindent("(%s is #%s %s)",name,rel,loc); if(ret == 1) printf("\n"); }
char *dirs[] = {"up","in","north","northeast","east","southeast","down","out","south","southwest","west","northwest"};
void p_fromto(int dir, char *locfrom, char *locto) {_pindent("(from %s go #%s to %s)\n", locfrom, dirs[dir], locto);}
void p_fromtoandback(int dir, char *locfrom, char *locto) {
        p_fromto(dir,locfrom,locto);
        p_fromto((dir+6)%12,locto,locfrom);
}
const int actions_count = 65;
char *actions_it[] = {
        "abbracciare","accendere","agitare","alzarsi","andare","aprire","annusare","ascoltare","aspettare","assaggiare",
        "attaccare","baciare","ballare","bere","bloccare","cantare","cercare","chiamare","chiedere","chiudere",
        "consultare","dare","dire","dormire","entrare","esaminare","girare","gridare","guardare","imprecare",
        "indossare","inventariare","lanciare","lasciare","leggere","mangiare","mappare","mettere","morsicare","mostrare",
        "nuotare","parlare","pensare","pregare","prendere","pulire","raccogliere","riparare","salire","saltare",
        "salutare","sbloccare","scaricare","sedersi","spegnere","spingere","spremere","svegliarsi","tagliare","tirare",
        "toccare","togliere","trovare","uscire","volare"
};
char *actions_it_pp[] = {
        "abbracciato","acceso","agitato","alzato","andato","aperto","annusato","ascoltato","aspettato","assaggiato",
        "attaccato","baciato","ballato","bevuto","bloccato","cantato","cercato","chiamato","chiesto","chiuso",
        "consultato","dato","detto","dormito","entrato","esaminato","girato","gridato","guardato","imprecato",
        "indossato","inventariato","lanciato","lasciato","letto","mangiato","mappato","messo","morsicato","mostrato",
        "nuotato","parlato","pensato","pregato","preso","pulito","raccolto","riparato","salito","saltato",
        "salutato","sbloccato","scaricato","seduto","spento","spinto","spremuto","svegliato","tagliato","tirato",
        "toccato","tolto","trovato","uscito","volato"
};
char *actions_en[] = {
        "hug","switch on","wave","stand","go","open","smell","listen","wait","taste",
        "attack","kiss","dance","drink","lock","sing","search","call","ask","close",
        "consult","give","tell","sleep","enter","examine","turn","shout","look","curse",
        "wear","inventory","throw","drop","read","eat","exits","put","bite","show",
        "swim","talk","think","pray","take","clean","pick up","fix","climb","jump",
        "greet","unlock","flush","sit on","switch off","push","squeeze","wake up","cut","pull",
        "feel","remove","find","leave","fly"
};
const int actprep_count = 4;
char *prep_en(char* prep) {
        if(prep) {
                if(strcmp(prep,"su") == 0) return "#on";
                else if(strcmp(prep,"in") == 0) return "#in";
                else if(strcmp(prep,"dietro") == 0) return "#behind";
                else if(strcmp(prep,"sotto") == 0) return "#under";
                else if(strcmp(prep,"a") == 0) return "to";
        }
        return "";
}
char *prep_it(char* prep) {
        if(prep) {
                if(strcmp(prep,"su") == 0) return "su/sul/sull/sullo/sulla/sui/sugli/sulle";
                else if(strcmp(prep,"in") == 0) return " in/nel/nell/nello/nella/nei/negli/nelle/dentro/attraverso";
                else if(strcmp(prep,"dietro") == 0) return "dietro";
                else if(strcmp(prep,"sotto") == 0) return "sotto";
                else if(strcmp(prep,"a") == 0) return "a/al/all/allo/alla/ai/agli/alle";
        }
        return "";
}
char *verb_en(char *verb_it) {
        char *action = NULL;
        for(int ct = 0; ct < actions_count; ct++) {
                if(strcmp(verb_it,actions_it[ct]) == 0) {
                        action = actions_en[ct];
                        break;
                }
        }
        if(action == NULL) {
                for(int ct = 0; ct < newActCmd.len; ct++) {
                        if(strcmp(verb_it,newActInf.strings[ct]) == 0) {
                                action = newActInf.strings[ct];
                                break;
                        }
                }
                if(action == NULL)
                        return "SCONOSCIUTO";
        }
        return action;
}
void p_action(char* handler, char *azione, char *obj1, char *obj2, char *prep) {
        char *action = NULL;
        if(strcmp(handler,"after")==0) {
                for(int ct = 0; ct < actions_count; ct++) {
                        if(strcmp(azione,actions_it_pp[ct]) == 0) {
                                action = actions_en[ct];
                                break;
                        }
                }
        } else {
                for(int ct = 0; ct < actions_count; ct++) {
                        if(strcmp(azione,actions_it[ct]) == 0) {
                                action = actions_en[ct];
                                break;
                        }
                }
        }
        if(action == NULL) {
                if(strcmp(handler,"after")==0) {
                        for(int ct = 0; ct < newActCmd.len; ct++) {
                                if(strcmp(newActPP.strings[ct],"") == 0) {
                                        char *inf = newActInf.strings[ct];
                                        if(strncmp(azione,inf,strlen(inf)-2) == 0) {
                                                action = newActInf.strings[ct];
                                                break;
                                        }
                                } else {
                                        if(strcmp(azione,newActPP.strings[ct]) == 0) {
                                                action = newActInf.strings[ct];
                                                break;
                                        }
                                }
                        }
                } else {
                        for(int ct = 0; ct < newActCmd.len; ct++) {
                                if(strcmp(azione,newActInf.strings[ct]) == 0) {
                                        action = newActInf.strings[ct];
                                        break;
                                }
                        }
                }
                if(action == NULL) {
                        p_error("AZIONE SCONOSCIUTA",azione);
                        return;
                }
        }
        _pindent("(%s [%s %s %s %s]) ",handler,action,obj1,prep? prep_en(prep): "",obj2);
}
void add_action(char *cmd, int args, char *inf, char *pp, char *prep) {
        array_add(&newActCmd,cmd,0,0,0);
        array_add(&newActInf,inf,0,0,0);
        array_add(&newActPP,pp? pp: "",0,0,0);
        array_add(&newActPrep,prep? prep: "",0,0,0);
        printf("(grammar [%s %s %s %s] for [%s %s %s %s])",
                cmd,args>0?"[object]":"",prep? prep_it(prep): "",args>1?"[object]":"",
                inf,args>0?"$":"",prep? prep_en(prep): "",args>1?"$":"");
}
void p_plaintext(char *str) {printf("%s ",str);}

#ifndef PCC_BUFFERSIZE
#define PCC_BUFFERSIZE 256
#endif /* !PCC_BUFFERSIZE */

#ifndef PCC_ARRAYSIZE
#define PCC_ARRAYSIZE 2
#endif /* !PCC_ARRAYSIZE */

#define VOID_VALUE (~(size_t)0)

typedef enum pcc_bool_tag {
    PCC_FALSE = 0,
    PCC_TRUE
} pcc_bool_t;

typedef struct pcc_char_array_tag {
    char *buf;
    size_t max;
    size_t len;
} pcc_char_array_t;

typedef struct pcc_range_tag {
    size_t start;
    size_t end;
} pcc_range_t;

typedef char *pcc_value_t;

typedef void *pcc_auxil_t;

typedef struct pcc_value_table_tag {
    pcc_value_t *buf;
    size_t max;
    size_t len;
} pcc_value_table_t;

typedef struct pcc_value_refer_table_tag {
    pcc_value_t **buf;
    size_t max;
    size_t len;
} pcc_value_refer_table_t;

typedef struct pcc_capture_tag {
    pcc_range_t range;
    char *string; /* mutable */
} pcc_capture_t;

typedef struct pcc_capture_table_tag {
    pcc_capture_t *buf;
    size_t max;
    size_t len;
} pcc_capture_table_t;

typedef struct pcc_capture_const_table_tag {
    const pcc_capture_t **buf;
    size_t max;
    size_t len;
} pcc_capture_const_table_t;

typedef struct pcc_thunk_tag pcc_thunk_t;
typedef struct pcc_thunk_array_tag pcc_thunk_array_t;

typedef void (*pcc_action_t)(pcc_context_t *, pcc_thunk_t *, pcc_value_t *);

typedef enum pcc_thunk_type_tag {
    PCC_THUNK_LEAF,
    PCC_THUNK_NODE
} pcc_thunk_type_t;

typedef struct pcc_thunk_leaf_tag {
    pcc_value_refer_table_t values;
    pcc_capture_const_table_t capts;
    pcc_capture_t capt0;
    pcc_action_t action;
} pcc_thunk_leaf_t;

typedef struct pcc_thunk_node_tag {
    const pcc_thunk_array_t *thunks; /* just a reference */
    pcc_value_t *value; /* just a reference */
} pcc_thunk_node_t;

typedef union pcc_thunk_data_tag {
    pcc_thunk_leaf_t leaf;
    pcc_thunk_node_t node;
} pcc_thunk_data_t;

struct pcc_thunk_tag {
    pcc_thunk_type_t type;
    pcc_thunk_data_t data;
};

struct pcc_thunk_array_tag {
    pcc_thunk_t **buf;
    size_t max;
    size_t len;
};

typedef struct pcc_thunk_chunk_tag {
    pcc_value_table_t values;
    pcc_capture_table_t capts;
    pcc_thunk_array_t thunks;
    size_t pos; /* the starting position in the character buffer */
} pcc_thunk_chunk_t;

typedef struct pcc_lr_entry_tag pcc_lr_entry_t;

typedef enum pcc_lr_answer_type_tag {
    PCC_LR_ANSWER_LR,
    PCC_LR_ANSWER_CHUNK
} pcc_lr_answer_type_t;

typedef union pcc_lr_answer_data_tag {
    pcc_lr_entry_t *lr;
    pcc_thunk_chunk_t *chunk;
} pcc_lr_answer_data_t;

typedef struct pcc_lr_answer_tag pcc_lr_answer_t;

struct pcc_lr_answer_tag {
    pcc_lr_answer_type_t type;
    pcc_lr_answer_data_t data;
    size_t pos; /* the absolute position in the input */
    pcc_lr_answer_t *hold;
};

typedef pcc_thunk_chunk_t *(*pcc_rule_t)(pcc_context_t *);

typedef struct pcc_rule_set_tag {
    pcc_rule_t *buf;
    size_t max;
    size_t len;
} pcc_rule_set_t;

typedef struct pcc_lr_head_tag pcc_lr_head_t;

struct pcc_lr_head_tag {
    pcc_rule_t rule;
    pcc_rule_set_t invol;
    pcc_rule_set_t eval;
    pcc_lr_head_t *hold;
};

typedef struct pcc_lr_memo_tag {
    pcc_rule_t rule;
    pcc_lr_answer_t *answer;
} pcc_lr_memo_t;

typedef struct pcc_lr_memo_map_tag {
    pcc_lr_memo_t *buf;
    size_t max;
    size_t len;
} pcc_lr_memo_map_t;

typedef struct pcc_lr_table_entry_tag {
    pcc_lr_head_t *head; /* just a reference */
    pcc_lr_memo_map_t memos;
    pcc_lr_answer_t *hold_a;
    pcc_lr_head_t *hold_h;
} pcc_lr_table_entry_t;

typedef struct pcc_lr_table_tag {
    pcc_lr_table_entry_t **buf;
    size_t max;
    size_t len;
} pcc_lr_table_t;

struct pcc_lr_entry_tag {
    pcc_rule_t rule;
    pcc_thunk_chunk_t *seed; /* just a reference */
    pcc_lr_head_t *head; /* just a reference */
};

typedef struct pcc_lr_stack_tag {
    pcc_lr_entry_t **buf;
    size_t max;
    size_t len;
} pcc_lr_stack_t;

struct pcc_context_tag {
    size_t pos; /* the position in the input of the first character currently buffered */
    size_t cur; /* the current parsing position in the character buffer */
    size_t level;
    pcc_char_array_t buffer;
    pcc_lr_table_t lrtable;
    pcc_lr_stack_t lrstack;
    pcc_auxil_t auxil;
};

#ifndef PCC_ERROR
#define PCC_ERROR(auxil) pcc_error()
static void pcc_error(void) {
    fprintf(stderr, "Syntax error\n");
    exit(1);
}
#endif /* !PCC_ERROR */

#ifndef PCC_GETCHAR
#define PCC_GETCHAR(auxil) getchar()
#endif /* !PCC_GETCHAR */

#ifndef PCC_MALLOC
#define PCC_MALLOC(auxil, size) pcc_malloc_e(size)
static void *pcc_malloc_e(size_t size) {
    void *const p = malloc(size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* !PCC_MALLOC */

#ifndef PCC_REALLOC
#define PCC_REALLOC(auxil, ptr, size) pcc_realloc_e(ptr, size)
static void *pcc_realloc_e(void *ptr, size_t size) {
    void *const p = realloc(ptr, size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* !PCC_REALLOC */

#ifndef PCC_FREE
#define PCC_FREE(auxil, ptr) free(ptr)
#endif /* !PCC_FREE */

#ifndef PCC_DEBUG
#define PCC_DEBUG(event, rule, level, pos, buffer, length) ((void)0)
#endif /* !PCC_DEBUG */

static char *pcc_strndup_e(pcc_auxil_t auxil, const char *str, size_t len) {
    const size_t m = strnlen(str, len);
    char *const s = (char *)PCC_MALLOC(auxil, m + 1);
    memcpy(s, str, m);
    s[m] = '\0';
    return s;
}

static void pcc_char_array__init(pcc_auxil_t auxil, pcc_char_array_t *array, size_t max) {
    array->len = 0;
    array->max = max;
    array->buf = (char *)PCC_MALLOC(auxil, array->max);
}

static void pcc_char_array__add(pcc_auxil_t auxil, pcc_char_array_t *array, char ch) {
    if (array->max <= array->len) {
        const size_t n = array->len + 1;
        size_t m = array->max;
        if (m == 0) m = 1;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        array->buf = (char *)PCC_REALLOC(auxil, array->buf, m);
        array->max = m;
    }
    array->buf[array->len++] = ch;
}

static void pcc_char_array__term(pcc_auxil_t auxil, pcc_char_array_t *array) {
    PCC_FREE(auxil, array->buf);
}

static void pcc_value_table__init(pcc_auxil_t auxil, pcc_value_table_t *table, size_t max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t *)PCC_MALLOC(auxil, sizeof(pcc_value_t) * table->max);
}

static void pcc_value_table__resize(pcc_auxil_t auxil, pcc_value_table_t *table, size_t len) {
    if (table->max < len) {
        size_t m = table->max;
        if (m == 0) m = 1;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        table->buf = (pcc_value_t *)PCC_REALLOC(auxil, table->buf, sizeof(pcc_value_t) * m);
        table->max = m;
    }
    table->len = len;
}

static void pcc_value_table__term(pcc_auxil_t auxil, pcc_value_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_value_refer_table__init(pcc_auxil_t auxil, pcc_value_refer_table_t *table, size_t max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t **)PCC_MALLOC(auxil, sizeof(pcc_value_t *) * table->max);
}

static void pcc_value_refer_table__resize(pcc_auxil_t auxil, pcc_value_refer_table_t *table, size_t len) {
    size_t i;
    if (table->max < len) {
        size_t m = table->max;
        if (m == 0) m = 1;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        table->buf = (pcc_value_t **)PCC_REALLOC(auxil, table->buf, sizeof(pcc_value_t *) * m);
        table->max = m;
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_value_refer_table__term(pcc_auxil_t auxil, pcc_value_refer_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_table__init(pcc_auxil_t auxil, pcc_capture_table_t *table, size_t max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_capture_t *)PCC_MALLOC(auxil, sizeof(pcc_capture_t) * table->max);
}

static void pcc_capture_table__resize(pcc_auxil_t auxil, pcc_capture_table_t *table, size_t len) {
    size_t i;
    for (i = len; i < table->len; i++) PCC_FREE(auxil, table->buf[i].string);
    if (table->max < len) {
        size_t m = table->max;
        if (m == 0) m = 1;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        table->buf = (pcc_capture_t *)PCC_REALLOC(auxil, table->buf, sizeof(pcc_capture_t) * m);
        table->max = m;
    }
    for (i = table->len; i < len; i++) {
        table->buf[i].range.start = 0;
        table->buf[i].range.end = 0;
        table->buf[i].string = NULL;
    }
    table->len = len;
}

static void pcc_capture_table__term(pcc_auxil_t auxil, pcc_capture_table_t *table) {
    while (table->len > 0) {
        table->len--;
        PCC_FREE(auxil, table->buf[table->len].string);
    }
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_const_table__init(pcc_auxil_t auxil, pcc_capture_const_table_t *table, size_t max) {
    table->len = 0;
    table->max = max;
    table->buf = (const pcc_capture_t **)PCC_MALLOC(auxil, sizeof(const pcc_capture_t *) * table->max);
}

static void pcc_capture_const_table__resize(pcc_auxil_t auxil, pcc_capture_const_table_t *table, size_t len) {
    size_t i;
    if (table->max < len) {
        size_t m = table->max;
        if (m == 0) m = 1;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        table->buf = (const pcc_capture_t **)PCC_REALLOC(auxil, (pcc_capture_t **)table->buf, sizeof(const pcc_capture_t *) * m);
        table->max = m;
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_capture_const_table__term(pcc_auxil_t auxil, pcc_capture_const_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static pcc_thunk_t *pcc_thunk__create_leaf(pcc_auxil_t auxil, pcc_action_t action, size_t valuec, size_t captc) {
    pcc_thunk_t *const thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_LEAF;
    pcc_value_refer_table__init(auxil, &thunk->data.leaf.values, valuec);
    pcc_value_refer_table__resize(auxil, &thunk->data.leaf.values, valuec);
    pcc_capture_const_table__init(auxil, &thunk->data.leaf.capts, captc);
    pcc_capture_const_table__resize(auxil, &thunk->data.leaf.capts, captc);
    thunk->data.leaf.capt0.range.start = 0;
    thunk->data.leaf.capt0.range.end = 0;
    thunk->data.leaf.capt0.string = NULL;
    thunk->data.leaf.action = action;
    return thunk;
}

static pcc_thunk_t *pcc_thunk__create_node(pcc_auxil_t auxil, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    pcc_thunk_t *const thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_NODE;
    thunk->data.node.thunks = thunks;
    thunk->data.node.value = value;
    return thunk;
}

static void pcc_thunk__destroy(pcc_auxil_t auxil, pcc_thunk_t *thunk) {
    if (thunk == NULL) return;
    switch (thunk->type) {
    case PCC_THUNK_LEAF:
        PCC_FREE(auxil, thunk->data.leaf.capt0.string);
        pcc_capture_const_table__term(auxil, &thunk->data.leaf.capts);
        pcc_value_refer_table__term(auxil, &thunk->data.leaf.values);
        break;
    case PCC_THUNK_NODE:
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, thunk);
}

static void pcc_thunk_array__init(pcc_auxil_t auxil, pcc_thunk_array_t *array, size_t max) {
    array->len = 0;
    array->max = max;
    array->buf = (pcc_thunk_t **)PCC_MALLOC(auxil, sizeof(pcc_thunk_t *) * array->max);
}

static void pcc_thunk_array__add(pcc_auxil_t auxil, pcc_thunk_array_t *array, pcc_thunk_t *thunk) {
    if (array->max <= array->len) {
        const size_t n = array->len + 1;
        size_t m = array->max;
        if (m == 0) m = 1;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        array->buf = (pcc_thunk_t **)PCC_REALLOC(auxil, array->buf, sizeof(pcc_thunk_t *) * m);
        array->max = m;
    }
    array->buf[array->len++] = thunk;
}

static void pcc_thunk_array__revert(pcc_auxil_t auxil, pcc_thunk_array_t *array, size_t len) {
    while (array->len > len) {
        array->len--;
        pcc_thunk__destroy(auxil, array->buf[array->len]);
    }
}

static void pcc_thunk_array__term(pcc_auxil_t auxil, pcc_thunk_array_t *array) {
    while (array->len > 0) {
        array->len--;
        pcc_thunk__destroy(auxil, array->buf[array->len]);
    }
    PCC_FREE(auxil, array->buf);
}

static pcc_thunk_chunk_t *pcc_thunk_chunk__create(pcc_auxil_t auxil) {
    pcc_thunk_chunk_t *const chunk = (pcc_thunk_chunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_chunk_t));
    pcc_value_table__init(auxil, &chunk->values, PCC_ARRAYSIZE);
    pcc_capture_table__init(auxil, &chunk->capts, PCC_ARRAYSIZE);
    pcc_thunk_array__init(auxil, &chunk->thunks, PCC_ARRAYSIZE);
    chunk->pos = 0;
    return chunk;
}

static void pcc_thunk_chunk__destroy(pcc_auxil_t auxil, pcc_thunk_chunk_t *chunk) {
    if (chunk == NULL) return;
    pcc_thunk_array__term(auxil, &chunk->thunks);
    pcc_capture_table__term(auxil, &chunk->capts);
    pcc_value_table__term(auxil, &chunk->values);
    PCC_FREE(auxil, chunk);
}

static void pcc_rule_set__init(pcc_auxil_t auxil, pcc_rule_set_t *set, size_t max) {
    set->len = 0;
    set->max = max;
    set->buf = (pcc_rule_t *)PCC_MALLOC(auxil, sizeof(pcc_rule_t) * set->max);
}

static size_t pcc_rule_set__index(pcc_auxil_t auxil, const pcc_rule_set_t *set, pcc_rule_t rule) {
    size_t i;
    for (i = 0; i < set->len; i++) {
        if (set->buf[i] == rule) return i;
    }
    return VOID_VALUE;
}

static pcc_bool_t pcc_rule_set__add(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    const size_t i = pcc_rule_set__index(auxil, set, rule);
    if (i != VOID_VALUE) return PCC_FALSE;
    if (set->max <= set->len) {
        const size_t n = set->len + 1;
        size_t m = set->max;
        if (m == 0) m = 1;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        set->buf = (pcc_rule_t *)PCC_REALLOC(auxil, set->buf, sizeof(pcc_rule_t) * m);
        set->max = m;
    }
    set->buf[set->len++] = rule;
    return PCC_TRUE;
}

static pcc_bool_t pcc_rule_set__remove(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    const size_t i = pcc_rule_set__index(auxil, set, rule);
    if (i == VOID_VALUE) return PCC_FALSE;
    memmove(set->buf + i, set->buf + (i + 1), sizeof(pcc_rule_t) * (set->len - (i + 1)));
    return PCC_TRUE;
}

static void pcc_rule_set__clear(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    set->len = 0;
}

static void pcc_rule_set__copy(pcc_auxil_t auxil, pcc_rule_set_t *set, const pcc_rule_set_t *src) {
    size_t i;
    pcc_rule_set__clear(auxil, set);
    for (i = 0; i < src->len; i++) {
        pcc_rule_set__add(auxil, set, src->buf[i]);
    }
}

static void pcc_rule_set__term(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    PCC_FREE(auxil, set->buf);
}

static pcc_lr_head_t *pcc_lr_head__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_head_t *const head = (pcc_lr_head_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_head_t));
    head->rule = rule;
    pcc_rule_set__init(auxil, &head->invol, PCC_ARRAYSIZE);
    pcc_rule_set__init(auxil, &head->eval, PCC_ARRAYSIZE);
    head->hold = NULL;
    return head;
}

static void pcc_lr_head__destroy(pcc_auxil_t auxil, pcc_lr_head_t *head) {
    if (head == NULL) return;
    pcc_lr_head__destroy(auxil, head->hold);
    pcc_rule_set__term(auxil, &head->eval);
    pcc_rule_set__term(auxil, &head->invol);
    PCC_FREE(auxil, head);
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr);

static pcc_lr_answer_t *pcc_lr_answer__create(pcc_auxil_t auxil, pcc_lr_answer_type_t type, size_t pos) {
    pcc_lr_answer_t *answer = (pcc_lr_answer_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_answer_t));
    answer->type = type;
    answer->pos = pos;
    answer->hold = NULL;
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        answer->data.lr = NULL;
        break;
    case PCC_LR_ANSWER_CHUNK:
        answer->data.chunk = NULL;
        break;
    default: /* unknown */
        PCC_FREE(auxil, answer);
        answer = NULL;
    }
    return answer;
}

static void pcc_lr_answer__set_chunk(pcc_auxil_t auxil, pcc_lr_answer_t *answer, pcc_thunk_chunk_t *chunk) {
    pcc_lr_answer_t *const a = pcc_lr_answer__create(auxil, answer->type, answer->pos);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        a->data.lr = answer->data.lr;
        break;
    case PCC_LR_ANSWER_CHUNK:
        a->data.chunk = answer->data.chunk;
        break;
    default: /* unknown */
        break;
    }
    a->hold = answer->hold;
    answer->hold = a;
    answer->type = PCC_LR_ANSWER_CHUNK;
    answer->data.chunk = chunk;
}

static void pcc_lr_answer__destroy(pcc_auxil_t auxil, pcc_lr_answer_t *answer) {
    if (answer == NULL) return;
    pcc_lr_answer__destroy(auxil, answer->hold);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        pcc_lr_entry__destroy(auxil, answer->data.lr);
        break;
    case PCC_LR_ANSWER_CHUNK:
        pcc_thunk_chunk__destroy(auxil, answer->data.chunk);
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, answer);
}

static void pcc_lr_memo_map__init(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, size_t max) {
    map->len = 0;
    map->max = max;
    map->buf = (pcc_lr_memo_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_memo_t) * map->max);
}

static size_t pcc_lr_memo_map__index(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    size_t i;
    for (i = 0; i < map->len; i++) {
        if (map->buf[i].rule == rule) return i;
    }
    return VOID_VALUE;
}

static void pcc_lr_memo_map__put(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    const size_t i = pcc_lr_memo_map__index(auxil, map, rule);
    if (i != VOID_VALUE) {
        pcc_lr_answer__destroy(auxil, map->buf[i].answer);
        map->buf[i].answer = answer;
    }
    else {
        if (map->max <= map->len) {
            const size_t n = map->len + 1;
            size_t m = map->max;
            if (m == 0) m = 1;
            while (m < n && m != 0) m <<= 1;
            if (m == 0) m = n;
            map->buf = (pcc_lr_memo_t *)PCC_REALLOC(auxil, map->buf, sizeof(pcc_lr_memo_t) * m);
            map->max = m;
        }
        map->buf[map->len].rule = rule;
        map->buf[map->len].answer = answer;
        map->len++;
    }
}

static pcc_lr_answer_t *pcc_lr_memo_map__get(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    const size_t i = pcc_lr_memo_map__index(auxil, map, rule);
    return (i != VOID_VALUE) ? map->buf[i].answer : NULL;
}

static void pcc_lr_memo_map__term(pcc_auxil_t auxil, pcc_lr_memo_map_t *map) {
    while (map->len > 0) {
        map->len--;
        pcc_lr_answer__destroy(auxil, map->buf[map->len].answer);
    }
    PCC_FREE(auxil, map->buf);
}

static pcc_lr_table_entry_t *pcc_lr_table_entry__create(pcc_auxil_t auxil) {
    pcc_lr_table_entry_t *const entry = (pcc_lr_table_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_table_entry_t));
    entry->head = NULL;
    pcc_lr_memo_map__init(auxil, &entry->memos, PCC_ARRAYSIZE);
    entry->hold_a = NULL;
    entry->hold_h = NULL;
    return entry;
}

static void pcc_lr_table_entry__destroy(pcc_auxil_t auxil, pcc_lr_table_entry_t *entry) {
    if (entry == NULL) return;
    pcc_lr_head__destroy(auxil, entry->hold_h);
    pcc_lr_answer__destroy(auxil, entry->hold_a);
    pcc_lr_memo_map__term(auxil, &entry->memos);
    PCC_FREE(auxil, entry);
}

static void pcc_lr_table__init(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_lr_table_entry_t **)PCC_MALLOC(auxil, sizeof(pcc_lr_table_entry_t *) * table->max);
}

static void pcc_lr_table__resize(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t len) {
    size_t i;
    for (i = len; i < table->len; i++) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    if (table->max < len) {
        size_t m = table->max;
        if (m == 0) m = 1;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        table->buf = (pcc_lr_table_entry_t **)PCC_REALLOC(auxil, table->buf, sizeof(pcc_lr_table_entry_t *) * m);
        table->max = m;
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_lr_table__set_head(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    table->buf[index]->head = head;
}

static void pcc_lr_table__hold_head(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    head->hold = table->buf[index]->hold_h;
    table->buf[index]->hold_h = head;
}

static void pcc_lr_table__set_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t index, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    pcc_lr_memo_map__put(auxil, &table->buf[index]->memos, rule, answer);
}

static void pcc_lr_table__hold_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t index, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    answer->hold = table->buf[index]->hold_a;
    table->buf[index]->hold_a = answer;
}

static pcc_lr_head_t *pcc_lr_table__get_head(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t index) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return table->buf[index]->head;
}

static pcc_lr_answer_t *pcc_lr_table__get_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t index, pcc_rule_t rule) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return pcc_lr_memo_map__get(auxil, &table->buf[index]->memos, rule);
}

static void pcc_lr_table__shift(pcc_auxil_t auxil, pcc_lr_table_t *table, size_t count) {
    size_t i;
    if (count > table->len) count = table->len;
    for (i = 0; i < count; i++) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    memmove(table->buf, table->buf + count, sizeof(pcc_lr_table_entry_t *) * (table->len - count));
    table->len -= count;
}

static void pcc_lr_table__term(pcc_auxil_t auxil, pcc_lr_table_t *table) {
    while (table->len > 0) {
        table->len--;
        pcc_lr_table_entry__destroy(auxil, table->buf[table->len]);
    }
    PCC_FREE(auxil, table->buf);
}

static pcc_lr_entry_t *pcc_lr_entry__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_entry_t *const lr = (pcc_lr_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_entry_t));
    lr->rule = rule;
    lr->seed = NULL;
    lr->head = NULL;
    return lr;
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr) {
    PCC_FREE(auxil, lr);
}

static void pcc_lr_stack__init(pcc_auxil_t auxil, pcc_lr_stack_t *stack, size_t max) {
    stack->len = 0;
    stack->max = max;
    stack->buf = (pcc_lr_entry_t **)PCC_MALLOC(auxil, sizeof(pcc_lr_entry_t *) * stack->max);
}

static void pcc_lr_stack__push(pcc_auxil_t auxil, pcc_lr_stack_t *stack, pcc_lr_entry_t *lr) {
    if (stack->max <= stack->len) {
        const size_t n = stack->len + 1;
        size_t m = stack->max;
        if (m == 0) m = 1;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        stack->buf = (pcc_lr_entry_t **)PCC_REALLOC(auxil, stack->buf, sizeof(pcc_lr_entry_t *) * m);
        stack->max = m;
    }
    stack->buf[stack->len++] = lr;
}

static pcc_lr_entry_t *pcc_lr_stack__pop(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    return stack->buf[--stack->len];
}

static void pcc_lr_stack__term(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    PCC_FREE(auxil, stack->buf);
}

static pcc_context_t *pcc_context__create(pcc_auxil_t auxil) {
    pcc_context_t *const ctx = (pcc_context_t *)PCC_MALLOC(auxil, sizeof(pcc_context_t));
    ctx->pos = 0;
    ctx->cur = 0;
    ctx->level = 0;
    pcc_char_array__init(auxil, &ctx->buffer, PCC_BUFFERSIZE);
    pcc_lr_table__init(auxil, &ctx->lrtable, PCC_BUFFERSIZE);
    pcc_lr_stack__init(auxil, &ctx->lrstack, PCC_ARRAYSIZE);
    ctx->auxil = auxil;
    return ctx;
}

static void pcc_context__destroy(pcc_context_t *ctx) {
    if (ctx == NULL) return;
    pcc_lr_stack__term(ctx->auxil, &ctx->lrstack);
    pcc_lr_table__term(ctx->auxil, &ctx->lrtable);
    pcc_char_array__term(ctx->auxil, &ctx->buffer);
    PCC_FREE(ctx->auxil, ctx);
}

static size_t pcc_refill_buffer(pcc_context_t *ctx, size_t num) {
    if (ctx->buffer.len >= ctx->cur + num) return ctx->buffer.len - ctx->cur;
    while (ctx->buffer.len < ctx->cur + num) {
        const int c = PCC_GETCHAR(ctx->auxil);
        if (c == EOF) break;
        pcc_char_array__add(ctx->auxil, &ctx->buffer, (char)c);
    }
    return ctx->buffer.len - ctx->cur;
}

static void pcc_commit_buffer(pcc_context_t *ctx) {
    memmove(ctx->buffer.buf, ctx->buffer.buf + ctx->cur, ctx->buffer.len - ctx->cur);
    ctx->buffer.len -= ctx->cur;
    ctx->pos += ctx->cur;
    pcc_lr_table__shift(ctx->auxil, &ctx->lrtable, ctx->cur);
    ctx->cur = 0;
}

static const char *pcc_get_capture_string(pcc_context_t *ctx, const pcc_capture_t *capt) {
    if (capt->string == NULL)
        ((pcc_capture_t *)capt)->string =
            pcc_strndup_e(ctx->auxil, ctx->buffer.buf + capt->range.start, capt->range.end - capt->range.start);
    return capt->string;
}

static size_t pcc_get_char_as_utf32(pcc_context_t *ctx, int *out) { /* with checking UTF-8 validity */
    int c, u;
    size_t n;
    if (pcc_refill_buffer(ctx, 1) < 1) return 0;
    c = (int)(unsigned char)ctx->buffer.buf[ctx->cur];
    n = (c < 0x80) ? 1 :
        ((c & 0xe0) == 0xc0) ? 2 :
        ((c & 0xf0) == 0xe0) ? 3 :
        ((c & 0xf8) == 0xf0) ? 4 : 0;
    if (n < 1) return 0;
    if (pcc_refill_buffer(ctx, n) < n) return 0;
    switch (n) {
    case 1:
        u = c;
        break;
    case 2:
        u = c & 0x1f;
        c = (int)(unsigned char)ctx->buffer.buf[ctx->cur + 1];
        if ((c & 0xc0) != 0x80) return 0;
        u <<= 6; u |= c & 0x3f;
        if (u < 0x80) return 0;
        break;
    case 3:
        u = c & 0x0f;
        c = (int)(unsigned char)ctx->buffer.buf[ctx->cur + 1];
        if ((c & 0xc0) != 0x80) return 0;
        u <<= 6; u |= c & 0x3f;
        c = (int)(unsigned char)ctx->buffer.buf[ctx->cur + 2];
        if ((c & 0xc0) != 0x80) return 0;
        u <<= 6; u |= c & 0x3f;
        if (u < 0x800) return 0;
        break;
    case 4:
        u = c & 0x07;
        c = (int)(unsigned char)ctx->buffer.buf[ctx->cur + 1];
        if ((c & 0xc0) != 0x80) return 0;
        u <<= 6; u |= c & 0x3f;
        c = (int)(unsigned char)ctx->buffer.buf[ctx->cur + 2];
        if ((c & 0xc0) != 0x80) return 0;
        u <<= 6; u |= c & 0x3f;
        c = (int)(unsigned char)ctx->buffer.buf[ctx->cur + 3];
        if ((c & 0xc0) != 0x80) return 0;
        u <<= 6; u |= c & 0x3f;
        if (u < 0x10000 || u > 0x10ffff) return 0;
        break;
    default:
        return 0;
    }
    if (out) *out = u;
    return n;
}

static pcc_bool_t pcc_apply_rule(pcc_context_t *ctx, pcc_rule_t rule, pcc_thunk_array_t *thunks, pcc_value_t *value) {
    static pcc_value_t null;
    pcc_thunk_chunk_t *c = NULL;
    const size_t p = ctx->pos + ctx->cur;
    pcc_bool_t b = PCC_TRUE;
    pcc_lr_answer_t *a = pcc_lr_table__get_answer(ctx->auxil, &ctx->lrtable, p, rule);
    pcc_lr_head_t *h = pcc_lr_table__get_head(ctx->auxil, &ctx->lrtable, p);
    if (h != NULL) {
        if (a == NULL && rule != h->rule && pcc_rule_set__index(ctx->auxil, &h->invol, rule) == VOID_VALUE) {
            b = PCC_FALSE;
            c = NULL;
        }
        else if (pcc_rule_set__remove(ctx->auxil, &h->eval, rule)) {
            b = PCC_FALSE;
            c = rule(ctx);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos + ctx->cur);
            a->data.chunk = c;
            pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
        }
    }
    if (b) {
        if (a != NULL) {
            ctx->cur = a->pos - ctx->pos;
            switch (a->type) {
            case PCC_LR_ANSWER_LR:
                if (a->data.lr->head == NULL) {
                    a->data.lr->head = pcc_lr_head__create(ctx->auxil, rule);
                    pcc_lr_table__hold_head(ctx->auxil, &ctx->lrtable, p, a->data.lr->head);
                }
                {
                    size_t i = ctx->lrstack.len;
                    while (i > 0) {
                        i--;
                        if (ctx->lrstack.buf[i]->head == a->data.lr->head) break;
                        ctx->lrstack.buf[i]->head = a->data.lr->head;
                        pcc_rule_set__add(ctx->auxil, &a->data.lr->head->invol, ctx->lrstack.buf[i]->rule);
                    }
                }
                c = a->data.lr->seed;
                break;
            case PCC_LR_ANSWER_CHUNK:
                c = a->data.chunk;
                break;
            default: /* unknown */
                break;
            }
        }
        else {
            pcc_lr_entry_t *const e = pcc_lr_entry__create(ctx->auxil, rule);
            pcc_lr_stack__push(ctx->auxil, &ctx->lrstack, e);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_LR, p);
            a->data.lr = e;
            pcc_lr_table__set_answer(ctx->auxil, &ctx->lrtable, p, rule, a);
            c = rule(ctx);
            pcc_lr_stack__pop(ctx->auxil, &ctx->lrstack);
            a->pos = ctx->pos + ctx->cur;
            if (e->head == NULL) {
                pcc_lr_answer__set_chunk(ctx->auxil, a, c);
            }
            else {
                e->seed = c;
                h = a->data.lr->head;
                if (h->rule != rule) {
                    c = a->data.lr->seed;
                    a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos + ctx->cur);
                    a->data.chunk = c;
                    pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
                }
                else {
                    pcc_lr_answer__set_chunk(ctx->auxil, a, a->data.lr->seed);
                    if (a->data.chunk == NULL) {
                        c = NULL;
                    }
                    else {
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, h);
                        for (;;) {
                            ctx->cur = p - ctx->pos;
                            pcc_rule_set__copy(ctx->auxil, &h->eval, &h->invol);
                            c = rule(ctx);
                            if (c == NULL || ctx->pos + ctx->cur <= a->pos) break;
                            pcc_lr_answer__set_chunk(ctx->auxil, a, c);
                            a->pos = ctx->pos + ctx->cur;
                        }
                        pcc_thunk_chunk__destroy(ctx->auxil, c);
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, NULL);
                        ctx->cur = a->pos - ctx->pos;
                        c = a->data.chunk;
                    }
                }
            }
        }
    }
    if (c == NULL) return PCC_FALSE;
    if (value == NULL) value = &null;
    memset(value, 0, sizeof(pcc_value_t)); /* in case */
    pcc_thunk_array__add(ctx->auxil, thunks, pcc_thunk__create_node(ctx->auxil, &c->thunks, value));
    return PCC_TRUE;
}

static void pcc_do_action(pcc_context_t *ctx, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    size_t i;
    for (i = 0; i < thunks->len; i++) {
        pcc_thunk_t *const thunk = thunks->buf[i];
        switch (thunk->type) {
        case PCC_THUNK_LEAF:
            thunk->data.leaf.action(ctx, thunk, value);
            break;
        case PCC_THUNK_NODE:
            pcc_do_action(ctx, thunk->data.node.thunks, thunk->data.node.value);
            break;
        default: /* unknown */
            break;
        }
    }
}

static void pcc_action_sentence_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("\n"); ret_mode = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if(last_indent == NULL) {printf("\n"); ret_mode = 1;} else {ret_mode = 0;}
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define last (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("%%%%%s\n",last);
#undef _0e
#undef _0s
#undef _0
#undef last
#undef __
#undef auxil
}

static void pcc_action_sentence_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)
            p_room(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct]);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) {
            int dir = last_list1.arg1[ct];
            char* locfrom = last_list1.strings[ct];
            if(last_list1.arg2) p_fromtoandback(dir, locfrom, last_name);
            else p_fromto(dir, locfrom, last_name);
    }
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) {
            int dir = last_list1.arg1[ct];
            char* locto = last_list1.strings[ct];
            if(last_list1.arg2[ct]) p_fromtoandback(dir, last_name, locto);
            else p_fromto(dir, last_name, locto);
    }
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_container(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct],1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_11(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_supporter(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct],1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_12(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_supporter(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct],0);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_13(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_container(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct],0);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_14(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_person(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct]);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_15(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_object(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct]);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_16(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_object(last_list1.strings[ct],last_list1.arg1[ct],last_list1.arg2[ct],last_list1.arg3[ct]);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_17(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"in",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_18(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"on",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_19(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"wornby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_20(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"heldby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_21(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_22(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"in",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_23(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"on",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_24(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"wornby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_25(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"heldby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_26(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"in",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_27(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"on",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_28(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"wornby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_29(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"heldby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_30(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(endif)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_31(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[1])
#define bool (*__pcc_in->data.leaf.values.buf[2])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    printf("(now)"); if((int)cond != (int)bool) printf("~"); printf("(%s)",_1);
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef bool
#undef cond
#undef __
#undef auxil
}

static void pcc_action_sentence_32(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s %s)",act,last_name);
#undef _0e
#undef _0s
#undef _0
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_33(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define dir (*__pcc_in->data.leaf.values.buf[5])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_extstring(&last_loc,'#',dirs[(int)dir]); p_action(act,verb,last_loc,"",NULL);
#undef _0e
#undef _0s
#undef _0
#undef dir
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_34(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_35(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name2,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_36(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,last_name2,"su");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_37(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name2,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_38(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,last_name2,"in");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_39(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name2,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_40(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,last_name2,"a");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_41(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name2,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_42(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,last_name2,"dietro");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_43(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name2,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_44(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,last_name2,"sotto");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_45(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_46(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define prep (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name2,"$");
#undef _0e
#undef _0s
#undef _0
#undef prep
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_47(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define prep (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,last_name2,prep);
#undef _0e
#undef _0s
#undef _0
#undef prep
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_48(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_name,"$");
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_49(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,last_name,"",NULL);
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_50(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define act (*__pcc_in->data.leaf.values.buf[3])
#define verb (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_action(act,verb,"","",NULL);
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef act
#undef __
#undef auxil
}

static void pcc_action_sentence_51(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cmd (*__pcc_in->data.leaf.values.buf[7])
#define prep (*__pcc_in->data.leaf.values.buf[6])
#define inf (*__pcc_in->data.leaf.values.buf[8])
#define pp (*__pcc_in->data.leaf.values.buf[9])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    add_action(cmd,2,inf,pp,prep);
#undef _0e
#undef _0s
#undef _0
#undef pp
#undef inf
#undef prep
#undef cmd
#undef __
#undef auxil
}

static void pcc_action_sentence_52(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cmd (*__pcc_in->data.leaf.values.buf[7])
#define prep (*__pcc_in->data.leaf.values.buf[6])
#define inf (*__pcc_in->data.leaf.values.buf[8])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    add_action(cmd,2,inf,NULL,prep);
#undef _0e
#undef _0s
#undef _0
#undef inf
#undef prep
#undef cmd
#undef __
#undef auxil
}

static void pcc_action_sentence_53(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cmd (*__pcc_in->data.leaf.values.buf[7])
#define inf (*__pcc_in->data.leaf.values.buf[8])
#define pp (*__pcc_in->data.leaf.values.buf[9])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    add_action(cmd,1,inf,pp,NULL);
#undef _0e
#undef _0s
#undef _0
#undef pp
#undef inf
#undef cmd
#undef __
#undef auxil
}

static void pcc_action_sentence_54(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cmd (*__pcc_in->data.leaf.values.buf[7])
#define inf (*__pcc_in->data.leaf.values.buf[8])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    add_action(cmd,1,inf,NULL,NULL);
#undef _0e
#undef _0s
#undef _0
#undef inf
#undef cmd
#undef __
#undef auxil
}

static void pcc_action_sentence_55(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cmd (*__pcc_in->data.leaf.values.buf[7])
#define inf (*__pcc_in->data.leaf.values.buf[8])
#define pp (*__pcc_in->data.leaf.values.buf[9])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    add_action(cmd,0,inf,pp,NULL);
#undef _0e
#undef _0s
#undef _0
#undef pp
#undef inf
#undef cmd
#undef __
#undef auxil
}

static void pcc_action_sentence_56(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cmd (*__pcc_in->data.leaf.values.buf[7])
#define inf (*__pcc_in->data.leaf.values.buf[8])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    add_action(cmd,0,inf,NULL,NULL);
#undef _0e
#undef _0s
#undef _0
#undef inf
#undef cmd
#undef __
#undef auxil
}

static void pcc_action_sentence_57(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if(array_contains(&rooms,last_name))	p_prop_val("look",last_name,(char*)str,0);
    else p_prop_val("descr",last_name,(char*)str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_58(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop_val("appearance",last_name,str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_59(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop_val("dict",last_name,str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_60(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("current player",last_name,1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_61(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define dir (*__pcc_in->data.leaf.values.buf[5])
#define vice (*__pcc_in->data.leaf.values.buf[11])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if(vice) p_fromtoandback((int)dir, last_locfrom, last_locto);
    else p_fromto((int)dir, last_locfrom, last_locto);
#undef _0e
#undef _0s
#undef _0
#undef vice
#undef dir
#undef __
#undef auxil
}

static void pcc_action_sentence_62(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_63(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"in",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_64(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"on",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_65(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"wornby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_66(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_rel(last_list1.strings[ct],"heldby",last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_67(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "in", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_68(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "on", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_69(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "wornby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_70(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "heldby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_71(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_room(last_name,last_genre,last_plural,last_proper);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_72(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define dir (*__pcc_in->data.leaf.values.buf[5])
#define vice (*__pcc_in->data.leaf.values.buf[11])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if(vice) p_fromtoandback((int)dir, last_locto, last_name);
    else p_fromto((int)dir, last_locto, last_name);
#undef _0e
#undef _0s
#undef _0
#undef vice
#undef dir
#undef __
#undef auxil
}

static void pcc_action_sentence_73(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_room(last_name,last_genre,last_plural,last_proper);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_74(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_container(last_name,last_genre,last_plural,last_proper, 1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_75(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_supporter(last_name,last_genre,last_plural,last_proper, 1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_76(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_supporter(last_name,last_genre,last_plural,last_proper, 0);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_77(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_container(last_name,last_genre,last_plural,last_proper, 0);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_78(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_person(last_name,last_genre,last_plural,last_proper);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_79(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_object(last_name,last_genre,last_plural,last_proper);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_80(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "in", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_81(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "on", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_82(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "wornby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_83(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "heldby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_84(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "in", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_85(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "on", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_86(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "wornby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_87(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "heldby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_88(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(increase score by %s)",last_name2);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_89(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(increase score by 1)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_90(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(decrease score by %s)",last_name2);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_91(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(decrease score by 1)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_92(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(collect %s)*",last_name2);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_93(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(into %s)",last_name3);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_94(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(append %s %s %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_95(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(append ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_96(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" %s %s)",last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_97(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(append %s ",last_name2);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_98(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_99(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(append ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_100(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_101(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_102(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s plus %s into %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_103(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s minus %s into %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_104(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s times %s into %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_105(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s divided by %s into %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_106(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s modulo %s into %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_107(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(raddom from %s to %s into %s)",last_name2,last_name3,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_108(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(fail)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_109(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(uppercase)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_110(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(quit)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_111(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(restart)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_112(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(save %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_113(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(restore)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_114(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(undo)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_115(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(game over)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_116(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(intro)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_117(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(banner)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_118(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(enter %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_119(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(scoring enabled)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_120(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("~(scoring enabled)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_121(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define num (*__pcc_in->data.leaf.values.buf[12])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(maximum score %s)",num);
#undef _0e
#undef _0s
#undef _0
#undef num
#undef __
#undef auxil
}

static void pcc_action_sentence_122(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(open or closed %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_123(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("{(%s is on) acces(oaie %s) (or) spent(oaie %s)}",last_name,last_name,last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_124(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_cmd_val("story title",str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_125(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_cmd_val("story blurb",str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_126(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_cmd_val("story noun",str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_127(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_cmd_val("story author",str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_128(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_cmd_val("story ifid",str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_129(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(story release %d)\n",atoi(str));
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_130(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(prep-di %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_131(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(prep-a %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_132(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(prep-da %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_133(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(prep-in %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_134(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(prep-su %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_135(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(a %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_136(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if(last_proper == 1) printf("{(object %s)(name %s)(or)%s}",last_name,last_name,last_name);
    else printf("(the %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_137(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define adj (*__pcc_in->data.leaf.values.buf[13])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("%s(oaie %s)",adj,last_name);
#undef _0e
#undef _0s
#undef _0
#undef adj
#undef __
#undef auxil
}

static void pcc_action_sentence_138(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_plaintext(str);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_139(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_plaintext(str);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_140(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("\n");
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_141(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[10])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_plaintext(str);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_sentence_142(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("\n");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_sentence_143(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define last (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_error("ERRORE DI SINTASSI",last);
#undef _0e
#undef _0s
#undef _0
#undef last
#undef __
#undef auxil
}

static void pcc_action_ADJECT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(select) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ORSTATEMENTS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (or) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECTMETHOD_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (at random) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECTMETHOD_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (stopping) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECTMETHOD_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (cycling) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECTMETHOD_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (purely at random) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECTMETHOD_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (then at random) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SELECTMETHOD_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" (then purely at random) ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_IF_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(if)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_THEN_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(then)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ELSE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(else)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ELSEIF_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(elseif)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITIONS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    ret_mode = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITIONS_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(or)");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITIONS_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    ret_mode = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~{");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(room %s)",last_list1.strings[ct]); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(vehicles %s)",last_list1.strings[ct]); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(supporter %s)",last_list1.strings[ct]); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(containers %s)",last_list1.strings[ct]); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(animate %s)",last_list1.strings[ct]); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(%s is #in %s)",last_list1.strings[ct],last_loc); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(%s is #on %s)",last_list1.strings[ct],last_loc); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(%s is #wornby %s)",last_list1.strings[ct],last_loc); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_11(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) printf("(%s is #heldby %s)",last_list1.strings[ct],last_loc); if((int)cond == 0) printf(" } ");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_12(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define bool (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    if((int)cond != (int)bool) printf("~"); printf("(%s)",_1);
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef bool
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_13(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define verb (*__pcc_in->data.leaf.values.buf[2])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("{(%s = [$Verb|$])",last_name); if((int)cond == 0) printf("~"); printf("($Verb = @%s)}",verb_en(verb));
#undef _0e
#undef _0s
#undef _0
#undef verb
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_14(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~"); printf("(command %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_15(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~"); p_prop("current room",last_name,0);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_16(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~"); printf("(player can see)");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_17(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~"); p_prop("current player",last_name,0);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_18(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define dir (*__pcc_in->data.leaf.values.buf[3])
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~");
    printf("(from %s go #%s to %s)", last_locfrom, dirs[(int)dir], last_locto);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef dir
#undef __
#undef auxil
}

static void pcc_action_CONDITION_19(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_free(&last_list1);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_20(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) {if((int)cond == 0) printf("~"); p_is_rel(last_list1.strings[ct],"in",last_loc,ret_mode);}
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_21(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) {if((int)cond == 0) printf("~"); p_is_rel(last_list1.strings[ct],"on",last_loc,ret_mode);}
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_22(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) {if((int)cond == 0) printf("~"); p_is_rel(last_list1.strings[ct],"wornby",last_loc,ret_mode);}
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_23(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) {if((int)cond == 0) printf("~"); p_is_rel(last_list1.strings[ct],"heldby",last_loc,ret_mode);}
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_24(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_25(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "in", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_26(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "on", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_27(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "wornby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_28(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_rel(last_name, "heldby", last_loc,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_29(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf("~");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_30(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define dir (*__pcc_in->data.leaf.values.buf[3])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(from %s go #%s to %s)", last_name, dirs[(int)dir], last_locto);
#undef _0e
#undef _0s
#undef _0
#undef dir
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_31(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is #in %s)",last_name,last_loc);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_32(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is #on %s)",last_name,last_loc);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_33(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is #wornby %s)",last_name,last_loc);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_34(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is #heldby %s)",last_name,last_loc);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_35(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(room %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_36(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(vehicle %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_37(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(supporter %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_38(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(container %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_39(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(person %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_40(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is one of %s)",last_name,last_name2);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_41(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is one of ",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_42(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(")");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_43(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define num (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is one of %s)",num,last_name2);
#undef _0e
#undef _0s
#undef _0
#undef num
#undef __
#undef auxil
}

static void pcc_action_CONDITION_44(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define num (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s is one of ",num);
#undef _0e
#undef _0s
#undef _0
#undef num
#undef __
#undef auxil
}

static void pcc_action_CONDITION_45(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define num (*__pcc_in->data.leaf.values.buf[4])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(")");
#undef _0e
#undef _0s
#undef _0
#undef num
#undef __
#undef auxil
}

static void pcc_action_CONDITION_46(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[5])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(@%s is one of %s",str,last_name2);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_CONDITION_47(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[5])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(@%s is one of ",str);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_CONDITION_48(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[5])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(")");
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_CONDITION_49(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_50(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" is one of %s)",last_name2);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_51(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_52(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" is one of ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_53(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(")");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_54(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define sym (*__pcc_in->data.leaf.values.buf[6])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s %s %s)",last_name2,sym,last_name3);
#undef _0e
#undef _0s
#undef _0
#undef sym
#undef __
#undef auxil
}

static void pcc_action_CONDITION_55(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s = ",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_56(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define list (*__pcc_in->data.leaf.values.buf[7])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(")");
#undef _0e
#undef _0s
#undef _0
#undef list
#undef __
#undef auxil
}

static void pcc_action_CONDITION_57(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_58(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define list (*__pcc_in->data.leaf.values.buf[7])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" = %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef list
#undef __
#undef auxil
}

static void pcc_action_CONDITION_59(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(%s = ",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_60(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define list (*__pcc_in->data.leaf.values.buf[7])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(")");
#undef _0e
#undef _0s
#undef _0
#undef list
#undef __
#undef auxil
}

static void pcc_action_CONDITION_61(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_CONDITION_62(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define list (*__pcc_in->data.leaf.values.buf[7])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" = %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef list
#undef __
#undef auxil
}

static void pcc_action_CONDITION_63(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if((int)cond == 0) printf(" ~");
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_64(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(number %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_65(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(unknown word %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_66(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(word %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_67(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(nonempty %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_68(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(empty %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_69(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(list %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_70(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(fully bound %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_71(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(bound %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_72(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(object %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_CONDITION_73(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define cond (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(empty %s)",last_name);
#undef _0e
#undef _0s
#undef _0
#undef cond
#undef __
#undef auxil
}

static void pcc_action_TILLEOL_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_TILLENDCMT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LASTTXT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LOHI_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_SU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LOHI_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_GIU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_INOUT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_DENTRO;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_INOUT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_FUORI;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_UPDOWN_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_GIU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_UPDOWN_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_SU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NOTVICE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_VICE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_NORDEST;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_NORDOVEST;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_NORD;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_GIU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_DENTRO;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_FUORI;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_SUDEST;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_SUDOVEST;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_SUD;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_SU;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_EST;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DIR_11(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)D_OVEST;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_DESCBY_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    if(array_contains(&rooms,last_name))	p_prop_val("look",last_name,(char*)str,0);
    else p_prop_val("descr",last_name,(char*)str,0);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_SYNON_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define str (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop_val("dict",last_name,str,1);
#undef _0e
#undef _0s
#undef _0
#undef str
#undef __
#undef auxil
}

static void pcc_action_ADJFEMALE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("female",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJPLURAL_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("plural",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJPROPER_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("proper",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJFEMALES_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("female",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJPLURALS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("plural",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJPROPERS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("proper",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJROOM_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("inherently dark",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJROOMS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("inherently dark",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("edible",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("item",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    _pindent("(%s provides light) (%s is on)\n",last_name,last_name); _pindent("(switchable %s)\n",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    _pindent("(%s provides light)\n",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("switchable",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_prop("on",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_prop("off",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("on-seat",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("on-seat",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("in-seat",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_prop("handled",last_name, ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECT_11(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("wearable",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("edible",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("item",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_prop("handled",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)
            _pindent("(%s provides light) (%s is on)\n(switchable %s)\n",
                    last_list1.strings[ct],last_list1.strings[ct],last_list1.strings[ct]
            );
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_4(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)
            _pindent("(%s provides light)\n",last_list1.strings[ct]);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_5(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)
            p_prop("switchable",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_6(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)	p_is_prop("on",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_7(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)	p_is_prop("off",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_8(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)	p_prop("on-seat",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_9(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++)	p_prop("in-seat",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJOBJECTS_10(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("wearable",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINER_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_prop("open",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINER_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_prop("closed",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINER_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_prop("openable",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINERS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_prop("open",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINERS_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    p_is_prop("closed",last_name,ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINERS_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_is_prop("closed",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ADJCONTAINERS_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    for(int ct = 0; ct < last_list1.len; ct++) p_prop("openable",last_list1.strings[ct],ret_mode);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_FULLLIST_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("[");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_FULLLIST_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("]");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LISTELS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf(" ");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LISTEL_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("$");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LISTEL_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("%s",last_name);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LISTEL_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define s (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("%s",s);
#undef _0e
#undef _0s
#undef _0
#undef s
#undef __
#undef auxil
}

static void pcc_action_LISTEL_3(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define n (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("%s",n);
#undef _0e
#undef _0s
#undef _0
#undef n
#undef __
#undef auxil
}

static void pcc_action_PARTLIST_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("[");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_PARTLIST_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("]");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_AT_POSITION_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define dir (*__pcc_in->data.leaf.values.buf[0])
#define vice (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_add(&last_list1,last_locfrom,(int)dir,(int)vice,0);
#undef _0e
#undef _0s
#undef _0
#undef vice
#undef dir
#undef __
#undef auxil
}

static void pcc_action_FROM_POSITION_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define dir (*__pcc_in->data.leaf.values.buf[0])
#define vice (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_add(&last_list1,last_locto,(int)dir,(int)vice,0);
#undef _0e
#undef _0s
#undef _0
#undef vice
#undef dir
#undef __
#undef auxil
}

static void pcc_action_ACT_INSTEAD_OF_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "instead of";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ACT_REFUSE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "refuse";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ACT_BEFORE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "before";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ACT_PREVENT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "prevent";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ACT_PERFORM_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "perform";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ACT_AFTER_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "after";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_TRY_TO_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "try";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NAME_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_proper = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NAME_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_proper = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NAME_2(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    array_add(&last_list1,last_name,last_genre,last_plural,last_proper);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NUMBER_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_WORD_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_VARNUM_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_SETNAME_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define word (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_name(word);
#undef _0e
#undef _0s
#undef _0
#undef word
#undef __
#undef auxil
}

static void pcc_action_SETNAME2_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define word (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_name2(word);
#undef _0e
#undef _0s
#undef _0
#undef word
#undef __
#undef auxil
}

static void pcc_action_SETNAME3_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define word (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_name3(word);
#undef _0e
#undef _0s
#undef _0
#undef word
#undef __
#undef auxil
}

static void pcc_action_SETVARNUM2_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define var (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_varornum2(var);
#undef _0e
#undef _0s
#undef _0
#undef var
#undef __
#undef auxil
}

static void pcc_action_SETVARNUM3_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define var (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_varornum3(var);
#undef _0e
#undef _0s
#undef _0
#undef var
#undef __
#undef auxil
}

static void pcc_action_SETLOCFROM_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define word (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_locfrom(word);
#undef _0e
#undef _0s
#undef _0
#undef word
#undef __
#undef auxil
}

static void pcc_action_SETLOCTO_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define word (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_locto(word);
#undef _0e
#undef _0s
#undef _0
#undef word
#undef __
#undef auxil
}

static void pcc_action_SETLOC_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define word (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_loc(word);
#undef _0e
#undef _0s
#undef _0
#undef word
#undef __
#undef auxil
}

static void pcc_action_DEFNAME_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(name %s) %s\n",last_name,name);
#undef _0e
#undef _0s
#undef _0
#undef name
#undef __
#undef auxil
}

static void pcc_action_DEFNAME_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define name (*__pcc_in->data.leaf.values.buf[0])
#define syn (*__pcc_in->data.leaf.values.buf[1])
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("(dict %s) %s\n",last_name,syn);
#undef _0e
#undef _0s
#undef _0
#undef syn
#undef name
#undef __
#undef auxil
}

static void pcc_action_QUOTED_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    __ = (char*)_1;
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETMS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 0; last_plural = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETFS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 1; last_plural = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETNN_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 0; last_plural = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETMP_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 0; last_plural = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDETFP_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 1; last_plural = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDET_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_proper = 0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ARTDET_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    last_genre = 0; last_plural = 0; last_proper = 1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_YESorNOT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_YESorNOT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_TRUEorFALSE_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_TRUEorFALSE_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_THEREISorISNOT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_THEREISorISNOT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ISorISNOT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ISorISNOT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_AREorARENOT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_AREorARENOT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ISAREorISARENOT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)0;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_ISAREorISARENOT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = (char*)1;
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_EQUAL_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "=";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_LESS_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = "<";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_GREATER_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    __ = ">";
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_INDENT_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
#define _1 pcc_get_capture_string(__pcc_ctx, __pcc_in->data.leaf.capts.buf[0])
#define _1s ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.start))
#define _1e ((const size_t)(__pcc_ctx->pos + __pcc_in->data.leaf.capts.buf[0]->range.end))
    set_string(&last_indent,(char*)_1);
#undef _1e
#undef _1s
#undef _1
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_INDENT_1(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    set_string(&last_indent,NULL);
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_STOPRET_0(pcc_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const size_t)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const size_t)__pcc_in->data.leaf.capt0.range.end)
    printf("\n");
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_sentence(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_UNDERSTAND(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_WORNBY(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_HELDBY(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJECT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TOHAVE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TOBE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NOW(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SELECT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ORSTATEMENTS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SELECTMETHOD(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_IF(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_THEN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ELSE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ELSEIF(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STATEMENTS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STATEMENT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_IF_ENDING(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_CONDITIONS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_CONDITION(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TILLEOL(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TILLENDCMT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LASTTXT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STORY(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LOHI(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_INOUT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_UPDOWN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NOTVICE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_VICE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DIR(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DESCBY(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SYNON(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DESCRIPT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DESCBASE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJMALE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJFEMALE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPLURAL(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPROPER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJGENRE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJBASE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJMALES(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJFEMALES(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPLURALS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPROPERS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJGENRES(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJBASES(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJROOM(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJROOMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJOBJECT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJOBJECTS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJCONTAINER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJCONTAINERS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_FULLLIST(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LISTELS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LISTEL(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PARTLIST(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPALL(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJALL(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPALLS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJALLS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_THATCAN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_THATIS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AT_POSITION(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AT_POSITIONS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_FROM_POSITION(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_FROM_POSITIONS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_INSTEAD_OF(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_REFUSE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_BEFORE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_PREVENT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_PERFORM(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_AFTER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TRY_TO(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NAME(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NAMES(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OBLSEP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OPTSEP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMBER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_WORD(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_VARNUM(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETNAME(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETNAME2(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETNAME3(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETVARNUM2(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETVARNUM3(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETLOCFROM(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETLOCTO(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SETLOC(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DEFNAME(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTED(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETFS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETNN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETMP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETFP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDET(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDFS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDMP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDFP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTIND(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPMS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPFS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPNN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPMP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPFP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDI(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPA(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDA(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPIN(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPSU(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NOT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_YESorNOT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TRUEorFALSE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_THEREIS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_THEREISorISNOT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_IS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ISorISNOT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AREorARENOT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ISARE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ISAREorISARENOT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AND(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OR(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SPACE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTE(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_COMMA(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_COLON(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SEMICOLON(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STOP(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_QMARK(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_EQUAL(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LESS(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_GREATER(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_INDENT(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule__(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STOPRET(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_EOL(pcc_context_t *ctx);

static pcc_thunk_chunk_t *pcc_evaluate_rule_sentence(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "sentence", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 14);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INDENT, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_0, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INDENT, &chunk->thunks, NULL)) goto L0003;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_1, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '/' ||
            (ctx->buffer.buf + ctx->cur)[1] != '/'
        ) goto L0004;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TILLEOL, &chunk->thunks, &(chunk->values.buf[0]))) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INDENT, &chunk->thunks, NULL)) goto L0004;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_2, 14, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '/' ||
            (ctx->buffer.buf + ctx->cur)[1] != '*'
        ) goto L0005;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TILLENDCMT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0005;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '*' ||
            (ctx->buffer.buf + ctx->cur)[1] != '/'
        ) goto L0005;
        ctx->cur += 2;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_3, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0006;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARE, &chunk->thunks, NULL)) goto L0006;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e'
        ) goto L0006;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_4, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0007;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALLS, &chunk->thunks, NULL)) goto L0007;
            goto L0008;
        L0007:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0008:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0009;
            goto L0010;
        L0009:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0010:;
        }
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_5, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0011;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0011;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0011;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0011;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT_POSITIONS, &chunk->thunks, NULL)) goto L0011;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0012;
            goto L0013;
        L0012:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0013:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_6, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0011:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_7, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0014;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0014;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0014;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i'
            ) goto L0016;
            ctx->cur += 2;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0016;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a'
            ) goto L0016;
            ctx->cur += 2;
            goto L0015;
        L0016:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i'
            ) goto L0017;
            ctx->cur += 3;
            goto L0015;
        L0017:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0014;
        L0015:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0014;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FROM_POSITIONS, &chunk->thunks, NULL)) goto L0014;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0018;
            goto L0019;
        L0018:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0019:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_8, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0014:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_9, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0021;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0021;
            goto L0022;
        L0021:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0022:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARE, &chunk->thunks, NULL)) goto L0020;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'i'
                ) goto L0026;
                ctx->cur += 7;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_10, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0025;
            L0026:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'i'
                ) goto L0027;
                ctx->cur += 8;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0027;
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'b' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'i'
                ) goto L0027;
                ctx->cur += 6;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_11, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0025;
            L0027:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'i'
                ) goto L0028;
                ctx->cur += 8;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_12, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0025;
            L0028:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 11) < 11 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[10] != 'i'
                ) goto L0029;
                ctx->cur += 11;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_13, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0025;
            L0029:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'e'
                ) goto L0030;
                ctx->cur += 7;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_14, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0025;
            L0030:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'i'
                ) goto L0031;
                ctx->cur += 7;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_15, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0025;
            L0031:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0024;
            L0025:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0032;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALLS, &chunk->thunks, NULL)) goto L0032;
                goto L0033;
            L0032:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0033:;
            }
            goto L0023;
        L0024:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_16, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALLS, &chunk->thunks, NULL)) goto L0034;
            goto L0023;
        L0034:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0020;
        L0023:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0037;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0037;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0037;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_17, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0036;
            L0037:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0038;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0038;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0038;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_18, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0036;
            L0038:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0039;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0039;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_19, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0036;
            L0039:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0040;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0040;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_20, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0036;
            L0040:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0035;
            L0036:;
            }
            goto L0041;
        L0035:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0041:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0042;
            goto L0043;
        L0042:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0043:;
        }
        goto L0001;
    L0020:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_21, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
            goto L0046;
        L0045:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0046:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARE, &chunk->thunks, NULL)) goto L0044;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0048;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0048;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_22, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0047;
        L0048:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0049;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0049;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_23, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0047;
        L0049:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0050;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0050;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_24, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0047;
        L0050:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0051;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0051;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_25, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0047;
        L0051:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0044;
        L0047:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0052;
            goto L0053;
        L0052:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0053:;
        }
        goto L0001;
    L0044:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARE, &chunk->thunks, NULL)) goto L0054;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0056;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0056;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_26, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0055;
        L0056:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0057;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0057;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_27, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0055;
        L0057:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0058;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0058;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_28, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0055;
        L0058:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0059;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0059;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_29, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0055;
        L0059:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0054;
        L0055:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0060;
            goto L0061;
        L0060:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0061:;
        }
        goto L0001;
    L0054:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IF, &chunk->thunks, NULL)) goto L0062;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CONDITIONS, &chunk->thunks, NULL)) goto L0062;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THEN, &chunk->thunks, NULL)) goto L0062;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STATEMENT, &chunk->thunks, NULL)) goto L0062;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IF_ENDING, &chunk->thunks, NULL)) goto L0063;
            goto L0064;
        L0063:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0064:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_30, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0062:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SELECT, &chunk->thunks, NULL)) goto L0065;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ORSTATEMENTS, &chunk->thunks, NULL)) goto L0065;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SELECTMETHOD, &chunk->thunks, NULL)) goto L0065;
        goto L0001;
    L0065:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOW, &chunk->thunks, NULL)) goto L0066;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISorISNOT, &chunk->thunks, &(chunk->values.buf[1]))) goto L0066;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TRUEorFALSE, &chunk->thunks, &(chunk->values.buf[2]))) goto L0066;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0066;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0066;
        {
            const size_t p = ctx->cur;
            size_t q;
            {
                int i;
                for (i = 0;; i++) {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (
                            pcc_refill_buffer(ctx, 1) < 1 ||
                            ctx->buffer.buf[ctx->cur] != '!'
                        ) goto L0068;
                        ctx->cur++;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0067;
                    L0068:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    }
                    {
                        int u;
                        const size_t n = pcc_get_char_as_utf32(ctx, &u);
                        if (n == 0) goto L0067;
                        ctx->cur += n;
                    }
                    if (ctx->cur == p) break;
                    continue;
                L0067:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    break;
                }
            }
            q = ctx->cur;
            chunk->capts.buf[0].range.start = p;
            chunk->capts.buf[0].range.end = q;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '!'
        ) goto L0066;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0066;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_31, 14, 1);
            thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
            thunk->data.leaf.values.buf[2] = &(chunk->values.buf[2]);
            thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0066:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ACT_INSTEAD_OF, &chunk->thunks, &(chunk->values.buf[3]))) goto L0071;
            goto L0070;
        L0071:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ACT_REFUSE, &chunk->thunks, &(chunk->values.buf[3]))) goto L0072;
            goto L0070;
        L0072:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ACT_BEFORE, &chunk->thunks, &(chunk->values.buf[3]))) goto L0073;
            goto L0070;
        L0073:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ACT_PREVENT, &chunk->thunks, &(chunk->values.buf[3]))) goto L0074;
            goto L0070;
        L0074:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ACT_PERFORM, &chunk->thunks, &(chunk->values.buf[3]))) goto L0075;
            goto L0070;
        L0075:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ACT_AFTER, &chunk->thunks, &(chunk->values.buf[3]))) goto L0076;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TOHAVE, &chunk->thunks, NULL)) goto L0078;
                goto L0077;
            L0078:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TOBE, &chunk->thunks, NULL)) goto L0079;
                goto L0077;
            L0079:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0076;
            L0077:;
            }
            goto L0070;
        L0076:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TRY_TO, &chunk->thunks, &(chunk->values.buf[3]))) goto L0080;
            goto L0070;
        L0080:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0069;
        L0070:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0082;
            ctx->cur += 4;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0082;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0084;
                goto L0083;
            L0084:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0085;
                goto L0083;
            L0085:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0082;
            L0083:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0082;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_32, 14, 1);
                thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0081;
        L0082:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[4]))) goto L0086;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0088;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0089;
                    goto L0090;
                L0089:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0090:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[5]))) goto L0088;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_33, 14, 1);
                    thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                    thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                    thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0087;
            L0088:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0091;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0094;
                        goto L0093;
                    L0094:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0095;
                        goto L0093;
                    L0095:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0092;
                    L0093:;
                    }
                    goto L0096;
                L0092:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0096:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'a'
                    ) goto L0098;
                    ctx->cur += 8;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_34, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0097;
                L0098:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0099;
                    goto L0097;
                L0099:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0091;
                L0097:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0091;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0101;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0102;
                        goto L0103;
                    L0102:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0103:;
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (
                                pcc_refill_buffer(ctx, 8) < 8 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != 'a'
                            ) goto L0107;
                            ctx->cur += 8;
                            goto L0106;
                        L0107:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (
                                pcc_refill_buffer(ctx, 13) < 13 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                                (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                                (ctx->buffer.buf + ctx->cur)[12] != 'o'
                            ) goto L0108;
                            ctx->cur += 13;
                            goto L0106;
                        L0108:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0105;
                        L0106:;
                        }
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_35, 14, 1);
                            thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0104;
                    L0105:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0109;
                        goto L0104;
                    L0109:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0101;
                    L0104:;
                    }
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_36, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0100;
                L0101:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (
                            pcc_refill_buffer(ctx, 6) < 6 ||
                            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                            (ctx->buffer.buf + ctx->cur)[5] != 'o'
                        ) goto L0112;
                        ctx->cur += 6;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0112;
                        goto L0111;
                    L0112:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0113;
                        goto L0111;
                    L0113:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0110;
                    L0111:;
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0116;
                            goto L0115;
                        L0116:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0117;
                            goto L0115;
                        L0117:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0114;
                        L0115:;
                        }
                        goto L0118;
                    L0114:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0118:;
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (
                                pcc_refill_buffer(ctx, 8) < 8 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != 'a'
                            ) goto L0122;
                            ctx->cur += 8;
                            goto L0121;
                        L0122:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (
                                pcc_refill_buffer(ctx, 13) < 13 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                                (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                                (ctx->buffer.buf + ctx->cur)[12] != 'o'
                            ) goto L0123;
                            ctx->cur += 13;
                            goto L0121;
                        L0123:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0120;
                        L0121:;
                        }
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_37, 14, 1);
                            thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0119;
                    L0120:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0124;
                        goto L0119;
                    L0124:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0110;
                    L0119:;
                    }
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_38, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0100;
                L0110:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0125;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0128;
                            goto L0127;
                        L0128:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0129;
                            goto L0127;
                        L0129:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0126;
                        L0127:;
                        }
                        goto L0130;
                    L0126:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0130:;
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (
                                pcc_refill_buffer(ctx, 8) < 8 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != 'a'
                            ) goto L0134;
                            ctx->cur += 8;
                            goto L0133;
                        L0134:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (
                                pcc_refill_buffer(ctx, 13) < 13 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                                (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                                (ctx->buffer.buf + ctx->cur)[12] != 'o'
                            ) goto L0135;
                            ctx->cur += 13;
                            goto L0133;
                        L0135:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0132;
                        L0133:;
                        }
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_39, 14, 1);
                            thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0131;
                    L0132:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0136;
                        goto L0131;
                    L0136:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0125;
                    L0131:;
                    }
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_40, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0100;
                L0125:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o'
                    ) goto L0137;
                    ctx->cur += 6;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0137;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0140;
                            goto L0139;
                        L0140:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0141;
                            goto L0139;
                        L0141:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0138;
                        L0139:;
                        }
                        goto L0142;
                    L0138:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0142:;
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (
                                pcc_refill_buffer(ctx, 8) < 8 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != 'a'
                            ) goto L0146;
                            ctx->cur += 8;
                            goto L0145;
                        L0146:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (
                                pcc_refill_buffer(ctx, 13) < 13 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                                (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                                (ctx->buffer.buf + ctx->cur)[12] != 'o'
                            ) goto L0147;
                            ctx->cur += 13;
                            goto L0145;
                        L0147:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0144;
                        L0145:;
                        }
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_41, 14, 1);
                            thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0143;
                    L0144:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0148;
                        goto L0143;
                    L0148:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0137;
                    L0143:;
                    }
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_42, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0100;
                L0137:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 5) < 5 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o'
                    ) goto L0149;
                    ctx->cur += 5;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0149;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0152;
                            goto L0151;
                        L0152:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0153;
                            goto L0151;
                        L0153:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0150;
                        L0151:;
                        }
                        goto L0154;
                    L0150:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0154:;
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        {
                            const size_t p = ctx->cur;
                            const size_t n = chunk->thunks.len;
                            if (
                                pcc_refill_buffer(ctx, 8) < 8 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != 'a'
                            ) goto L0158;
                            ctx->cur += 8;
                            goto L0157;
                        L0158:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            if (
                                pcc_refill_buffer(ctx, 13) < 13 ||
                                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                                (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                                (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                                (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                                (ctx->buffer.buf + ctx->cur)[12] != 'o'
                            ) goto L0159;
                            ctx->cur += 13;
                            goto L0157;
                        L0159:;
                            ctx->cur = p;
                            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                            goto L0156;
                        L0157:;
                        }
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_43, 14, 1);
                            thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0155;
                    L0156:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0160;
                        goto L0155;
                    L0160:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0149;
                    L0155:;
                    }
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_44, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0100;
                L0149:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0091;
                L0100:;
                }
                goto L0087;
            L0091:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0161;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0164;
                        goto L0163;
                    L0164:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0165;
                        goto L0163;
                    L0165:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0162;
                    L0163:;
                    }
                    goto L0166;
                L0162:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0166:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'a'
                    ) goto L0168;
                    ctx->cur += 8;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_45, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0167;
                L0168:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0169;
                    goto L0167;
                L0169:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0161;
                L0167:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0161;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0161;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0161;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0172;
                        goto L0171;
                    L0172:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0173;
                        goto L0171;
                    L0173:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0170;
                    L0171:;
                    }
                    goto L0174;
                L0170:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0174:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (
                            pcc_refill_buffer(ctx, 8) < 8 ||
                            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                            (ctx->buffer.buf + ctx->cur)[7] != 'a'
                        ) goto L0178;
                        ctx->cur += 8;
                        goto L0177;
                    L0178:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (
                            pcc_refill_buffer(ctx, 13) < 13 ||
                            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                            (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                            (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                            (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                            (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                            (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                            (ctx->buffer.buf + ctx->cur)[12] != 'o'
                        ) goto L0179;
                        ctx->cur += 13;
                        goto L0177;
                    L0179:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0176;
                    L0177:;
                    }
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_46, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0175;
                L0176:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0180;
                    goto L0175;
                L0180:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0161;
                L0175:;
                }
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_47, 14, 1);
                    thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                    thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                    thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0087;
            L0161:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0181;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0184;
                        goto L0183;
                    L0184:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0185;
                        goto L0183;
                    L0185:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0182;
                    L0183:;
                    }
                    goto L0186;
                L0182:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0186:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'a'
                    ) goto L0188;
                    ctx->cur += 8;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_48, 14, 1);
                        thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                        thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0187;
                L0188:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0189;
                    goto L0187;
                L0189:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0181;
                L0187:;
                }
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_49, 14, 1);
                    thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                    thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0087;
            L0181:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_50, 14, 1);
                    thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                    thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
            L0087:;
            }
            goto L0081;
        L0086:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0069;
        L0081:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0191;
            goto L0192;
        L0191:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0192:;
        }
        goto L0001;
    L0069:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UNDERSTAND, &chunk->thunks, NULL)) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[7]))) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0193;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a'
            ) goto L0195;
            ctx->cur += 8;
            goto L0194;
        L0195:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 13) < 13 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                (ctx->buffer.buf + ctx->cur)[7] != '\'' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[10] != 't' ||
                (ctx->buffer.buf + ctx->cur)[11] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[12] != 'o'
            ) goto L0196;
            ctx->cur += 13;
            goto L0194;
        L0196:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0193;
        L0194:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0193;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[8]))) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0193;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (
            pcc_refill_buffer(ctx, 10) < 10 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'o'
        ) goto L0193;
        ctx->cur += 10;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0193;
        ctx->cur += 7;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0193;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[9]))) goto L0193;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0197;
            goto L0198;
        L0197:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0198:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_51, 14, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
            thunk->data.leaf.values.buf[8] = &(chunk->values.buf[8]);
            thunk->data.leaf.values.buf[9] = &(chunk->values.buf[9]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0193:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UNDERSTAND, &chunk->thunks, NULL)) goto L0199;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[7]))) goto L0199;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0199;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0199;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0199;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[6]))) goto L0199;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0199;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0199;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0199;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[8]))) goto L0199;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0200;
            goto L0201;
        L0200:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0201:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_52, 14, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
            thunk->data.leaf.values.buf[8] = &(chunk->values.buf[8]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0199:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UNDERSTAND, &chunk->thunks, NULL)) goto L0202;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[7]))) goto L0202;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0202;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0202;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[8]))) goto L0202;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0202;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (
            pcc_refill_buffer(ctx, 10) < 10 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'o'
        ) goto L0202;
        ctx->cur += 10;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0202;
        ctx->cur += 7;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0202;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[9]))) goto L0202;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0203;
            goto L0204;
        L0203:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0204:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_53, 14, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.values.buf[8] = &(chunk->values.buf[8]);
            thunk->data.leaf.values.buf[9] = &(chunk->values.buf[9]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0202:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UNDERSTAND, &chunk->thunks, NULL)) goto L0205;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[7]))) goto L0205;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0205;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0205;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0205;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0205;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0205;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[8]))) goto L0205;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0206;
            goto L0207;
        L0206:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0207:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_54, 14, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.values.buf[8] = &(chunk->values.buf[8]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0205:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UNDERSTAND, &chunk->thunks, NULL)) goto L0208;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[7]))) goto L0208;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0208;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0208;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0208;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[8]))) goto L0208;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0208;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0208;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0208;
        if (
            pcc_refill_buffer(ctx, 10) < 10 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'o'
        ) goto L0208;
        ctx->cur += 10;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0208;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0208;
        ctx->cur += 7;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0208;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[9]))) goto L0208;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0209;
            goto L0210;
        L0209:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0210:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_55, 14, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.values.buf[8] = &(chunk->values.buf[8]);
            thunk->data.leaf.values.buf[9] = &(chunk->values.buf[9]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0208:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UNDERSTAND, &chunk->thunks, NULL)) goto L0211;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[7]))) goto L0211;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0211;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0211;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0211;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[8]))) goto L0211;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0212;
            goto L0213;
        L0212:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0213:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_56, 14, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.values.buf[8] = &(chunk->values.buf[8]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0211:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0214;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'z' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'e'
            ) goto L0216;
            ctx->cur += 11;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0216;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0216;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0216;
            goto L0215;
        L0216:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'a'
                ) goto L0218;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0218;
                goto L0219;
            L0218:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0219:;
            }
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'z' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'e'
            ) goto L0217;
            ctx->cur += 11;
            goto L0215;
        L0217:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0214;
        L0215:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0214;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0214;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0220;
            goto L0221;
        L0220:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0221:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0214;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_57, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0222;
            goto L0223;
        L0222:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0223:;
        }
        goto L0001;
    L0214:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0224;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 's' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'o'
            ) goto L0226;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0226;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0226;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0226;
            goto L0225;
        L0226:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o'
                ) goto L0228;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0228;
                goto L0229;
            L0228:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0229:;
            }
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 's' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'o'
            ) goto L0227;
            ctx->cur += 7;
            goto L0225;
        L0227:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0224;
        L0225:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0224;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0224;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0230;
            goto L0231;
        L0230:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0231:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0224;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_58, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0232;
            goto L0233;
        L0232:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0233:;
        }
        goto L0001;
    L0224:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0234;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'm'
            ) goto L0236;
            ctx->cur += 7;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0236;
                if (!(
                    u == 0x00006f ||
                    u == 0x000069
                )) goto L0236;
                ctx->cur += n;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0236;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0236;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0236;
            goto L0235;
        L0236:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 3) < 3 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'o'
                    ) goto L0240;
                    ctx->cur += 3;
                    goto L0239;
                L0240:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 4) < 4 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'i'
                    ) goto L0241;
                    ctx->cur += 4;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0241;
                    goto L0239;
                L0241:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0238;
                L0239:;
                }
                goto L0242;
            L0238:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0242:;
            }
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'm'
            ) goto L0237;
            ctx->cur += 7;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0237;
                if (!(
                    u == 0x00006f ||
                    u == 0x000069
                )) goto L0237;
                ctx->cur += n;
            }
            goto L0235;
        L0237:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0234;
        L0235:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0234;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0234;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0243;
            goto L0244;
        L0243:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0244:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0234;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_59, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0245;
            goto L0246;
        L0245:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0246:;
        }
        goto L0001;
    L0234:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0247;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't'
        ) goto L0247;
        ctx->cur += 6;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e'
            ) goto L0249;
            ctx->cur += 3;
            goto L0248;
        L0249:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0250;
            ctx->cur += 4;
            goto L0248;
        L0250:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0247;
        L0248:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0247;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0247;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0247;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_60, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0251;
            goto L0252;
        L0251:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0252:;
        }
        goto L0001;
    L0247:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[5]))) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THEREIS, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0255;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0255;
            goto L0254;
        L0255:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0256;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0256;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0256;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i'
            ) goto L0256;
            ctx->cur += 2;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0256;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'a'
                ) goto L0258;
                ctx->cur += 2;
                goto L0257;
            L0258:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e'
                ) goto L0259;
                ctx->cur += 4;
                goto L0257;
            L0259:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e'
                ) goto L0260;
                ctx->cur += 6;
                goto L0257;
            L0260:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'a'
                ) goto L0261;
                ctx->cur += 5;
                goto L0257;
            L0261:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e'
                ) goto L0262;
                ctx->cur += 4;
                goto L0257;
            L0262:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0256;
            L0257:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0256;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0265;
                    goto L0266;
                L0265:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0266:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[5]))) goto L0264;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0264;
                goto L0263;
            L0264:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n'
                ) goto L0267;
                ctx->cur += 2;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0267;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LOHI, &chunk->thunks, &(chunk->values.buf[5]))) goto L0267;
                goto L0263;
            L0267:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[5]))) goto L0268;
                goto L0263;
            L0268:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[5]))) goto L0269;
                goto L0263;
            L0269:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0256;
            L0263:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 5) < 5 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o'
                    ) goto L0272;
                    ctx->cur += 5;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0272;
                    goto L0273;
                L0272:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0273:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0271;
                goto L0270;
            L0271:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0274;
                goto L0270;
            L0274:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0275;
                goto L0270;
            L0275:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0256;
            L0270:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0256;
            goto L0254;
        L0256:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0277;
                goto L0278;
            L0277:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0278:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[5]))) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0276;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0276;
            goto L0254;
        L0276:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0253;
        L0254:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOTVICE, &chunk->thunks, &(chunk->values.buf[11]))) goto L0280;
            goto L0279;
        L0280:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VICE, &chunk->thunks, &(chunk->values.buf[11]))) goto L0281;
            goto L0279;
        L0281:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0253;
        L0279:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_61, 14, 1);
            thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
            thunk->data.leaf.values.buf[11] = &(chunk->values.buf[11]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0282;
            goto L0283;
        L0282:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0283:;
        }
        goto L0001;
    L0253:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_62, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0285;
            goto L0286;
        L0285:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0286:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0284;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0284;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0290;
                ctx->cur += 8;
                goto L0289;
            L0290:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0291;
                ctx->cur += 10;
                goto L0289;
            L0291:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0288;
            L0289:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0288;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0288;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_63, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0287;
        L0288:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0294;
                ctx->cur += 8;
                goto L0293;
            L0294:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0295;
                ctx->cur += 10;
                goto L0293;
            L0295:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0292;
            L0293:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0292;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0292;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_64, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0287;
        L0292:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a'
                ) goto L0298;
                ctx->cur += 7;
                goto L0297;
            L0298:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0299;
                ctx->cur += 9;
                goto L0297;
            L0299:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0296;
            L0297:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0296;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0296;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_65, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0287;
        L0296:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e'
                ) goto L0302;
                ctx->cur += 5;
                goto L0301;
            L0302:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'o'
                ) goto L0303;
                ctx->cur += 7;
                goto L0301;
            L0303:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0300;
            L0301:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0300;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0300;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_66, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0287;
        L0300:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0284;
        L0287:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0304;
            goto L0305;
        L0304:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0305:;
        }
        goto L0001;
    L0284:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0307;
            goto L0308;
        L0307:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0308:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0306;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0306;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0312;
                ctx->cur += 8;
                goto L0311;
            L0312:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0313;
                ctx->cur += 10;
                goto L0311;
            L0313:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0310;
            L0311:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0310;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0314;
                goto L0315;
            L0314:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0315:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0310;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_67, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0309;
        L0310:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0318;
                ctx->cur += 8;
                goto L0317;
            L0318:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0319;
                ctx->cur += 10;
                goto L0317;
            L0319:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0316;
            L0317:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0316;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0320;
                goto L0321;
            L0320:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0321:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0316;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_68, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0309;
        L0316:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a'
                ) goto L0324;
                ctx->cur += 7;
                goto L0323;
            L0324:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0325;
                ctx->cur += 9;
                goto L0323;
            L0325:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0322;
            L0323:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0322;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0326;
                goto L0327;
            L0326:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0327:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0322;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_69, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0309;
        L0322:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e'
                ) goto L0330;
                ctx->cur += 5;
                goto L0329;
            L0330:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'o'
                ) goto L0331;
                ctx->cur += 7;
                goto L0329;
            L0331:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0328;
            L0329:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0328;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0332;
                goto L0333;
            L0332:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0333:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0328;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_70, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0309;
        L0328:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0306;
        L0309:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0334;
            goto L0335;
        L0334:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0335:;
        }
        goto L0001;
    L0306:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0338;
                goto L0339;
            L0338:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0339:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DEFNAME, &chunk->thunks, NULL)) goto L0337;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0340;
                goto L0341;
            L0340:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0341:;
            }
            goto L0342;
        L0337:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0342:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0336;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DESCRIPT, &chunk->thunks, NULL)) goto L0344;
            goto L0343;
        L0344:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0348;
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'a'
                    ) goto L0348;
                    ctx->cur += 6;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0348;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_71, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALL, &chunk->thunks, NULL)) goto L0349;
                        goto L0350;
                    L0349:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    L0350:;
                    }
                    goto L0351;
                L0348:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0351:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0353;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[5]))) goto L0353;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0353;
                    goto L0352;
                L0353:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[5]))) goto L0354;
                    goto L0352;
                L0354:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[5]))) goto L0355;
                    goto L0352;
                L0355:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0347;
                L0352:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0357;
                    goto L0356;
                L0357:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0358;
                    goto L0356;
                L0358:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0359;
                    goto L0356;
                L0359:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0347;
                L0356:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0347;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOTVICE, &chunk->thunks, &(chunk->values.buf[11]))) goto L0361;
                    goto L0360;
                L0361:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VICE, &chunk->thunks, &(chunk->values.buf[11]))) goto L0362;
                    goto L0360;
                L0362:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0347;
                L0360:;
                }
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_72, 14, 1);
                    thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
                    thunk->data.leaf.values.buf[11] = &(chunk->values.buf[11]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0346;
            L0347:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0364;
                    goto L0365;
                L0364:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0365:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'a'
                    ) goto L0367;
                    ctx->cur += 6;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_73, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0367:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 7) < 7 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'o'
                    ) goto L0368;
                    ctx->cur += 7;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_74, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0368:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'o'
                    ) goto L0369;
                    ctx->cur += 8;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0369;
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'b' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'e'
                    ) goto L0369;
                    ctx->cur += 6;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_75, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0369:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'o'
                    ) goto L0370;
                    ctx->cur += 8;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_76, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0370:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 11) < 11 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[9] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[10] != 'e'
                    ) goto L0371;
                    ctx->cur += 11;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_77, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0371:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 7) < 7 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'a'
                    ) goto L0372;
                    ctx->cur += 7;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_78, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0372:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 7) < 7 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'g' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'o'
                    ) goto L0373;
                    ctx->cur += 7;
                    {
                        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_79, 14, 1);
                        thunk->data.leaf.capt0.range.start = chunk->pos;
                        thunk->data.leaf.capt0.range.end = ctx->cur;
                        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                    }
                    goto L0366;
                L0373:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0363;
                L0366:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0374;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALL, &chunk->thunks, NULL)) goto L0374;
                    goto L0375;
                L0374:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0375:;
                }
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0376;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0378;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0378;
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_80, 14, 1);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0377;
                    L0378:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0379;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0379;
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_81, 14, 1);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0377;
                    L0379:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0380;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0380;
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_82, 14, 1);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0377;
                    L0380:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0381;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0381;
                        {
                            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_83, 14, 1);
                            thunk->data.leaf.capt0.range.start = chunk->pos;
                            thunk->data.leaf.capt0.range.end = ctx->cur;
                            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                        }
                        goto L0377;
                    L0381:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0376;
                    L0377:;
                    }
                    goto L0382;
                L0376:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0382:;
                }
                goto L0346;
            L0363:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0383;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0383;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_84, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0346;
            L0383:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0384;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0384;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_85, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0346;
            L0384:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0385;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0385;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_86, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0346;
            L0385:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0386;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0386;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_87, 14, 1);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0346;
            L0386:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0345;
            L0346:;
            }
            goto L0343;
        L0345:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0336;
        L0343:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0387;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DESCRIPT, &chunk->thunks, NULL)) goto L0387;
            goto L0388;
        L0387:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0388:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0389;
            goto L0390;
        L0389:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0390:;
        }
        goto L0001;
    L0336:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0393;
                if (!(
                    u == 0x000049 ||
                    u == 0x000069
                )) goto L0393;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a'
            ) goto L0393;
            ctx->cur += 9;
            goto L0392;
        L0393:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0394;
                if (!(
                    u == 0x000041 ||
                    u == 0x000061
                )) goto L0394;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a'
            ) goto L0394;
            ctx->cur += 6;
            goto L0392;
        L0394:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0391;
        L0392:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0391;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0391;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o'
        ) goto L0391;
        ctx->cur += 9;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0391;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0391;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0391;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_88, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0391:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0397;
                if (!(
                    u == 0x000049 ||
                    u == 0x000069
                )) goto L0397;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a'
            ) goto L0397;
            ctx->cur += 9;
            goto L0396;
        L0397:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0398;
                if (!(
                    u == 0x000041 ||
                    u == 0x000061
                )) goto L0398;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a'
            ) goto L0398;
            ctx->cur += 6;
            goto L0396;
        L0398:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0395;
        L0396:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0395;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0395;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o'
        ) goto L0395;
        ctx->cur += 9;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_89, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0395:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0399;
            if (!(
                u == 0x000044 ||
                u == 0x000064
            )) goto L0399;
            ctx->cur += n;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a'
            ) goto L0401;
            ctx->cur += 9;
            goto L0400;
        L0401:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 's' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'i'
            ) goto L0402;
            ctx->cur += 9;
            goto L0400;
        L0402:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0399;
        L0400:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0399;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0399;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o'
        ) goto L0399;
        ctx->cur += 9;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0399;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0399;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0399;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_90, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0399:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0403;
            if (!(
                u == 0x000044 ||
                u == 0x000064
            )) goto L0403;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[7] != 't' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'a'
        ) goto L0403;
        ctx->cur += 9;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0403;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0403;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o'
        ) goto L0403;
        ctx->cur += 9;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_91, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0403:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0404;
            if (!(
                u == 0x000063 ||
                u == 0x000043
            )) goto L0404;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'a'
        ) goto L0404;
        ctx->cur += 9;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0404;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0405;
            goto L0406;
        L0405:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0406:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0404;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0404;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 't' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0404;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0404;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0404;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0404;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_92, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CONDITIONS, &chunk->thunks, NULL)) goto L0404;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0404;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0404;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME3, &chunk->thunks, NULL)) goto L0404;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_93, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0404:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0407;
            if (!(
                u == 0x000063 ||
                u == 0x000043
            )) goto L0407;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0407;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME3, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0407;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0407;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_94, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0407:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0408;
            if (!(
                u == 0x000063 ||
                u == 0x000043
            )) goto L0408;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0408;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0408;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_95, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0408;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0408;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0408;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME3, &chunk->thunks, NULL)) goto L0408;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0408;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0408;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0408;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_96, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0408:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0409;
            if (!(
                u == 0x000063 ||
                u == 0x000043
            )) goto L0409;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0409;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0409;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0409;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0409;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0409;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_97, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0409;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0409;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0409;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0409;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_98, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0409:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0410;
            if (!(
                u == 0x000063 ||
                u == 0x000043
            )) goto L0410;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a'
        ) goto L0410;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0410;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_99, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0410;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0410;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0410;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_100, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0410;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0410;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0410;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0410;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_101, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0410:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0413;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0413;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0413;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0413;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'a'
                ) goto L0415;
                ctx->cur += 5;
                goto L0414;
            L0415:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0416;
                ctx->cur += 9;
                goto L0414;
            L0416:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0413;
            L0414:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0413;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0413;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0413;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0413;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0418;
                goto L0417;
            L0418:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
                    (ctx->buffer.buf + ctx->cur)[3] != '\xb9'
                ) goto L0419;
                ctx->cur += 4;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0419;
                goto L0417;
            L0419:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0413;
            L0417:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0413;
            goto L0412;
        L0413:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0420;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0420;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0420;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 5) < 5 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'a'
                    ) goto L0424;
                    ctx->cur += 5;
                    goto L0423;
                L0424:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o'
                    ) goto L0425;
                    ctx->cur += 9;
                    goto L0423;
                L0425:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0422;
                L0423:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0422;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0422;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0422;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0422;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0427;
                    goto L0426;
                L0427:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 4) < 4 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
                        (ctx->buffer.buf + ctx->cur)[3] != '\xb9'
                    ) goto L0428;
                    ctx->cur += 4;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0428;
                    goto L0426;
                L0428:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0422;
                L0426:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0422;
                goto L0421;
            L0422:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0429;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0429;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->cur] != '+'
                ) goto L0429;
                ctx->cur++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0429;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0429;
                goto L0421;
            L0429:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0420;
            L0421:;
            }
            goto L0412;
        L0420:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0411;
        L0412:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_102, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0411:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0432;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0432;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0432;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0432;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'f' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'f' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'z' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'a'
                ) goto L0434;
                ctx->cur += 10;
                goto L0433;
            L0434:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 11) < 11 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'z' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[10] != 'e'
                ) goto L0435;
                ctx->cur += 11;
                goto L0433;
            L0435:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0436;
                ctx->cur += 9;
                goto L0433;
            L0436:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0432;
            L0433:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0432;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0432;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0432;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0432;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0438;
                goto L0437;
            L0438:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o'
                ) goto L0439;
                ctx->cur += 4;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0439;
                goto L0437;
            L0439:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0432;
            L0437:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0432;
            goto L0431;
        L0432:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0440;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0440;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0440;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 10) < 10 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'f' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'f' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'z' ||
                        (ctx->buffer.buf + ctx->cur)[9] != 'a'
                    ) goto L0444;
                    ctx->cur += 10;
                    goto L0443;
                L0444:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 11) < 11 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'z' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[10] != 'e'
                    ) goto L0445;
                    ctx->cur += 11;
                    goto L0443;
                L0445:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o'
                    ) goto L0446;
                    ctx->cur += 9;
                    goto L0443;
                L0446:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0442;
                L0443:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0442;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0442;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0442;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0442;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0448;
                    goto L0447;
                L0448:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 4) < 4 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'o'
                    ) goto L0449;
                    ctx->cur += 4;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0449;
                    goto L0447;
                L0449:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0442;
                L0447:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0442;
                goto L0441;
            L0442:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0450;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0450;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->cur] != '-'
                ) goto L0450;
                ctx->cur++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0450;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0450;
                goto L0441;
            L0450:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0440;
            L0441:;
            }
            goto L0431;
        L0440:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0430;
        L0431:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_103, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0430:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0453;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0453;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0453;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0453;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o'
                ) goto L0455;
                ctx->cur += 8;
                goto L0454;
            L0455:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 15) < 15 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[10] != 'z' ||
                    (ctx->buffer.buf + ctx->cur)[11] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[12] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[13] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[14] != 'e'
                ) goto L0456;
                ctx->cur += 15;
                goto L0454;
            L0456:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0457;
                ctx->cur += 9;
                goto L0454;
            L0457:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0453;
            L0454:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0453;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0453;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0453;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0453;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0459;
                goto L0458;
            L0459:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'r'
                ) goto L0460;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0460;
                goto L0458;
            L0460:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0453;
            L0458:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0453;
            goto L0452;
        L0453:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0461;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0461;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0461;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 8) < 8 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'o'
                    ) goto L0465;
                    ctx->cur += 8;
                    goto L0464;
                L0465:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 15) < 15 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'c' ||
                        (ctx->buffer.buf + ctx->cur)[9] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[10] != 'z' ||
                        (ctx->buffer.buf + ctx->cur)[11] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[12] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[13] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[14] != 'e'
                    ) goto L0466;
                    ctx->cur += 15;
                    goto L0464;
                L0466:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o'
                    ) goto L0467;
                    ctx->cur += 9;
                    goto L0464;
                L0467:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0463;
                L0464:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0463;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0463;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0463;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0463;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0469;
                    goto L0468;
                L0469:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 3) < 3 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'r'
                    ) goto L0470;
                    ctx->cur += 3;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0470;
                    goto L0468;
                L0470:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0463;
                L0468:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0463;
                goto L0462;
            L0463:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0471;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0471;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->cur] != '*'
                ) goto L0471;
                ctx->cur++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0471;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0471;
                goto L0462;
            L0471:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0461;
            L0462:;
            }
            goto L0452;
        L0461:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0451;
        L0452:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_104, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0451:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0474;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0474;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0474;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0474;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'e'
                ) goto L0476;
                ctx->cur += 9;
                goto L0475;
            L0476:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'z' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'e'
                ) goto L0477;
                ctx->cur += 9;
                goto L0475;
            L0477:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0478;
                ctx->cur += 9;
                goto L0475;
            L0478:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0474;
            L0475:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0474;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0474;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0474;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0474;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0480;
                goto L0479;
            L0480:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'o'
                ) goto L0481;
                ctx->cur += 6;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0481;
                goto L0479;
            L0481:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0474;
            L0479:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0474;
            goto L0473;
        L0474:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0482;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0482;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0482;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'e'
                    ) goto L0486;
                    ctx->cur += 9;
                    goto L0485;
                L0486:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'z' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'e'
                    ) goto L0487;
                    ctx->cur += 9;
                    goto L0485;
                L0487:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o'
                    ) goto L0488;
                    ctx->cur += 9;
                    goto L0485;
                L0488:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0484;
                L0485:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0484;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0484;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0484;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0484;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0490;
                    goto L0489;
                L0490:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o'
                    ) goto L0491;
                    ctx->cur += 6;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0491;
                    goto L0489;
                L0491:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0484;
                L0489:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0484;
                goto L0483;
            L0484:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0492;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0492;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->cur] != '/'
                ) goto L0492;
                ctx->cur++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0492;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0492;
                goto L0483;
            L0492:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0482;
            L0483:;
            }
            goto L0473;
        L0482:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0472;
        L0473:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_105, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0472:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0495;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0495;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0495;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0495;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'o'
                ) goto L0497;
                ctx->cur += 6;
                goto L0496;
            L0497:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0498;
                ctx->cur += 9;
                goto L0496;
            L0498:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0495;
            L0496:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0495;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0495;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0495;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0495;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0500;
                goto L0499;
            L0500:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'o'
                ) goto L0501;
                ctx->cur += 6;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0501;
                goto L0499;
            L0501:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0495;
            L0499:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0495;
            goto L0494;
        L0495:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0502;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0502;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0502;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o'
                    ) goto L0506;
                    ctx->cur += 6;
                    goto L0505;
                L0506:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 9) < 9 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                        (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                        (ctx->buffer.buf + ctx->cur)[8] != 'o'
                    ) goto L0507;
                    ctx->cur += 9;
                    goto L0505;
                L0507:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0504;
                L0505:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0504;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0504;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0504;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0504;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0509;
                    goto L0508;
                L0509:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (
                        pcc_refill_buffer(ctx, 6) < 6 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                        (ctx->buffer.buf + ctx->cur)[5] != 'o'
                    ) goto L0510;
                    ctx->cur += 6;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0510;
                    goto L0508;
                L0510:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0504;
                L0508:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0504;
                goto L0503;
            L0504:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0511;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0511;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->cur] != '%'
                ) goto L0511;
                ctx->cur++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0511;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0511;
                goto L0503;
            L0511:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0502;
            L0503:;
            }
            goto L0494;
        L0502:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0493;
        L0494:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_106, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0493:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0514;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o'
            ) goto L0514;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0514;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0514;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0514;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 't' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a'
            ) goto L0514;
            ctx->cur += 3;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0514;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0514;
            goto L0513;
        L0514:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0515;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0515;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0515;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0516;
                goto L0517;
            L0516:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0517:;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o'
            ) goto L0515;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0515;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0515;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0515;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 't' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a'
            ) goto L0515;
            ctx->cur += 3;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0515;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0515;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0515;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0515;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0515;
            goto L0513;
        L0515:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0512;
        L0513:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_107, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0512:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 's' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i'
        ) goto L0518;
        ctx->cur += 8;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_108, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0518:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o'
        ) goto L0519;
        ctx->cur += 9;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_109, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0519:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't'
        ) goto L0520;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_110, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0520:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 10) < 10 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'a'
        ) goto L0521;
        ctx->cur += 10;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_111, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0521:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a'
        ) goto L0522;
        ctx->cur += 5;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0522;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0522;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_112, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0522:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'a'
        ) goto L0523;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_113, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0523:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0524;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_114, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0524:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0525;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0525;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r'
        ) goto L0525;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_115, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0525:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0526;
            if (!(
                u == 0x000049 ||
                u == 0x000069
            )) goto L0526;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 11) < 11 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'z' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[10] != 'e'
        ) goto L0526;
        ctx->cur += 11;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0527;
            goto L0528;
        L0527:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0528:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_116, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0526:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0529;
            if (!(
                u == 0x000049 ||
                u == 0x000069
            )) goto L0529;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 11) < 11 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'z' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[10] != 'e'
        ) goto L0529;
        ctx->cur += 11;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0530;
            goto L0531;
        L0530:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0531:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_117, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0529:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0532;
            if (!(
                u == 0x000045 ||
                u == 0x000065
            )) goto L0532;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a'
        ) goto L0532;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0532;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0532;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0532;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0533;
            goto L0534;
        L0533:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0534:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_118, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0532:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0536;
            goto L0537;
        L0536:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0537:;
        }
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0535;
            if (!(
                u == 0x000050 ||
                u == 0x000070
            )) goto L0535;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'o'
        ) goto L0535;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0535;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0538;
            goto L0539;
        L0538:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0539:;
        }
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'b' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[7] != 't' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'o'
        ) goto L0535;
        ctx->cur += 9;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_119, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0535:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0541;
            goto L0542;
        L0541:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0542:;
        }
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0540;
            if (!(
                u == 0x000050 ||
                u == 0x000070
            )) goto L0540;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'o'
        ) goto L0540;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0540;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0543;
            goto L0544;
        L0543:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0544:;
        }
        if (
            pcc_refill_buffer(ctx, 12) < 12 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'b' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[8] != 't' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[10] != 't' ||
            (ctx->buffer.buf + ctx->cur)[11] != 'o'
        ) goto L0540;
        ctx->cur += 12;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_120, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0540:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0546;
            goto L0547;
        L0546:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0547:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0548;
                if (!(
                    u == 0x00004d ||
                    u == 0x00006d
                )) goto L0548;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 's' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o'
            ) goto L0548;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0548;
            goto L0549;
        L0548:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0549:;
        }
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0545;
            if (!(
                u == 0x000050 ||
                u == 0x000070
            )) goto L0545;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'o'
        ) goto L0545;
        ctx->cur += 8;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0545;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0550;
                if (!(
                    u == 0x00004d ||
                    u == 0x00006d
                )) goto L0550;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 's' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o'
            ) goto L0550;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0550;
            goto L0551;
        L0550:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0551:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0552;
            goto L0553;
        L0552:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0553:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0554;
            goto L0555;
        L0554:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0555:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMBER, &chunk->thunks, &(chunk->values.buf[12]))) goto L0545;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_121, 14, 1);
            thunk->data.leaf.values.buf[12] = &(chunk->values.buf[12]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0545:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CONDITIONS, &chunk->thunks, NULL)) goto L0556;
        goto L0001;
    L0556:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0559;
                goto L0560;
            L0559:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0560:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0561;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0561;
                goto L0562;
            L0561:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0562:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0558;
            goto L0563;
        L0558:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0563:;
        }
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't'
        ) goto L0557;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0557;
            if (!(
                u == 0x00006f ||
                u == 0x000061 ||
                u == 0x000069 ||
                u == 0x000065
            )) goto L0557;
            ctx->cur += n;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0557;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'o'
        ) goto L0557;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0557;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's'
        ) goto L0557;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0557;
            if (!(
                u == 0x00006f ||
                u == 0x000061 ||
                u == 0x000069 ||
                u == 0x000065
            )) goto L0557;
            ctx->cur += n;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QMARK, &chunk->thunks, NULL)) goto L0557;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_122, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0557:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0566;
                goto L0567;
            L0566:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0567:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0568;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0568;
                goto L0569;
            L0568:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0569:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0565;
            goto L0570;
        L0565:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0570:;
        }
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's'
        ) goto L0564;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0564;
            if (!(
                u == 0x00006f ||
                u == 0x000061 ||
                u == 0x000069 ||
                u == 0x000065
            )) goto L0564;
            ctx->cur += n;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0564;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'o'
        ) goto L0564;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0564;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't'
        ) goto L0564;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0564;
            if (!(
                u == 0x00006f ||
                u == 0x000061 ||
                u == 0x000069 ||
                u == 0x000065
            )) goto L0564;
            ctx->cur += n;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QMARK, &chunk->thunks, NULL)) goto L0564;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_123, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0564:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0573;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0573;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i'
            ) goto L0573;
            ctx->cur += 2;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0573;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a'
            ) goto L0573;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0573;
            goto L0572;
        L0573:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0574;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o'
                ) goto L0575;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0575;
                goto L0576;
            L0575:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0576:;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 't' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o'
            ) goto L0574;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0574;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0577;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0577;
                goto L0578;
            L0577:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0578:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0574;
            goto L0572;
        L0574:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0580;
                goto L0581;
            L0580:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0581:;
            }
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0579;
                if (!(
                    u == 0x000054 ||
                    u == 0x000074
                )) goto L0579;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o'
            ) goto L0579;
            ctx->cur += 5;
            goto L0572;
        L0579:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0571;
        L0572:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0582;
            goto L0583;
        L0582:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0583:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0585;
            goto L0584;
        L0585:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LASTTXT, &chunk->thunks, &(chunk->values.buf[10]))) goto L0586;
            goto L0584;
        L0586:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0571;
        L0584:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_124, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0587;
            goto L0588;
        L0587:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0588:;
        }
        goto L0001;
    L0571:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0591;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o'
                ) goto L0592;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0592;
                goto L0593;
            L0592:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0593:;
            }
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 't' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 't' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'o'
            ) goto L0591;
            ctx->cur += 10;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0591;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0594;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0594;
                goto L0595;
            L0594:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0595:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0591;
            goto L0590;
        L0591:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0597;
                goto L0598;
            L0597:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0598:;
            }
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0596;
                if (!(
                    u == 0x000054 ||
                    u == 0x000074
                )) goto L0596;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'o'
            ) goto L0596;
            ctx->cur += 9;
            goto L0590;
        L0596:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0589;
        L0590:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0599;
            goto L0600;
        L0599:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0600:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0602;
            goto L0601;
        L0602:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LASTTXT, &chunk->thunks, &(chunk->values.buf[10]))) goto L0603;
            goto L0601;
        L0603:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0589;
        L0601:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_125, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0604;
            goto L0605;
        L0604:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0605:;
        }
        goto L0001;
    L0589:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0608;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o'
                ) goto L0609;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0609;
                goto L0610;
            L0609:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0610:;
            }
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'o'
            ) goto L0608;
            ctx->cur += 11;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0608;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0611;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0611;
                goto L0612;
            L0611:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0612:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0608;
            goto L0607;
        L0608:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0614;
                goto L0615;
            L0614:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0615:;
            }
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0613;
                if (!(
                    u == 0x000053 ||
                    u == 0x000073
                )) goto L0613;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'o'
            ) goto L0613;
            ctx->cur += 10;
            goto L0607;
        L0613:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0606;
        L0607:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0616;
            goto L0617;
        L0616:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0617:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0619;
            goto L0618;
        L0619:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LASTTXT, &chunk->thunks, &(chunk->values.buf[10]))) goto L0620;
            goto L0618;
        L0620:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0606;
        L0618:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_126, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0621;
            goto L0622;
        L0621:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0622:;
        }
        goto L0001;
    L0606:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0625;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o'
                ) goto L0626;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0626;
                goto L0627;
            L0626:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0627:;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0625;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0625;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0628;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0628;
                goto L0629;
            L0628:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0629:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0625;
            goto L0624;
        L0625:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0631;
                goto L0632;
            L0631:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0632:;
            }
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0630;
                if (!(
                    u == 0x000041 ||
                    u == 0x000061
                )) goto L0630;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e'
            ) goto L0630;
            ctx->cur += 5;
            goto L0624;
        L0630:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0623;
        L0624:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0633;
            goto L0634;
        L0633:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0634:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0636;
            goto L0635;
        L0636:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LASTTXT, &chunk->thunks, &(chunk->values.buf[10]))) goto L0637;
            goto L0635;
        L0637:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0623;
        L0635:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_127, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0638;
            goto L0639;
        L0638:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0639:;
        }
        goto L0001;
    L0623:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0642;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'o'
                ) goto L0643;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0643;
                goto L0644;
            L0643:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0644:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'f' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'd'
                ) goto L0646;
                ctx->cur += 4;
                goto L0645;
            L0646:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'I' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'F' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'I' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'D'
                ) goto L0647;
                ctx->cur += 4;
                goto L0645;
            L0647:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0642;
            L0645:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0642;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0648;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0648;
                goto L0649;
            L0648:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0649:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0642;
            goto L0641;
        L0642:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0651;
                goto L0652;
            L0651:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0652:;
            }
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0650;
                if (!(
                    u == 0x000049 ||
                    u == 0x000069
                )) goto L0650;
                ctx->cur += n;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd'
                ) goto L0654;
                ctx->cur += 3;
                goto L0653;
            L0654:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'F' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'I' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'D'
                ) goto L0655;
                ctx->cur += 3;
                goto L0653;
            L0655:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0650;
            L0653:;
            }
            goto L0641;
        L0650:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0640;
        L0641:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0656;
            goto L0657;
        L0656:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0657:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0659;
            goto L0658;
        L0659:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LASTTXT, &chunk->thunks, &(chunk->values.buf[10]))) goto L0660;
            goto L0658;
        L0660:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0640;
        L0658:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_128, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0661;
            goto L0662;
        L0661:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0662:;
        }
        goto L0001;
    L0640:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0665;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'a'
                ) goto L0666;
                ctx->cur += 3;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0666;
                goto L0667;
            L0666:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0667:;
            }
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0665;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0665;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0668;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STORY, &chunk->thunks, NULL)) goto L0668;
                goto L0669;
            L0668:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0669:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0665;
            goto L0664;
        L0665:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0671;
                goto L0672;
            L0671:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0672:;
            }
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0670;
                if (!(
                    u == 0x000052 ||
                    u == 0x000072
                )) goto L0670;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0670;
            ctx->cur += 6;
            goto L0664;
        L0670:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0663;
        L0664:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0673;
            goto L0674;
        L0673:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0674:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0676;
            goto L0675;
        L0676:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LASTTXT, &chunk->thunks, &(chunk->values.buf[10]))) goto L0677;
            goto L0675;
        L0677:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0663;
        L0675:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_129, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0678;
            goto L0679;
        L0678:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0679:;
        }
        goto L0001;
    L0663:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0682;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0682;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_130, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0682:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0683;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0683;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_131, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0683:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0684;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0684;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_132, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0684:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0685;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0685;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_133, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0685:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0686;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0686;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_134, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0686:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0687;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0687;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_135, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0687:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0688;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0688;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_136, 14, 1);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0681;
        L0688:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0680;
        L0681:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0689;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJECT, &chunk->thunks, &(chunk->values.buf[13]))) goto L0689;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_137, 14, 1);
                thunk->data.leaf.values.buf[13] = &(chunk->values.buf[13]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0690;
        L0689:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0690:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0680;
        goto L0001;
    L0680:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0691;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_138, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0691;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_sentence, &chunk->thunks, NULL)) goto L0691;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0692;
            goto L0693;
        L0692:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0693:;
        }
        goto L0001;
    L0691:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0694;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_139, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '.'
        ) goto L0694;
        ctx->cur++;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_140, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0694:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[10]))) goto L0695;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_141, 14, 1);
            thunk->data.leaf.values.buf[10] = &(chunk->values.buf[10]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0695:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0696;
        goto L0001;
    L0696:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SEMICOLON, &chunk->thunks, NULL)) goto L0697;
        goto L0001;
    L0697:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COLON, &chunk->thunks, NULL)) goto L0698;
        goto L0001;
    L0698:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '.'
        ) goto L0699;
        ctx->cur++;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_142, 14, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0699:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TILLEOL, &chunk->thunks, &(chunk->values.buf[0]))) goto L0700;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0700;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_sentence_143, 14, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0700:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "sentence", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "sentence", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_UNDERSTAND(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "UNDERSTAND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000049 ||
            u == 0x000069
        )) goto L0000;
        ctx->cur += n;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i'
        ) goto L0002;
        ctx->cur += 6;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[7] != 't' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'a'
        ) goto L0003;
        ctx->cur += 9;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "UNDERSTAND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "UNDERSTAND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_WORNBY(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "WORNBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 8) < 8 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[4] != 's' ||
        (ctx->buffer.buf + ctx->cur)[5] != 's' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[7] != 't'
    ) goto L0000;
    ctx->cur += 8;
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x00006f ||
            u == 0x000061 ||
            u == 0x000065 ||
            u == 0x000069
        )) goto L0000;
        ctx->cur += n;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "WORNBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "WORNBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_HELDBY(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "HELDBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 4) < 4 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[2] != 't' ||
        (ctx->buffer.buf + ctx->cur)[3] != 't'
    ) goto L0000;
    ctx->cur += 4;
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x00006f ||
            u == 0x000061 ||
            u == 0x000065 ||
            u == 0x000069
        )) goto L0000;
        ctx->cur += n;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "HELDBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "HELDBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJECT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't'
            ) goto L0002;
            ctx->cur += 5;
            goto L0001;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's'
            ) goto L0003;
            ctx->cur += 5;
            goto L0001;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's'
            ) goto L0004;
            ctx->cur += 5;
            goto L0001;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't'
            ) goto L0005;
            ctx->cur += 5;
            goto L0001;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0000;
        L0001:;
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x00006f ||
            u == 0x000061 ||
            u == 0x000069 ||
            u == 0x000065
        )) goto L0000;
        ctx->cur += n;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJECT_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TOHAVE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "TOHAVE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e'
        ) goto L0002;
        ctx->cur += 5;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r'
        ) goto L0003;
        ctx->cur += 4;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "TOHAVE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "TOHAVE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TOBE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "TOBE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[1] != 's' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e'
        ) goto L0002;
        ctx->cur += 6;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[1] != 's' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 's' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i'
        ) goto L0003;
        ctx->cur += 7;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[1] != 's' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r'
        ) goto L0004;
        ctx->cur += 5;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "TOBE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "TOBE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NOW(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "NOW", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0002;
            if (!(
                u == 0x00004f ||
                u == 0x00006f
            )) goto L0002;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0003;
            if (!(
                u == 0x000041 ||
                u == 0x000061
            )) goto L0003;
            ctx->cur += n;
        }
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o'
        ) goto L0003;
        ctx->cur += 5;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "NOW", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "NOW", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SELECT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SELECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000073 ||
            u == 0x000053
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 8) < 8 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'z' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[7] != 'a'
    ) goto L0000;
    ctx->cur += 8;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECT_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SELECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SELECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ORSTATEMENTS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ORSTATEMENTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STATEMENTS, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OR, &chunk->thunks, NULL)) goto L0001;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ORSTATEMENTS_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ORSTATEMENTS, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ORSTATEMENTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ORSTATEMENTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SELECTMETHOD(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SELECTMETHOD", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->cur] != 'a'
            ) goto L0004;
            ctx->cur++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o'
            ) goto L0004;
            ctx->cur += 4;
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[9] != 't' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'e'
            ) goto L0005;
            ctx->cur += 11;
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECTMETHOD_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 10) < 10 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[8] != 't' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'i'
        ) goto L0006;
        ctx->cur += 10;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECTMETHOD_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 12) < 12 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[10] != 't' ||
            (ctx->buffer.buf + ctx->cur)[11] != 'e'
        ) goto L0007;
        ctx->cur += 12;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECTMETHOD_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0007:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 13) < 13 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[10] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[11] != 't' ||
            (ctx->buffer.buf + ctx->cur)[12] != 'e'
        ) goto L0008;
        ctx->cur += 13;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0008;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'a'
        ) goto L0008;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0008;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0008;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECTMETHOD_3, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0008:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i'
        ) goto L0009;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0009;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->cur] != 'a'
            ) goto L0011;
            ctx->cur++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0011;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o'
            ) goto L0011;
            ctx->cur += 4;
            goto L0010;
        L0011:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[9] != 't' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'e'
            ) goto L0012;
            ctx->cur += 11;
            goto L0010;
        L0012:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0009;
        L0010:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECTMETHOD_4, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0009:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i'
        ) goto L0013;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0013;
        if (
            pcc_refill_buffer(ctx, 13) < 13 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[9] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[10] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[11] != 't' ||
            (ctx->buffer.buf + ctx->cur)[12] != 'e'
        ) goto L0013;
        ctx->cur += 13;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0013;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'a'
        ) goto L0013;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0013;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0013;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SELECTMETHOD_5, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0013:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SELECTMETHOD", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SELECTMETHOD", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_IF(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "IF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'S' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_IF_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "IF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "IF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_THEN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "THEN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 6) < 6 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'a'
    ) goto L0000;
    ctx->cur += 6;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_THEN_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "THEN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "THEN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ELSE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ELSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 10) < 10 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 't' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'm' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[8] != 't' ||
        (ctx->buffer.buf + ctx->cur)[9] != 'i'
    ) goto L0000;
    ctx->cur += 10;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ELSE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ELSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ELSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ELSEIF(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ELSEIF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 10) < 10 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 't' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'm' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[8] != 't' ||
        (ctx->buffer.buf + ctx->cur)[9] != 'i'
    ) goto L0000;
    ctx->cur += 10;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'e'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ELSEIF_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ELSEIF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ELSEIF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STATEMENTS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "STATEMENTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STATEMENT, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SEMICOLON, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STATEMENTS, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "STATEMENTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "STATEMENTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STATEMENT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "STATEMENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_sentence, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "STATEMENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "STATEMENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_IF_ENDING(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "IF_ENDING", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ELSEIF, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CONDITIONS, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THEN, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ELSE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STATEMENTS, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "IF_ENDING", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "IF_ENDING", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_CONDITIONS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "CONDITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITIONS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CONDITION, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e'
            ) goto L0003;
            ctx->cur += 2;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
            goto L0002;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0004;
            goto L0002;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SEMICOLON, &chunk->thunks, NULL)) goto L0005;
            goto L0002;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OR, &chunk->thunks, NULL)) goto L0006;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITIONS_1, 0, 0);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0002;
        L0006:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0001;
        L0002:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CONDITIONS, &chunk->thunks, NULL)) goto L0001;
        goto L0007;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0007:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITIONS_2, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "CONDITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "CONDITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_CONDITION(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "CONDITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 8);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_0, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AREorARENOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_1, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0004;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_2, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i'
            ) goto L0005;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0005;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_3, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i'
            ) goto L0006;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_4, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0006:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'i'
            ) goto L0007;
            ctx->cur += 11;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0007;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_5, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0007:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0008;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0008;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_6, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALLS, &chunk->thunks, NULL)) goto L0009;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_7, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0009:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0010;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0010;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_8, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0010:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0011;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0011;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_9, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0011:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0012;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0012;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_10, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0012:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0013;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0013;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_11, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0003;
        L0013:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISorISNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0014;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TRUEorFALSE, &chunk->thunks, &(chunk->values.buf[1]))) goto L0014;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0014;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0014;
        {
            const size_t p = ctx->cur;
            size_t q;
            {
                int i;
                for (i = 0;; i++) {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        const size_t p = ctx->cur;
                        const size_t n = chunk->thunks.len;
                        if (
                            pcc_refill_buffer(ctx, 1) < 1 ||
                            ctx->buffer.buf[ctx->cur] != '?'
                        ) goto L0016;
                        ctx->cur++;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0015;
                    L0016:;
                        ctx->cur = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    }
                    {
                        int u;
                        const size_t n = pcc_get_char_as_utf32(ctx, &u);
                        if (n == 0) goto L0015;
                        ctx->cur += n;
                    }
                    if (ctx->cur == p) break;
                    continue;
                L0015:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    break;
                }
            }
            q = ctx->cur;
            chunk->capts.buf[0].range.start = p;
            chunk->capts.buf[0].range.end = q;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '?'
        ) goto L0014;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0014;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_12, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
            thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0014:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0018;
            goto L0019;
        L0018:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0019:;
        }
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'b' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o'
        ) goto L0017;
        ctx->cur += 5;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0017;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0017;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISorISNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0017;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[2]))) goto L0017;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_13, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.values.buf[2] = &(chunk->values.buf[2]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0017:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0021;
            goto L0022;
        L0021:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0022:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0020;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0020;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISorISNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0020;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0020;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0020;
        ctx->cur += 7;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_14, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0020:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0023;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0024;
            ctx->cur += 7;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0024;
            goto L0025;
        L0024:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0025:;
        }
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'a'
        ) goto L0023;
        ctx->cur += 6;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0023;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'e'
                ) goto L0028;
                ctx->cur += 7;
                goto L0027;
            L0028:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0029;
                ctx->cur += 8;
                goto L0027;
            L0029:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0026;
            L0027:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0026;
            goto L0030;
        L0026:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0030:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISAREorISARENOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0023;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0031;
            goto L0032;
        L0031:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0032:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0023;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_15, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0023:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0033;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't'
        ) goto L0033;
        ctx->cur += 6;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e'
            ) goto L0035;
            ctx->cur += 3;
            goto L0034;
        L0035:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0036;
            ctx->cur += 4;
            goto L0034;
        L0036:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0033;
        L0034:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0033;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_YESorNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0033;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
        ) goto L0033;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0033;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e'
        ) goto L0033;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_16, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0033:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0037;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't'
        ) goto L0037;
        ctx->cur += 6;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e'
            ) goto L0039;
            ctx->cur += 3;
            goto L0038;
        L0039:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0040;
            ctx->cur += 4;
            goto L0038;
        L0040:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0037;
        L0038:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0037;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISAREorISARENOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0037;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0041;
            goto L0042;
        L0041:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0042:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0037;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_17, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0037:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[3]))) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THEREISorISNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0045;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0045;
            goto L0044;
        L0045:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0046;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0046;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0046;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_YESorNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0046;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i'
            ) goto L0046;
            ctx->cur += 2;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0046;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'a'
                ) goto L0048;
                ctx->cur += 2;
                goto L0047;
            L0048:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e'
                ) goto L0049;
                ctx->cur += 4;
                goto L0047;
            L0049:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e'
                ) goto L0050;
                ctx->cur += 6;
                goto L0047;
            L0050:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'a'
                ) goto L0051;
                ctx->cur += 5;
                goto L0047;
            L0051:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e'
                ) goto L0052;
                ctx->cur += 4;
                goto L0047;
            L0052:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0046;
            L0047:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0046;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0055;
                    goto L0056;
                L0055:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0056:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[3]))) goto L0054;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0054;
                goto L0053;
            L0054:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n'
                ) goto L0057;
                ctx->cur += 2;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0057;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LOHI, &chunk->thunks, &(chunk->values.buf[3]))) goto L0057;
                goto L0053;
            L0057:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[3]))) goto L0058;
                goto L0053;
            L0058:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[3]))) goto L0059;
                goto L0053;
            L0059:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0046;
            L0053:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 5) < 5 ||
                        (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                        (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                        (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                        (ctx->buffer.buf + ctx->cur)[4] != 'o'
                    ) goto L0062;
                    ctx->cur += 5;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0062;
                    goto L0063;
                L0062:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                L0063:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0061;
                goto L0060;
            L0061:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0064;
                goto L0060;
            L0064:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0065;
                goto L0060;
            L0065:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0046;
            L0060:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0046;
            goto L0044;
        L0046:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0067;
                goto L0068;
            L0067:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0068:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISAREorISARENOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[3]))) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0066;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0066;
            goto L0044;
        L0066:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0043;
        L0044:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_18, 8, 1);
            thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0043:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_19, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0070;
            goto L0071;
        L0070:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0071:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0069;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0069;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_YESorNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0069;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0075;
                ctx->cur += 8;
                goto L0074;
            L0075:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0076;
                ctx->cur += 10;
                goto L0074;
            L0076:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0073;
            L0074:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0073;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0073;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_20, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0072;
        L0073:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0079;
                ctx->cur += 8;
                goto L0078;
            L0079:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0080;
                ctx->cur += 10;
                goto L0078;
            L0080:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0077;
            L0078:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0077;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0077;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_21, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0072;
        L0077:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a'
                ) goto L0083;
                ctx->cur += 7;
                goto L0082;
            L0083:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0084;
                ctx->cur += 9;
                goto L0082;
            L0084:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0081;
            L0082:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0081;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0081;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_22, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0072;
        L0081:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e'
                ) goto L0087;
                ctx->cur += 5;
                goto L0086;
            L0087:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'o'
                ) goto L0088;
                ctx->cur += 7;
                goto L0086;
            L0088:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0085;
            L0086:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0085;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0085;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_23, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0072;
        L0085:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0069;
        L0072:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0089;
            goto L0090;
        L0089:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0090:;
        }
        goto L0001;
    L0069:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0092;
            goto L0093;
        L0092:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0093:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0091;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0091;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_YESorNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0091;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_24, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0097;
                ctx->cur += 8;
                goto L0096;
            L0097:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0098;
                ctx->cur += 10;
                goto L0096;
            L0098:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0095;
            L0096:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0095;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0099;
                goto L0100;
            L0099:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0100:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0095;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_25, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0094;
        L0095:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0103;
                ctx->cur += 8;
                goto L0102;
            L0103:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 10) < 10 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'o'
                ) goto L0104;
                ctx->cur += 10;
                goto L0102;
            L0104:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0101;
            L0102:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0101;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0105;
                goto L0106;
            L0105:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0106:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0101;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_26, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0094;
        L0101:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a'
                ) goto L0109;
                ctx->cur += 7;
                goto L0108;
            L0109:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o'
                ) goto L0110;
                ctx->cur += 9;
                goto L0108;
            L0110:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0107;
            L0108:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0107;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0111;
                goto L0112;
            L0111:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0112:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0107;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_27, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0094;
        L0107:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e'
                ) goto L0115;
                ctx->cur += 5;
                goto L0114;
            L0115:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'o'
                ) goto L0116;
                ctx->cur += 7;
                goto L0114;
            L0116:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0113;
            L0114:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0113;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0117;
                goto L0118;
            L0117:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0118:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0113;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_28, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0094;
        L0113:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0091;
        L0094:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0119;
            goto L0120;
        L0119:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0120:;
        }
        goto L0001;
    L0091:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0123;
                goto L0124;
            L0123:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0124:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0122;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0122;
            goto L0125;
        L0122:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0125:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISorISNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0121;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_29, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0129;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[3]))) goto L0129;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0129;
                goto L0128;
            L0129:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[3]))) goto L0130;
                goto L0128;
            L0130:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[3]))) goto L0131;
                goto L0128;
            L0131:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0127;
            L0128:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0133;
                goto L0132;
            L0133:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0134;
                goto L0132;
            L0134:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0135;
                goto L0132;
            L0135:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0127;
            L0132:;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0127;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_30, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.values.buf[3] = &(chunk->values.buf[3]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0126;
        L0127:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0136;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0136;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0137;
                goto L0138;
            L0137:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0138:;
            }
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_31, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0126;
        L0136:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0139;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0139;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_32, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0126;
        L0139:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORNBY, &chunk->thunks, NULL)) goto L0140;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0140;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_33, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0126;
        L0140:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HELDBY, &chunk->thunks, NULL)) goto L0141;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOC, &chunk->thunks, NULL)) goto L0141;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_34, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0126;
        L0141:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0143;
                goto L0144;
            L0143:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0144:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'z' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'a'
                ) goto L0146;
                ctx->cur += 6;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_35, 8, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0145;
            L0146:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'o'
                ) goto L0147;
                ctx->cur += 7;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_36, 8, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0145;
            L0147:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'o'
                ) goto L0148;
                ctx->cur += 8;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_37, 8, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0145;
            L0148:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 11) < 11 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 't' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[9] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[10] != 'e'
                ) goto L0149;
                ctx->cur += 11;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_38, 8, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0145;
            L0149:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 7) < 7 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'a'
                ) goto L0150;
                ctx->cur += 7;
                {
                    pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_39, 8, 1);
                    thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                    thunk->data.leaf.capt0.range.start = chunk->pos;
                    thunk->data.leaf.capt0.range.end = ctx->cur;
                    pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
                }
                goto L0145;
            L0150:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALL, &chunk->thunks, NULL)) goto L0151;
                goto L0145;
            L0151:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0142;
            L0145:;
            }
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0152;
                goto L0153;
            L0152:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            L0153:;
            }
            goto L0126;
        L0142:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0121;
        L0126:;
        }
        goto L0001;
    L0121:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0155;
            goto L0156;
        L0155:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0156:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0154;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0154;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0154;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0154;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0157;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0157;
            goto L0158;
        L0157:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0158:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0154;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0154;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_40, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0154:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0160;
            goto L0161;
        L0160:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0161:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0159;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0159;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0159;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0159;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0162;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0162;
            goto L0163;
        L0162:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0163:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0159;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_41, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0159;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_42, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0159:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMBER, &chunk->thunks, &(chunk->values.buf[4]))) goto L0164;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0164;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0164;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0164;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0165;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0165;
            goto L0166;
        L0165:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0166:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0164;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0164;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_43, 8, 1);
            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0164:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMBER, &chunk->thunks, &(chunk->values.buf[4]))) goto L0167;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0167;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0167;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0167;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0168;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0168;
            goto L0169;
        L0168:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0169:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0167;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_44, 8, 1);
            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0167;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_45, 8, 1);
            thunk->data.leaf.values.buf[4] = &(chunk->values.buf[4]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0167:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[5]))) goto L0170;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0170;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0170;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0171;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0171;
            goto L0172;
        L0171:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0172:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0170;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0170;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_46, 8, 1);
            thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0170:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[5]))) goto L0173;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0173;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0173;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0174;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0174;
            goto L0175;
        L0174:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0175:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0173;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_47, 8, 1);
            thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0173;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_48, 8, 1);
            thunk->data.leaf.values.buf[5] = &(chunk->values.buf[5]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0173:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_49, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0176;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0176;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0176;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0176;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0177;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0177;
            goto L0178;
        L0177:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0178:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0176;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME2, &chunk->thunks, NULL)) goto L0176;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_50, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0176:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_51, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0179;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0179;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0179;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0179;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o'
            ) goto L0180;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0180;
            goto L0181;
        L0180:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0181:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0179;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_52, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0179;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_53, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0179:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0183;
            goto L0184;
        L0183:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0184:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM2, &chunk->thunks, NULL)) goto L0182;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, &(chunk->values.buf[6]))) goto L0186;
            goto L0185;
        L0186:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LESS, &chunk->thunks, &(chunk->values.buf[6]))) goto L0187;
            goto L0185;
        L0187:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_GREATER, &chunk->thunks, &(chunk->values.buf[6]))) goto L0188;
            goto L0185;
        L0188:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0182;
        L0185:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETVARNUM3, &chunk->thunks, NULL)) goto L0182;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_54, 8, 1);
            thunk->data.leaf.values.buf[6] = &(chunk->values.buf[6]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0189;
            goto L0190;
        L0189:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0190:;
        }
        goto L0001;
    L0182:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0192;
            goto L0193;
        L0192:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0193:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0191;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0191;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_55, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, &(chunk->values.buf[7]))) goto L0191;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_56, 8, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0194;
            goto L0195;
        L0194:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0195:;
        }
        goto L0001;
    L0191:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_57, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, &(chunk->values.buf[7]))) goto L0196;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0196;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0196;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_58, 8, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0197;
            goto L0198;
        L0197:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0198:;
        }
        goto L0001;
    L0196:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0200;
            goto L0201;
        L0200:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0201:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0199;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0199;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_59, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PARTLIST, &chunk->thunks, &(chunk->values.buf[7]))) goto L0199;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_60, 8, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0202;
            goto L0203;
        L0202:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0203:;
        }
        goto L0001;
    L0199:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_61, 8, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PARTLIST, &chunk->thunks, &(chunk->values.buf[7]))) goto L0204;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQUAL, &chunk->thunks, NULL)) goto L0204;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0204;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_62, 8, 1);
            thunk->data.leaf.values.buf[7] = &(chunk->values.buf[7]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOPRET, &chunk->thunks, NULL)) goto L0205;
            goto L0206;
        L0205:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0206:;
        }
        goto L0001;
    L0204:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0208;
            goto L0209;
        L0208:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0209:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0207;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0207;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISorISNOT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0207;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_63, 8, 1);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTIND, &chunk->thunks, NULL)) goto L0210;
            goto L0211;
        L0210:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0211:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'o'
            ) goto L0213;
            ctx->cur += 6;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_64, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0213:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a'
            ) goto L0214;
            ctx->cur += 6;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0214;
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[9] != 't' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'a'
            ) goto L0214;
            ctx->cur += 11;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_65, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0214:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a'
            ) goto L0215;
            ctx->cur += 6;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_66, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0215:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a'
            ) goto L0216;
            ctx->cur += 5;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0216;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n'
            ) goto L0216;
            ctx->cur += 3;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0216;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a'
            ) goto L0216;
            ctx->cur += 5;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_67, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0216:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a'
            ) goto L0217;
            ctx->cur += 5;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0217;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a'
            ) goto L0217;
            ctx->cur += 5;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_68, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0217:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a'
            ) goto L0218;
            ctx->cur += 5;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_69, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0218:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't'
            ) goto L0219;
            ctx->cur += 8;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0219;
                if (!(
                    u == 0x00006f ||
                    u == 0x000061
                )) goto L0219;
                ctx->cur += n;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0219;
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 't' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[8] != 't' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'e'
            ) goto L0219;
            ctx->cur += 10;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_70, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0219:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[7] != 't'
            ) goto L0220;
            ctx->cur += 8;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0220;
                if (!(
                    u == 0x00006f ||
                    u == 0x000061
                )) goto L0220;
                ctx->cur += n;
            }
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_71, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0220:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'o'
            ) goto L0221;
            ctx->cur += 7;
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_72, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0221:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't'
            ) goto L0222;
            ctx->cur += 4;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0222;
                if (!(
                    u == 0x00006f ||
                    u == 0x000061
                )) goto L0222;
                ctx->cur += n;
            }
            {
                pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_CONDITION_73, 8, 1);
                thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
                thunk->data.leaf.capt0.range.start = chunk->pos;
                thunk->data.leaf.capt0.range.end = ctx->cur;
                pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
            }
            goto L0212;
        L0222:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0207;
        L0212:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0207;
        goto L0001;
    L0207:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "CONDITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "CONDITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TILLEOL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "TILLEOL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOL, &chunk->thunks, NULL)) goto L0002;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0001;
                L0002:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                }
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_TILLEOL_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "TILLEOL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TILLENDCMT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "TILLENDCMT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    if (
                        pcc_refill_buffer(ctx, 2) < 2 ||
                        (ctx->buffer.buf + ctx->cur)[0] != '*' ||
                        (ctx->buffer.buf + ctx->cur)[1] != '/'
                    ) goto L0002;
                    ctx->cur += 2;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0001;
                L0002:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                }
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_TILLENDCMT_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "TILLENDCMT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LASTTXT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "LASTTXT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (
                        u == 0x00002f ||
                        u == 0x00002e ||
                        u == 0x00000d ||
                        u == 0x00000a
                    ) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LASTTXT_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "LASTTXT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STORY(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "STORY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 't' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'a'
        ) goto L0002;
        ctx->cur += 6;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'a'
        ) goto L0003;
        ctx->cur += 9;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "STORY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "STORY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LOHI(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "LOHI", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0002;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LOHI_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'b' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o'
        ) goto L0003;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LOHI_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "LOHI", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "LOHI", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_INOUT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "INOUT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o'
        ) goto L0002;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_INOUT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i'
        ) goto L0003;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_INOUT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "INOUT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "INOUT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_UPDOWN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "UPDOWN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a'
        ) goto L0002;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_UPDOWN_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o'
        ) goto L0003;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_UPDOWN_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "UPDOWN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "UPDOWN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NOTVICE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "NOTVICE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0002;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0002;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[7] != 's' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'a'
        ) goto L0002;
        ctx->cur += 9;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'z' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'a'
        ) goto L0003;
        ctx->cur += 5;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0003;
        ctx->cur += 7;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NOTVICE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "NOTVICE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "NOTVICE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_VICE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "VICE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0001;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->cur] != 'e'
            ) goto L0003;
            ctx->cur++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[7] != 's' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a'
            ) goto L0003;
            ctx->cur += 9;
            goto L0002;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n'
            ) goto L0004;
            ctx->cur += 3;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'o'
            ) goto L0004;
            ctx->cur += 7;
            goto L0002;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0001;
        L0002:;
        }
        goto L0005;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0005:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_VICE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "VICE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DIR(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "DIR", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[5] != 's' ||
            (ctx->buffer.buf + ctx->cur)[6] != 't'
        ) goto L0002;
        ctx->cur += 7;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[7] != 's' ||
            (ctx->buffer.buf + ctx->cur)[8] != 't'
        ) goto L0003;
        ctx->cur += 9;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'd'
        ) goto L0004;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[3] != '\xb9'
        ) goto L0005;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_3, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 't' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o'
        ) goto L0006;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_4, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i'
        ) goto L0007;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_5, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0007:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's' ||
            (ctx->buffer.buf + ctx->cur)[5] != 't'
        ) goto L0008;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_6, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0008:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[6] != 's' ||
            (ctx->buffer.buf + ctx->cur)[7] != 't'
        ) goto L0009;
        ctx->cur += 8;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_7, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0009:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'd'
        ) goto L0010;
        ctx->cur += 3;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_8, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0010:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u'
        ) goto L0011;
        ctx->cur += 2;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_9, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0011:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[1] != 's' ||
            (ctx->buffer.buf + ctx->cur)[2] != 't'
        ) goto L0012;
        ctx->cur += 3;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_10, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0012:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't'
        ) goto L0013;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DIR_11, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0013:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "DIR", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "DIR", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DESCBY(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "DESCBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0001;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    if (
        pcc_refill_buffer(ctx, 8) < 8 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[2] != 's' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[6] != 't' ||
        (ctx->buffer.buf + ctx->cur)[7] != 't'
    ) goto L0000;
    ctx->cur += 8;
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x00006f ||
            u == 0x000061 ||
            u == 0x000069 ||
            u == 0x000065
        )) goto L0000;
        ctx->cur += n;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0004;
        ctx->cur += 4;
        goto L0003;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0005;
        ctx->cur += 2;
        goto L0003;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0003:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DESCBY_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "DESCBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "DESCBY", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SYNON(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SYNON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'n'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 7) < 7 ||
        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'm'
    ) goto L0000;
    ctx->cur += 7;
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x00006f ||
            u == 0x000069
        )) goto L0000;
        ctx->cur += n;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SYNON_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SYNON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SYNON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DESCRIPT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "DESCRIPT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DESCBASE, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPTSEP, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DESCRIPT, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "DESCRIPT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "DESCRIPT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DESCBASE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "DESCBASE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DESCBY, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SYNON, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "DESCBASE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "DESCBASE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJMALE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJMALE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'e'
        ) goto L0002;
        ctx->cur += 8;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0003;
        ctx->cur += 7;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJMALE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJMALE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJFEMALE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJFEMALE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'e'
        ) goto L0002;
        ctx->cur += 9;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'a'
        ) goto L0003;
        ctx->cur += 7;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJFEMALE_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJFEMALE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJFEMALE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPLURAL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJPLURAL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 7) < 7 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'u' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'e'
    ) goto L0000;
    ctx->cur += 7;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJPLURAL_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJPLURAL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJPLURAL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPROPER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJPROPER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 7) < 7 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'o'
    ) goto L0000;
    ctx->cur += 7;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJPROPER_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJPROPER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJPROPER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJGENRE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJGENRE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJMALE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJFEMALE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJGENRE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJGENRE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJBASE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJBASE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJGENRE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJPLURAL, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJPROPER, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJBASE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJBASE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJMALES(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJMALES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'i'
        ) goto L0002;
        ctx->cur += 8;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i'
        ) goto L0003;
        ctx->cur += 6;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJMALES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJMALES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJFEMALES(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJFEMALES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 9) < 9 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[8] != 'i'
        ) goto L0002;
        ctx->cur += 9;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'e'
        ) goto L0003;
        ctx->cur += 7;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJFEMALES_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJFEMALES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJFEMALES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPLURALS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJPLURALS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 7) < 7 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'u' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'i'
    ) goto L0000;
    ctx->cur += 7;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJPLURALS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJPLURALS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJPLURALS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJPROPERS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJPROPERS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 6) < 6 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'i'
    ) goto L0000;
    ctx->cur += 6;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJPROPERS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJPROPERS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJPROPERS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJGENRES(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJGENRES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJMALES, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJFEMALES, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJGENRES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJGENRES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJBASES(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJBASES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJGENRES, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJPLURALS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJPROPERS, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJBASES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJBASES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJROOM(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJROOM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'b' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'i'
    ) goto L0000;
    ctx->cur += 3;
    {
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    u == 0x00006f ||
                    u == 0x000061 ||
                    u == 0x000065
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            break;
        }
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJROOM_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJROOM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJROOM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJROOMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJROOMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'b' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'i'
    ) goto L0000;
    ctx->cur += 3;
    {
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    u == 0x000065
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            break;
        }
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJROOMS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJROOMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJROOMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJOBJECT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJOBJECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 12) < 12 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[11] != 'e'
            ) goto L0004;
            ctx->cur += 12;
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0005;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e'
            ) goto L0005;
            ctx->cur += 8;
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a'
            ) goto L0008;
            ctx->cur += 5;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0008;
                if (!(
                    u == 0x000074 ||
                    u == 0x000062
                )) goto L0008;
                ctx->cur += n;
            }
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e'
            ) goto L0008;
            ctx->cur += 3;
            goto L0007;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0009;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0009;
            ctx->cur += 7;
            goto L0007;
        L0009:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0006;
        L0007:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0010;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0010;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a'
            ) goto L0012;
            ctx->cur += 8;
            goto L0011;
        L0012:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e'
            ) goto L0013;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0013;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0013;
            ctx->cur += 4;
            goto L0011;
        L0013:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0010;
        L0011:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0010;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e'
        ) goto L0010;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0010;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's'
        ) goto L0010;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0010;
            if (!(
                u == 0x00006f ||
                u == 0x000061
            )) goto L0010;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0010:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0014;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0014;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a'
            ) goto L0016;
            ctx->cur += 8;
            goto L0015;
        L0016:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e'
            ) goto L0017;
            ctx->cur += 8;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0017;
            ctx->cur += 4;
            goto L0015;
        L0017:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0014;
        L0015:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_3, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0014:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'e'
            ) goto L0020;
            ctx->cur += 10;
            goto L0019;
        L0020:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 13) < 13 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[11] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[12] != 'e'
            ) goto L0021;
            ctx->cur += 13;
            goto L0019;
        L0021:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0022;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'e'
                ) goto L0024;
                ctx->cur += 9;
                goto L0023;
            L0024:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0025;
                ctx->cur += 8;
                goto L0023;
            L0025:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0022;
            L0023:;
            }
            goto L0019;
        L0022:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0018;
        L0019:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_4, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0018:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o'
        ) goto L0026;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_5, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0026:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o'
        ) goto L0027;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_6, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0027:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e'
        ) goto L0028;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0028;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0028;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0028;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0028;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0028;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'e'
        ) goto L0028;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_7, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0028:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0031;
            ctx->cur += 4;
            goto L0030;
        L0031:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0032;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0032;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i'
                ) goto L0034;
                ctx->cur += 3;
                goto L0033;
            L0034:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'e'
                ) goto L0035;
                ctx->cur += 5;
                goto L0033;
            L0035:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0032;
            L0033:;
            }
            goto L0030;
        L0032:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0029;
        L0030:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0029;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0029;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0029;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0029;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0029;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e'
            ) goto L0037;
            ctx->cur += 5;
            goto L0036;
        L0037:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
                (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
            ) goto L0038;
            ctx->cur += 4;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0038;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0038;
            ctx->cur += 6;
            goto L0036;
        L0038:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0029;
        L0036:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_8, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0029:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0039;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0039;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i'
            ) goto L0041;
            ctx->cur += 3;
            goto L0040;
        L0041:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e'
            ) goto L0042;
            ctx->cur += 5;
            goto L0040;
        L0042:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0039;
        L0040:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0039;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0039;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0039;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0039;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0039;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
        ) goto L0039;
        ctx->cur += 4;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0039;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e'
            ) goto L0044;
            ctx->cur += 5;
            goto L0043;
        L0044:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
                (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
            ) goto L0045;
            ctx->cur += 4;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0045;
            ctx->cur += 6;
            goto L0043;
        L0045:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0039;
        L0043:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_9, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0039:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATIS, &chunk->thunks, NULL)) goto L0047;
            goto L0048;
        L0047:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0048:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[8] != 't'
            ) goto L0050;
            ctx->cur += 9;
            goto L0049;
        L0050:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 's'
            ) goto L0051;
            ctx->cur += 4;
            goto L0049;
        L0051:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't'
            ) goto L0052;
            ctx->cur += 7;
            goto L0049;
        L0052:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0046;
        L0049:;
        }
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0046;
            if (!(
                u == 0x00006f ||
                u == 0x000061
            )) goto L0046;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_10, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0046:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'e'
            ) goto L0055;
            ctx->cur += 11;
            goto L0054;
        L0055:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0056;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'e'
            ) goto L0056;
            ctx->cur += 9;
            goto L0054;
        L0056:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0053;
        L0054:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECT_11, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0053:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJOBJECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJOBJECT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJOBJECTS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJOBJECTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 12) < 12 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[11] != 'i'
            ) goto L0004;
            ctx->cur += 12;
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0005;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e'
            ) goto L0005;
            ctx->cur += 8;
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'i'
            ) goto L0008;
            ctx->cur += 9;
            goto L0007;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0009;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 't' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'e'
            ) goto L0009;
            ctx->cur += 7;
            goto L0007;
        L0009:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0006;
        L0007:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATIS, &chunk->thunks, NULL)) goto L0011;
            goto L0012;
        L0011:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0012:;
        }
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'g' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[8] != 't'
            ) goto L0014;
            ctx->cur += 9;
            goto L0013;
        L0014:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 's'
            ) goto L0015;
            ctx->cur += 4;
            goto L0013;
        L0015:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[6] != 't'
            ) goto L0016;
            ctx->cur += 7;
            goto L0013;
        L0016:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0010;
        L0013:;
        }
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0010;
            if (!(
                u == 0x000069 ||
                u == 0x000065
            )) goto L0010;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0010:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0017;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'o'
            ) goto L0019;
            ctx->cur += 10;
            goto L0018;
        L0019:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'o'
            ) goto L0020;
            ctx->cur += 10;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0020;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0020;
            ctx->cur += 4;
            goto L0018;
        L0020:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0017;
        L0018:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e'
        ) goto L0017;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0017;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's'
        ) goto L0017;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0017;
            if (!(
                u == 0x000069 ||
                u == 0x000065
            )) goto L0017;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_3, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0017:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e'
        ) goto L0021;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0021;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'o'
            ) goto L0023;
            ctx->cur += 10;
            goto L0022;
        L0023:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'o'
            ) goto L0024;
            ctx->cur += 10;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0024;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0024;
            ctx->cur += 4;
            goto L0022;
        L0024:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0021;
        L0022:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_4, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0021:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 't' ||
                (ctx->buffer.buf + ctx->cur)[2] != 't' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'i'
            ) goto L0027;
            ctx->cur += 10;
            goto L0026;
        L0027:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 13) < 13 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 's' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[4] != 't' ||
                (ctx->buffer.buf + ctx->cur)[5] != 't' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[11] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[12] != 'i'
            ) goto L0028;
            ctx->cur += 13;
            goto L0026;
        L0028:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0029;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 9) < 9 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[8] != 'e'
                ) goto L0031;
                ctx->cur += 9;
                goto L0030;
            L0031:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0032;
                ctx->cur += 8;
                goto L0030;
            L0032:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0029;
            L0030:;
            }
            goto L0026;
        L0029:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0025;
        L0026:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_5, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0025:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's'
        ) goto L0033;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0033;
            if (!(
                u == 0x000069 ||
                u == 0x000065
            )) goto L0033;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_6, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0033:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't'
        ) goto L0034;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0034;
            if (!(
                u == 0x000069 ||
                u == 0x000065
            )) goto L0034;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_7, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0034:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e'
            ) goto L0037;
            ctx->cur += 4;
            goto L0036;
        L0037:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPSU, &chunk->thunks, NULL)) goto L0038;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i'
                ) goto L0040;
                ctx->cur += 3;
                goto L0039;
            L0040:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'i'
                ) goto L0041;
                ctx->cur += 5;
                goto L0039;
            L0041:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0038;
            L0039:;
            }
            goto L0036;
        L0038:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0035;
        L0036:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0035;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0035;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0035;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0035;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0035;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e'
            ) goto L0043;
            ctx->cur += 5;
            goto L0042;
        L0043:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
                (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
            ) goto L0044;
            ctx->cur += 4;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0044;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0044;
            ctx->cur += 6;
            goto L0042;
        L0044:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0035;
        L0042:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_8, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0035:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPIN, &chunk->thunks, NULL)) goto L0045;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i'
            ) goto L0047;
            ctx->cur += 3;
            goto L0046;
        L0047:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'q' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'i'
            ) goto L0048;
            ctx->cur += 5;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0048;
            goto L0046;
        L0048:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0045;
        L0046:;
        }
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0045;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i'
        ) goto L0045;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0045;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'e'
            ) goto L0050;
            ctx->cur += 5;
            goto L0049;
        L0050:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
                (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
            ) goto L0051;
            ctx->cur += 4;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0051;
            if (
                pcc_refill_buffer(ctx, 6) < 6 ||
                (ctx->buffer.buf + ctx->cur)[0] != 's' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'e'
            ) goto L0051;
            ctx->cur += 6;
            goto L0049;
        L0051:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0045;
        L0049:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_9, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0045:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 11) < 11 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[10] != 'i'
            ) goto L0054;
            ctx->cur += 11;
            goto L0053;
        L0054:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0055;
            if (
                pcc_refill_buffer(ctx, 9) < 9 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
                (ctx->buffer.buf + ctx->cur)[4] != 's' ||
                (ctx->buffer.buf + ctx->cur)[5] != 's' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'e'
            ) goto L0055;
            ctx->cur += 9;
            goto L0053;
        L0055:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0052;
        L0053:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJOBJECTS_10, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0052:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJOBJECTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJOBJECTS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJCONTAINER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJCONTAINER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o'
        ) goto L0002;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINER_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o'
        ) goto L0003;
        ctx->cur += 6;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINER_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'e'
            ) goto L0006;
            ctx->cur += 8;
            goto L0005;
        L0006:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'e'
            ) goto L0007;
            ctx->cur += 10;
            goto L0005;
        L0007:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0008;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e'
                ) goto L0010;
                ctx->cur += 6;
                goto L0009;
            L0010:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0011;
                ctx->cur += 8;
                goto L0009;
            L0011:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0008;
            L0009:;
            }
            goto L0005;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0004;
        L0005:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINER_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJCONTAINER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJCONTAINER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJCONTAINERS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJCONTAINERS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[4] != 't'
        ) goto L0002;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0002;
            if (!(
                u == 0x000069 ||
                u == 0x000065
            )) goto L0002;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINERS_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[4] != 's'
        ) goto L0003;
        ctx->cur += 5;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0003;
            if (!(
                u == 0x000069 ||
                u == 0x000065
            )) goto L0003;
            ctx->cur += n;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINERS_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINERS_2, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 8) < 8 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i'
            ) goto L0006;
            ctx->cur += 8;
            goto L0005;
        L0006:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 10) < 10 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[6] != 'b' ||
                (ctx->buffer.buf + ctx->cur)[7] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[8] != 'l' ||
                (ctx->buffer.buf + ctx->cur)[9] != 'i'
            ) goto L0007;
            ctx->cur += 10;
            goto L0005;
        L0007:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THATCAN, &chunk->thunks, NULL)) goto L0008;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 6) < 6 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e'
                ) goto L0010;
                ctx->cur += 6;
                goto L0009;
            L0010:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 8) < 8 ||
                    (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
                    (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
                    (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
                    (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
                    (ctx->buffer.buf + ctx->cur)[4] != 'd' ||
                    (ctx->buffer.buf + ctx->cur)[5] != 'e' ||
                    (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
                    (ctx->buffer.buf + ctx->cur)[7] != 'e'
                ) goto L0011;
                ctx->cur += 8;
                goto L0009;
            L0011:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0008;
            L0009:;
            }
            goto L0005;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0004;
        L0005:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ADJCONTAINERS_3, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJCONTAINERS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJCONTAINERS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_FULLLIST(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "FULLLIST", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != '['
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_FULLLIST_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LISTELS, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != ']'
    ) goto L0000;
    ctx->cur++;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_FULLLIST_1, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "FULLLIST", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "FULLLIST", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LISTELS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "LISTELS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LISTEL, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0001;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LISTELS_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LISTELS, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "LISTELS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "LISTELS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LISTEL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "LISTEL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 2);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '$'
        ) goto L0002;
        ctx->cur++;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LISTEL_0, 2, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0003;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LISTEL_1, 2, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[0]))) goto L0004;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LISTEL_2, 2, 0);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMBER, &chunk->thunks, &(chunk->values.buf[1]))) goto L0005;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LISTEL_3, 2, 0);
            thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FULLLIST, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "LISTEL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "LISTEL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PARTLIST(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PARTLIST", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != '['
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_PARTLIST_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LISTELS, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != '|'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LISTELS, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != ']'
    ) goto L0000;
    ctx->cur++;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_PARTLIST_1, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PARTLIST", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PARTLIST", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPALL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PROPALL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJALL, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPTSEP, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALL, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PROPALL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PROPALL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJALL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJALL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJBASE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJOBJECT, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJCONTAINER, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJROOM, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJALL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJALL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PROPALLS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PROPALLS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJALLS, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPTSEP, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PROPALLS, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PROPALLS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PROPALLS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ADJALLS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ADJALLS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJBASES, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJOBJECTS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJCONTAINERS, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ADJROOMS, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ADJALLS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ADJALLS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_THATCAN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "THATCAN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'e'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[3] != '\xb2'
        ) goto L0002;
        ctx->cur += 4;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 's' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'o'
        ) goto L0003;
        ctx->cur += 7;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "THATCAN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "THATCAN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_THATIS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "THATIS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'e'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\xa8'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0003;
        ctx->cur += 4;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 4) < 4 ||
        (ctx->buffer.buf + ctx->cur)[0] != 's' ||
        (ctx->buffer.buf + ctx->cur)[1] != 't' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'a' ||
        (ctx->buffer.buf + ctx->cur)[3] != 't'
    ) goto L0000;
    ctx->cur += 4;
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000061 ||
            u == 0x000065 ||
            u == 0x000069 ||
            u == 0x00006f
        )) goto L0000;
        ctx->cur += n;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "THATIS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "THATIS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AT_POSITION(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "AT_POSITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 2);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[0]))) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[0]))) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0006;
        goto L0005;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0007;
        goto L0005;
    L0007:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0008;
        goto L0005;
    L0008:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0005:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCFROM, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOTVICE, &chunk->thunks, &(chunk->values.buf[1]))) goto L0010;
        goto L0009;
    L0010:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VICE, &chunk->thunks, &(chunk->values.buf[1]))) goto L0011;
        goto L0009;
    L0011:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0009:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_AT_POSITION_0, 2, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "AT_POSITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "AT_POSITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AT_POSITIONS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "AT_POSITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT_POSITION, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OBLSEP, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT_POSITIONS, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "AT_POSITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "AT_POSITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_FROM_POSITION(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "FROM_POSITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 2);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0003;
            goto L0004;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0004:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIR, &chunk->thunks, &(chunk->values.buf[0]))) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n'
        ) goto L0005;
        ctx->cur += 2;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LOHI, &chunk->thunks, &(chunk->values.buf[0]))) goto L0005;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_UPDOWN, &chunk->thunks, &(chunk->values.buf[0]))) goto L0006;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INOUT, &chunk->thunks, &(chunk->values.buf[0]))) goto L0007;
        goto L0001;
    L0007:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
                (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
                (ctx->buffer.buf + ctx->cur)[3] != 's' ||
                (ctx->buffer.buf + ctx->cur)[4] != 'o'
            ) goto L0010;
            ctx->cur += 5;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0010;
            goto L0011;
        L0010:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0011:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0009;
        goto L0008;
    L0009:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0012;
        goto L0008;
    L0012:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDA, &chunk->thunks, NULL)) goto L0013;
        goto L0008;
    L0013:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0008:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETLOCTO, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOTVICE, &chunk->thunks, &(chunk->values.buf[1]))) goto L0015;
        goto L0014;
    L0015:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VICE, &chunk->thunks, &(chunk->values.buf[1]))) goto L0016;
        goto L0014;
    L0016:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0014:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_FROM_POSITION_0, 2, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "FROM_POSITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "FROM_POSITION", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_FROM_POSITIONS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "FROM_POSITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FROM_POSITION, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OBLSEP, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FROM_POSITIONS, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "FROM_POSITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "FROM_POSITIONS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_INSTEAD_OF(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ACT_INSTEAD_OF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000049 ||
            u == 0x000069
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 5) < 5 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'v' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'c' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'e'
    ) goto L0000;
    ctx->cur += 5;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ACT_INSTEAD_OF_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ACT_INSTEAD_OF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ACT_INSTEAD_OF", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_REFUSE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ACT_REFUSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000052 ||
            u == 0x000072
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 6) < 6 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'f' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
        (ctx->buffer.buf + ctx->cur)[4] != 't' ||
        (ctx->buffer.buf + ctx->cur)[5] != 'a'
    ) goto L0000;
    ctx->cur += 6;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ACT_REFUSE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ACT_REFUSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ACT_REFUSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_BEFORE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ACT_BEFORE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000050 ||
            u == 0x000070
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 4) < 4 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'm' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'a'
    ) goto L0000;
    ctx->cur += 4;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ACT_BEFORE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ACT_BEFORE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ACT_BEFORE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_PREVENT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ACT_PREVENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000049 ||
            u == 0x000069
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 8) < 8 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
        (ctx->buffer.buf + ctx->cur)[5] != 's' ||
        (ctx->buffer.buf + ctx->cur)[6] != 'c' ||
        (ctx->buffer.buf + ctx->cur)[7] != 'i'
    ) goto L0000;
    ctx->cur += 8;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'i'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ACT_PREVENT_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ACT_PREVENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ACT_PREVENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_PERFORM(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ACT_PERFORM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000050 ||
            u == 0x000070
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 2) < 2 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'r'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ACT_PERFORM_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ACT_PERFORM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ACT_PERFORM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ACT_AFTER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ACT_AFTER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000044 ||
            u == 0x000064
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'o'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ACT_AFTER_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ACT_AFTER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ACT_AFTER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TRY_TO(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "TRY_TO", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            u == 0x000050 ||
            u == 0x000070
        )) goto L0000;
        ctx->cur += n;
    }
    if (
        pcc_refill_buffer(ctx, 4) < 4 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'r' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'v' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'a'
    ) goto L0000;
    ctx->cur += 4;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AT, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_TRY_TO_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "TRY_TO", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "TRY_TO", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NAME(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "NAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDET, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NAME_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NAME_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NAME_2, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "NAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "NAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NAMES(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "NAMES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAME, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OBLSEP, &chunk->thunks, NULL)) goto L0001;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAMES, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "NAMES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "NAMES", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OBLSEP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "OBLSEP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COMMA, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AND, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "OBLSEP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "OBLSEP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OPTSEP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "OPTSEP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OBLSEP, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "OPTSEP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "OPTSEP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMBER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "NUMBER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->cur] != '0'
            ) goto L0002;
            ctx->cur++;
            goto L0001;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0003;
                if (!(
                    (u >= 0x000031 && u <= 0x000039)
                )) goto L0003;
                ctx->cur += n;
            }
            {
                int i;
                for (i = 0;; i++) {
                    const size_t p = ctx->cur;
                    const size_t n = chunk->thunks.len;
                    {
                        int u;
                        const size_t n = pcc_get_char_as_utf32(ctx, &u);
                        if (n == 0) goto L0004;
                        if (!(
                            (u >= 0x000030 && u <= 0x000039)
                        )) goto L0004;
                        ctx->cur += n;
                    }
                    if (ctx->cur == p) break;
                    continue;
                L0004:;
                    ctx->cur = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    break;
                }
            }
            goto L0001;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0000;
        L0001:;
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NUMBER_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "NUMBER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "NUMBER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_WORD(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "WORD", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p0 = ctx->cur;
            const size_t n0 = chunk->thunks.len;
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (!(
                        (u >= 0x000041 && u <= 0x00005a) ||
                        (u >= 0x000061 && u <= 0x00007a) ||
                        u == 0x0000e0 ||
                        u == 0x0000e8 ||
                        u == 0x0000e9 ||
                        u == 0x0000ec ||
                        u == 0x0000f2 ||
                        u == 0x0000f9 ||
                        u == 0x00005f
                    )) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
            if (i < 1) {
                ctx->cur = p0;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n0);
                goto L0000;
            }
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_WORD_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "WORD", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "WORD", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_VARNUM(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "VARNUM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p0 = ctx->cur;
            const size_t n0 = chunk->thunks.len;
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (!(
                        (u >= 0x000041 && u <= 0x00005a) ||
                        (u >= 0x000030 && u <= 0x000039) ||
                        u == 0x00005f
                    )) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
            if (i < 1) {
                ctx->cur = p0;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n0);
                goto L0000;
            }
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_VARNUM_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "VARNUM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "VARNUM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETNAME(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETNAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETNAME_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETNAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETNAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETNAME2(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETNAME2", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETNAME2_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETNAME2", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETNAME2", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETNAME3(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETNAME3", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETNAME3_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETNAME3", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETNAME3", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETVARNUM2(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETVARNUM2", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VARNUM, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETVARNUM2_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETVARNUM2", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETVARNUM2", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETVARNUM3(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETVARNUM3", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VARNUM, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETVARNUM3_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETVARNUM3", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETVARNUM3", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETLOCFROM(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETLOCFROM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETLOCFROM_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETLOCFROM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETLOCFROM", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETLOCTO(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETLOCTO", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETLOCTO_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETLOCTO", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETLOCTO", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SETLOC(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SETLOC", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 1);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_WORD, &chunk->thunks, &(chunk->values.buf[0]))) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_SETLOC_0, 1, 0);
        thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SETLOC", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SETLOC", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DEFNAME(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "DEFNAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 2);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SETNAME, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COMMA, &chunk->thunks, NULL)) goto L0001;
        goto L0002;
    L0001:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'h' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[6] != 't'
        ) goto L0003;
        ctx->cur += 7;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0003;
            if (!(
                u == 0x00006f ||
                u == 0x000061 ||
                u == 0x000069 ||
                u == 0x000065
            )) goto L0003;
            ctx->cur += n;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[0]))) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DEFNAME_0, 2, 0);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0004;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0004:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0005;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0005;
        if (
            pcc_refill_buffer(ctx, 7) < 7 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'm'
        ) goto L0005;
        ctx->cur += 7;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0005;
            if (!(
                u == 0x00006f ||
                u == 0x000069
            )) goto L0005;
            ctx->cur += n;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTED, &chunk->thunks, &(chunk->values.buf[1]))) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0005;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_DEFNAME_1, 2, 0);
            thunk->data.leaf.values.buf[0] = &(chunk->values.buf[0]);
            thunk->data.leaf.values.buf[1] = &(chunk->values.buf[1]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0006;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0006:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_COMMA, &chunk->thunks, NULL)) goto L0007;
        goto L0008;
    L0007:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0008:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "DEFNAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "DEFNAME", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTED(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "QUOTED", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTE, &chunk->thunks, NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.len;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (
                        u == 0x000022
                    ) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.buf[0].range.start = p;
        chunk->capts.buf[0].range.end = q;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_QUOTE, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_QUOTED_0, 0, 1);
        thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "QUOTED", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "QUOTED", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTDETMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'I' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l'
        ) goto L0004;
        ctx->cur += 2;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'L' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o'
        ) goto L0005;
        ctx->cur += 2;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETMS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTDETMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTDETMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETFS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTDETFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'L' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETFS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTDETFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTDETFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETNN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTDETNN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\''
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'L' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\''
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETNN_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTDETNN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTDETNN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETMP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTDETMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'i'
        ) goto L0003;
        ctx->cur++;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'G' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i'
        ) goto L0004;
        ctx->cur += 3;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'I'
        ) goto L0005;
        ctx->cur++;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETMP_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTDETMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTDETMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDETFP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTDETFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'L' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDETFP_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTDETFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTDETFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTDET(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTDET", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETMS, &chunk->thunks, NULL)) goto L0004;
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETFS, &chunk->thunks, NULL)) goto L0005;
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETNN, &chunk->thunks, NULL)) goto L0006;
            goto L0003;
        L0006:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETMP, &chunk->thunks, NULL)) goto L0007;
            goto L0003;
        L0007:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTDETFP, &chunk->thunks, NULL)) goto L0008;
            goto L0003;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDET_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ARTDET_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTDET", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTINDMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTINDMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTINDMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDFS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTINDFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'a'
        ) goto L0002;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\''
        ) goto L0003;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTINDFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTINDFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDMP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTINDMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i'
        ) goto L0003;
        ctx->cur += 5;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTINDMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTINDMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTINDFP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTINDFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 5) < 5 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[3] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[4] != 'e'
    ) goto L0000;
    ctx->cur += 5;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTINDFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTINDFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARTIND(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARTIND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTINDMS, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTINDFS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTINDMP, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARTINDFP, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARTIND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARTIND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPMS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'o'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'l'
        ) goto L0003;
        ctx->cur++;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPMS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPFS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'a'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPFS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPNN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPNN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != '\''
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPNN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPNN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPMP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'i'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'i'
        ) goto L0003;
        ctx->cur++;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPMP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPFP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 3) < 3 ||
        (ctx->buffer.buf + ctx->cur)[0] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[1] != 'l' ||
        (ctx->buffer.buf + ctx->cur)[2] != 'e'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPFP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPMS, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPFS, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPNN, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPMP, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPFP, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDI(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPDI", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e'
            ) goto L0004;
            ctx->cur += 2;
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'D' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e'
            ) goto L0005;
            ctx->cur += 2;
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i'
            ) goto L0008;
            ctx->cur += 2;
            goto L0007;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'D' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'i'
            ) goto L0009;
            ctx->cur += 2;
            goto L0007;
        L0009:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0006;
        L0007:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\''
        ) goto L0010;
        ctx->cur += 2;
        goto L0001;
    L0010:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPDI", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPDI", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPA(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'a'
        ) goto L0002;
        ctx->cur++;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'A'
        ) goto L0003;
        ctx->cur++;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0004;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0004:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPDA(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPDA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'd' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'D' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0004;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0004:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPDA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPDA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPIN(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPIN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e'
            ) goto L0004;
            ctx->cur += 2;
            goto L0003;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'N' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'e'
            ) goto L0005;
            ctx->cur += 2;
            goto L0003;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'i' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n'
            ) goto L0008;
            ctx->cur += 2;
            goto L0007;
        L0008:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                (ctx->buffer.buf + ctx->cur)[0] != 'I' ||
                (ctx->buffer.buf + ctx->cur)[1] != 'n'
            ) goto L0009;
            ctx->cur += 2;
            goto L0007;
        L0009:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0006;
        L0007:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPIN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPIN", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PREPSU(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "PREPSU", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'S' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'u'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREP, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0006;
        goto L0004;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0004:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "PREPSU", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "PREPSU", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NOT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "NOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'N' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n'
        ) goto L0003;
        ctx->cur += 3;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "NOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "NOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_YESorNOT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "YESorNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOT, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_YESorNOT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_YESorNOT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "YESorNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TRUEorFALSE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "TRUEorFALSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'v' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0002;
        ctx->cur += 4;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_TRUEorFALSE_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'f' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[3] != 's' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'o'
        ) goto L0003;
        ctx->cur += 5;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_TRUEorFALSE_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "TRUEorFALSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "TRUEorFALSE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_THEREIS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "THEREIS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'c' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\'' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[3] != '\xa8'
        ) goto L0002;
        ctx->cur += 4;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'C' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\'' ||
            (ctx->buffer.buf + ctx->cur)[2] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[3] != '\xa8'
        ) goto L0003;
        ctx->cur += 4;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "THEREIS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "THEREIS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_THEREISorISNOT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "THEREISorISNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOT, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_THEREISorISNOT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_THEREISorISNOT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_THEREIS, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "THEREISorISNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "THEREISorISNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_IS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "IS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\xa8'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '\xc3' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\x88'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "IS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "IS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ISorISNOT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ISorISNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOT, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ISorISNOT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ISorISNOT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ISorISNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ISorISNOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ARE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 's' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0002;
        ctx->cur += 4;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'S' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o'
        ) goto L0003;
        ctx->cur += 4;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ARE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ARE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AREorARENOT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "AREorARENOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOT, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_AREorARENOT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_AREorARENOT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "AREorARENOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "AREorARENOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ISARE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ISARE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ISARE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ISARE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ISAREorISARENOT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "ISAREorISARENOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NOT, &chunk->thunks, NULL)) goto L0002;
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ISAREorISARENOT_0, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_ISAREorISARENOT_1, 0, 0);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ISARE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "ISAREorISARENOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "ISAREorISARENOT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "AT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'd'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'A' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'd'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'a'
        ) goto L0004;
        ctx->cur++;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'A'
        ) goto L0005;
        ctx->cur++;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "AT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "AT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AND(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "AND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'e' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'd'
        ) goto L0002;
        ctx->cur += 2;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'e'
        ) goto L0003;
        ctx->cur++;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "AND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "AND", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OR(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "OR", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'p' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e'
        ) goto L0002;
        ctx->cur += 6;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'd'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != 'o'
        ) goto L0004;
        ctx->cur++;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "OR", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "OR", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SPACE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SPACE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.len;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    u == 0x000020 ||
                    u == 0x000009
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n0);
            goto L0000;
        }
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SPACE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SPACE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_QUOTE(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "QUOTE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != '\"'
    ) goto L0000;
    ctx->cur++;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "QUOTE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "QUOTE", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_COMMA(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "COMMA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != ','
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "COMMA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "COMMA", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_COLON(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "COLON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != ':'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "COLON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "COLON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SEMICOLON(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "SEMICOLON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != ';'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "SEMICOLON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "SEMICOLON", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STOP(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "STOP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != '.'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "STOP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "STOP", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_QMARK(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "QMARK", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->cur] != '?'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "QMARK", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "QMARK", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_EQUAL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "EQUAL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '='
        ) goto L0002;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0003;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'u' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'l' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e'
        ) goto L0003;
        ctx->cur += 6;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPA, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_EQUAL_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "EQUAL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "EQUAL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LESS(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "LESS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '<'
        ) goto L0002;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0003;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'n' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'e'
        ) goto L0003;
        ctx->cur += 6;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0004;
            goto L0005;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0005:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_LESS_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "LESS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "LESS", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_GREATER(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "GREATER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '>'
        ) goto L0002;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_IS, &chunk->thunks, NULL)) goto L0003;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            (ctx->buffer.buf + ctx->cur)[0] != 'm' ||
            (ctx->buffer.buf + ctx->cur)[1] != 'a' ||
            (ctx->buffer.buf + ctx->cur)[2] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[3] != 'g' ||
            (ctx->buffer.buf + ctx->cur)[4] != 'i' ||
            (ctx->buffer.buf + ctx->cur)[5] != 'o' ||
            (ctx->buffer.buf + ctx->cur)[6] != 'r' ||
            (ctx->buffer.buf + ctx->cur)[7] != 'e'
        ) goto L0003;
        ctx->cur += 8;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0004;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PREPDI, &chunk->thunks, NULL)) goto L0004;
            goto L0005;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0005:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_GREATER_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "GREATER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "GREATER", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_INDENT(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "INDENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 1);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        {
            const size_t p = ctx->cur;
            size_t q;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SPACE, &chunk->thunks, NULL)) goto L0002;
            q = ctx->cur;
            chunk->capts.buf[0].range.start = p;
            chunk->capts.buf[0].range.end = q;
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_INDENT_0, 0, 1);
            thunk->data.leaf.capts.buf[0] = &(chunk->capts.buf[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_INDENT_1, 0, 1);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
        }
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "INDENT", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule__(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "_", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.len;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    u == 0x000020 ||
                    u == 0x000009
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            break;
        }
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "_", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STOPRET(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "STOPRET", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STOP, &chunk->thunks, NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_STOPRET_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "STOPRET", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "STOPRET", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_EOL(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->cur;
    PCC_DEBUG(PCC_DBG_EVALUATE, "EOL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->buffer.len - chunk->pos));
    ctx->level++;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '\n'
        ) goto L0002;
        ctx->cur++;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            (ctx->buffer.buf + ctx->cur)[0] != '\r' ||
            (ctx->buffer.buf + ctx->cur)[1] != '\n'
        ) goto L0003;
        ctx->cur += 2;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->cur] != '\r'
        ) goto L0004;
        ctx->cur++;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(PCC_DBG_MATCH, "EOL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(PCC_DBG_NOMATCH, "EOL", ctx->level, chunk->pos, (ctx->buffer.buf + chunk->pos), (ctx->cur - chunk->pos));
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

pcc_context_t *pcc_create(void *auxil) {
    return pcc_context__create(auxil);
}

int pcc_parse(pcc_context_t *ctx, char **ret) {
    pcc_thunk_array_t thunks;
    pcc_thunk_array__init(ctx->auxil, &thunks, PCC_ARRAYSIZE);
    if (pcc_apply_rule(ctx, pcc_evaluate_rule_sentence, &thunks, ret))
        pcc_do_action(ctx, &thunks, ret);
    else
        PCC_ERROR(ctx->auxil);
    pcc_commit_buffer(ctx);
    pcc_thunk_array__term(ctx->auxil, &thunks);
    return pcc_refill_buffer(ctx, 1) >= 1;
}

void pcc_destroy(pcc_context_t *ctx) {
    pcc_context__destroy(ctx);
}


int main(int argc, char *argv[]) {
  init();
  pcc_context_t *ctx = pcc_create(NULL);
  while (pcc_parse(ctx, NULL));
  pcc_destroy(ctx);
  final();
  return 0;
}